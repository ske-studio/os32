# Changelog

OS32 の変更履歴。

## v1.1 — ツリー再編、V86 で MS-DOS、ホストからの直接ファイル配置

v1.0 系から 92 コミット。KernelAPI は v26 (118 エントリ) から
**v39 (172 エントリ)** になった。

### V86 / VDM

- HDD イメージ (NHD/HDI) から **MS-DOS 5.00A が V86 上で起動する**。
  内部コマンドも外部 .EXE も走る
- カーネルスタックを 0x1FC000 へ移し、V86 ゲストに 640KB を渡せるようにした
- ゲスト由来ポインタの範囲検証を共通化して全経路に適用
- V86 セッション中の画面描画を抑止

### ツリーとビルドの再編

- `programs/` を `userland/` と `game/` の 2 層に再編
- SDK を `sdk/` として切り出し、`make sdk` で成果物化。バージョン付き
  配布 tarball とサードパーティ向けサンプルを追加
- 標準アプリを `apps/` に分離し、OS のソースツリーに依存せず SDK だけで
  ビルドする形にした (サードパーティのアプリと同じ形)
- ゲームも SDK 経由の独立ビルドにし、モノレポは委譲するだけにした
- ユーザー空間ライブラリを静的アーカイブ (.a) にまとめた
- 配備マニフェストを所有する層ごとに分割
- カーネル空間とユーザー空間のコンパイルフラグを分離
- 派生アセットとゲーム DB を生成物にし、git 追跡から外した
- `STATIC_ASSERT` を導入し、レイアウト不変条件をコンパイル時に検証

### ホストからの直接ファイル配置

- 稼働中のゲストへ、ホストから任意のユーザーランドファイルを置けるように
  した。HostDrv へ同期してゲストで `hsync` を走らせる往復が要らない。
  NP21/W 内蔵 aidebug の `/api/deploy` でゲスト物理メモリへ直接書き、
  ゲスト側の常駐エージェント (`kernel/hotdeploy.c`) が CRC を検証して
  ファイル化する
- 反映はシェルが応答している時だけ。書き換えはユーザーランドに限り、
  カーネル (`/boot/`) とシステム常駐物 (`/sys/`) はゲストが拒否する
- MCP に `emu_put_file` / `emu_deploy_status` を追加
- 無言で失敗していた配備経路 6 箇所を修復 (マニフェスト参照切れ、
  `recv` の 4KB 切り詰め、glob 解決の取り違え、成立していなかった
  `dp-%` ルール、削除済みコマンドへの push、NHD 取り込みの不親切な失敗)

### グラフィックスと表示

- 漫画向けグレースケール画像形式 **MGX** の一式を追加
- 16px ビットマップフォント生成を `gen_font16.py` に置き換え。等倍で
  読めることを基準に、はみ出す字だけ横に畳んでベースライン基準で焼く
- 外部プログラムで漢字が出ない問題を修正 (変換表フラグが別実体になる)

### ゲームとツール

- タイルマップ盤面・サウンド・セーブ・自動プレイ連携を追加
- ローカル LLM によるゲーム自動プレイ基盤 (`tools/autoplay/`)
- ゲーム UI のピクセル正確なモックアップ生成

### 信頼性 (Phase 1-3)

- カーネル内 selftest を追加。プリミティブの境界ケースを毎ブート検証する
- 全例外/IRQ ベクタに正しいスタブを配置し、`irq_save`/`irq_restore` を導入。
  盲目的な `cli`/`sti` を一掃
- pgalloc のエイリアシング解消、無言クランプの除去、境界検証
- PIC IMR の RMW を `irq_save` で保護、V86 共有状態に `volatile`
- `exec_ctx_stack` の添字ずれと `saved_esp` のネスト上書きを修正
- `kprintf` 末尾 `%` の無限ループ、`kstrncat n==0`、LZ4 境界チェック
- tick 一周・シリアル無言ドロップ・ISR とのサウンド競合
- 空白入りクォート引数が `exec` で分解される問題
- SQLite MEMSYS5 プールを 200KB から 384KB へ拡大
- `exec_heap` を `kmalloc` の共通実装に統合、デッドコード削除

### その他

- OS のバージョン文字列を `SYS_VERSION` (`include/config.h`) に一本化した。
  以前は `cmd_base.c` に直書きされており、タグと表示がずれていた

## v1.0 — 最初の安定版および公開リリース

### カーネル (73KB)

- i386 プロテクトモード + ページング + ガードページ
- IDT/PIC 完全制御 (IRQ再マッピング)
- kmalloc カーネルメモリアロケータ
- KernelAPI v26 (118エントリ) ※当時の値。現行は v1.1 の項を参照
- プログラム終了時のリソース自動回収 (FD/リダイレクト/パイプ/共有メモリ)

### ファイルシステム

- VFS 仮想ファイルシステム基盤
- ext2 読み書き対応
- FAT12 読み込み対応
- パイプバッファ (`|`)、リダイレクト (`>`, `>>`, `<`, `2>`)

### デバイスドライバ

- KBD (uPD8251A), IDE (HDD), FDC (fd0)
- Serial (RS-232C), FM (YM2203), RTC (uPD4990A), KCG (漢字ROM)

### シェル

- 外部プログラム方式の高機能シェル
- Tab補完、コマンド履歴、ワイルドカード、環境変数
- スクリプトエンジン (`if`/`else`/`for`/`while`/`goto`)
- `/etc/profile` による起動時自動設定

### コマンド (v0.1 新規追加)

- `sleep` — 指定秒数ウェイト
- `touch` — 空ファイル作成
- `hexdump` — 16進ダンプ表示
- `find` — ファイル名検索 (再帰走査)
- `sort` — 行ソート (-r/-n オプション)
- `du` — ディスク使用量表示
- `cal` — カレンダー表示 (RTC連携)
- `diff` — 簡易ファイル比較
- `time` — コマンド実行時間計測 (シェル内蔵)

### エディタ

- `edit` — VZ Editorにインスパイアされたテキストエディタ (AIを使用して移植)
  - ギャップバッファ、Undo/Redo、ブロック操作
  - ファイラー、マクロエンジン、ウィンドウ分割

### グラフィックス

- 640×400 16色 CPU直接描画
- libos32gfx ユーザーライブラリ
- サーフェス・スプライト・VBZベクタ形式
- ラスタパレットアニメーション

### ドキュメント

- README.md、INSTALL.md、CHANGELOG.md、LICENSE (MIT)
- 10部構成仕様書 + 開発ガイドライン + リリースロードマップ
- manページ 59本
