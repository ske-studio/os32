# cd

作業ディレクトリを変更する

## SYNOPSIS

`cd [PATH]`

## DESCRIPTION

指定されたディレクトリに作業ディレクトリを変更する。
引数なしの場合は `$HOME` に移動する (未設定時は `/`)。

## EXAMPLES

- `cd /bin` — /bin に移動
- `cd ..` — 親ディレクトリに移動
- `cd` — ホームディレクトリに移動

## SEE ALSO

pwd, ls
