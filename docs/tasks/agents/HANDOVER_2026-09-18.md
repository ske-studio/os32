# 引き継ぎ (2026-09-18) — 次の PM へ

> 発行: PM (Claude Code `claude-opus-5`、2026-09-18)
> 前回: [`HANDOVER_2026-09-16.md`](HANDOVER_2026-09-16.md) (その残件は §1 のとおりほぼ片付いた)

基点: `feat/gui` = **`1352a0e`**。作業ツリーは **clean**。`main` より **40 コミット**進んでいる。
`make check` / `make check-guest` / `make all` / `make external` はいずれも直近で **EXIT=0**。

## 0. まず読むもの

| 目的 | 場所 |
|---|---|
| 今やるべきこと | **本書 §2** |
| 動いているゲストを叩く | スキル **`run-os32`** (`.claude/skills/run-os32/`) — 今回作った |
| 今日踏んだ罠 | [`../../POLICY_DEBUG.md`](../../POLICY_DEBUG.md) **§4-40〜§4-43** |
| 長期計画 | [`../v3/PLAN.md`](../v3/PLAN.md) / [`../realhw/PLAN.md`](../realhw/PLAN.md) |

## 1. 前回の残件 — ほぼ片付いた

| 件 | 状態 |
|---|---|
| kstring の速度実測 | **完了**。x86 はアセンブリ維持と決定 ([`../portability/TASK_KSTRING_BENCH.md`](../portability/TASK_KSTRING_BENCH.md)) |
| 試験ランナー 段 1 (`$?`) | 完了 (前回) |
| 段 2 (合否を機械が読める形に) | **受入完了** + 追補 ([`../test/TASK_TEST_RESULT.md`](../test/TASK_TEST_RESULT.md)) |
| 段 3 (一括実行と集計) | **受入完了**。`make check-guest` が **EXIT=0** ([`../test/TASK_TEST_RUNNER.md`](../test/TASK_TEST_RUNNER.md)) |
| ARM 実装 | 未着手 (KAPI 生成器が v3 まで保留、ボード未定) |
| LAN の実機項目 | 未着手 → **狙いが変わった** ([`../realhw/PLAN.md`](../realhw/PLAN.md) §5) |

**副産物として、カーネル層の不具合が 3 件出て全部直った** (§4)。

## 1-b. 追記 (同日、PM 交代後) — **H13 は解決。H16 が新たに出て解決**

基点は `feat/gui` = **`6967e5f`**。`make check` **EXIT=0**。作業ツリー clean。

| 件 | 結果 |
|---|---|
| **穴 H13** | **解決** (`a913ca0`)。§2-1 の見立て (`WK_TEXTAREA` がカーソルキーを扱っていない) は**外れ**。
  真因は `widget::set_focus()` が自分の `WEV_FOCUS` を捨てていたこと。[`../gui/TASK_EDIT_GUI.md`](../gui/TASK_EDIT_GUI.md) §9 |
| **穴 H16 (新規)** | **解決** (`6967e5f`)。**開いて保存するだけでファイルが 1 バイトずつ伸びていた。**
  試験が不具合を期待値として固定していた。同 §10 |
| **E2** (縦スクロール) | **合格** |
| **E3** (日本語) | **合格**。折り返しは文字境界、往復はホストの元ファイルと `cmp` で IDENTICAL |
| **E6** (閉じる確認) | **合格** |
| **E4** (クリップボード) | **合格**。全角 43 文字が往復する。§1-c |
| **E5** (印刷) | **合格**。全文 299 バイトがホストへ出る |
| **E7** (書けない場所) | **合格**。`save failed, error -2` + ダイアログ。黙って成功にしない |

**票 `TASK_EDIT_GUI` は E1〜E10 が全部合格して終わり。**
**v1.4 の残りは §2-2 の 2 件 (8bpp バックエンド R2 / About) だけ。**

## 1-c. E4 / E5 — **ini の変更は要らなかった。常駐 1 本だけ**

最初「`np21x64w.ini` に LAN の設定が無いので [D2] の承認が要る」と判断したが**誤り**。
`/api/net` に聞いたら**最初から有効**で、`tx_dropped` が 158,823 — ゲストはずっと
送っていて**受け手が居なかっただけ**だった。

```bash
python3 tools/host_agent.py --listen 127.0.0.1:8026 \
  --state-dir <dir> --print-dir <dir> --spool-dir <dir> --clip file:<path>
```

起動した瞬間に 3-way HELLO が通って E4 / E5 が合格した。**承認不要。**

**教訓: 設定は ini ではなく生きている口 (`/api/net`) に聞く。**
[`../../POLICY_DEBUG.md`](../../POLICY_DEBUG.md) §4-45。

## 1-d. 報告のみ (未修正) — `widget::focused()` がアプリから使えない

引数が**窓スロットの添字**で、アプリが持つ WindowId から引けない。そのためアプリは
フォーカスを自分で追うしかなく、それが H13 を踏む前提になっていた。直すには既存の輸出の
意味を変えるか末尾に 1 本追記することになり、**どちらも契約の変更**なので承認待ち。
H13 自体は塞がっているので急がない。[`../gui/TASK_EDIT_GUI.md`](../gui/TASK_EDIT_GUI.md) §9-6。

## 2. 次にやること (推奨順)

> **§2-1 は片付いた** (§1-b)。次は §2-2 / §2-3、または §1-c の決裁。

### 2-1. ~~**穴 H13 — 編集面でカーソルキーが効かない**~~ ← **解決済み (§1-b)**

[`../gui/TASK_EDIT_GUI.md`](../gui/TASK_EDIT_GUI.md) §8。**切り分け済み**:

- **文字は届く** (`ZZZ` が入る) → フォーカスは編集面にある
- **注入側には矢印キーがある** (`aidebug_keys.cpp` の `UP` 0x3a / `DOWN` 0x3d ほか)
- **ファイル選択の一覧では `DOWN` が効く** → 同じ注入で受け手が違うと効かない

→ **`WK_TEXTAREA` のキー処理がカーソルキーを扱っていない。**

**直せば受入 E2 / E3 / E4 が一度に動く。**
E5 (印刷) / E6 (閉じる確認) / E7 (書けない場所) は **H13 と独立なので先に確かめられる。**

### 2-2. v1.x の残り 2 件

| 件 | 備考 |
|---|---|
| PEGC / Cirrus の 8bpp バックエンド (R2) | 未着手。**ユーザーは後回しでよいと明言** |
| About ダイアログ | 未着手。小物 |

**テキストエディタが終われば v1.4 は実質完了。**

### 2-3. 積み残しの小物

- `manifest_missing` の集計 (別票)、H4 の世代ディレクトリ
- `cp -r` の 64 項目での部分コピー、`cat -n` のファイル跨ぎ
- 完了票の `archive/` 移動 (`tools/move_docs.py`)
- **第 2 陣の試験の適合** (画面デモ・対話・ベンチ、`hal_test` の色判定)
  — [`../test/TASK_TEST_RESULT.md`](../test/TASK_TEST_RESULT.md) §8

## 3. 今日決まったこと (覆さないこと)

| 決裁 | 内容 |
|---|---|
| kstring | **x86 はアセンブリ維持**。C 版は他 32 ビットアーキ向け |
| 移植の対象 | **32 ビットのみ**。16/64 ビットは範囲外。64 ビット抽象化を足さない |
| メモリ配置 D1 | SHM 224KB / スタックを `0x2FC000` へ / 予算 468KB |
| メモリ配置 D2 | `KHEAP_BASE` は**浮かせたまま**、門はリンカの `ASSERT` |
| 次期カーネルの呼び方 | **v3** (2.0 が出荷済みなので「v2」と呼ばない。2026-09-15 の決裁) |
| v3 の順序 | **C11 → メモリマップ再配置 → ドライバの動的読み込み → PCI → Intel 82557** |
| 実機 | **当面使わない**。検証はエミュレータで十分 |
| 実機 LAN | **オンボードの Intel 82557**。LGY-98 は高価・品薄で取り下げ。**既存ドライバは消さない** |
| Trident | **保留**。NP21/W の実装が結線されておらず検証できない |
| GUI 契約 | 部品は**末尾追記のみ** (`WK_TEXTAREA` を追加、`WK_TEXTBOX` は不変) |
| エディタ | **機能は最低限**。取り消しも検索も要らない |

## 4. 今日直したカーネル層の不具合 3 件

**どれも「人間の目視では見つからない」種類だった。** 機械に読ませて初めて露見した。

1. **`db_last_error()` がカーネル番地を返す** ([`../sqlite/TASK_DB_ERRSTR.md`](../sqlite/TASK_DB_ERRSTR.md))
   — 実機で DB を調べる唯一の道具が**エラー時に必ず死ぬ**状態。しかも文書が
   「診断には必ずこれを出せ」と指示していた。
2. **SHM 帯がカーネルスタックを飲んでいた** ([`../memory/TASK_KSTACK_USER.md`](../memory/TASK_KSTACK_USER.md))
   — スタックが設計の 16KB ではなく **4KB**。**余裕は 224 バイト**で、あと少し育てば三重フォルトだった。
3. **`fstat` がリダイレクトを見ず `isatty` と食い違う** ([`../test/TASK_FSTAT_REDIR.md`](../test/TASK_FSTAT_REDIR.md))
   — 対話で叩く限り正しく見える。**3 段構えの穴**だった (API が食い違い、試験が検出できず、終了コードも出ない)。

## 5. 今日整えた道具

| 道具 | 効果 |
|---|---|
| スキル **`run-os32`** | 組んで、動いているゲストを叩く入口。`doctor` / `cmd --wait` / `pull` / `shot` / `deploy` / `smoke` |
| `tools/guest_tests.py` + `make check-guest` | ゲストで一括実行してホストで集計。**終了コードと集計行の両方を突き合わせる** |
| `tools/gen_memmap.py` | メモリマップの**生成**と、重なり・逆転・写しのずれの検査 |
| `tools/check_tree_unchanged.py` | 試験がソースを書き換えたまま戻していないかの番人 |
| **並列ビルド** | `make kernel` 23.7→11.8 秒、`make all` 70.1→31.0 秒、**`make check` 9 分以上→152 秒** |

## 6. 罠 (`POLICY_DEBUG.md` に立項済み)

| § | 内容 |
|---|---|
| **4-40** | **`make check` を途中で止めると変異がソースに残る。** `git add -A` が壊れたコードを拾う。打ち切ったら必ず `git status` |
| 4-41 | ビルドと検査の並列化。**`check` は 2 段** (書き換えない 48 本 → 変異する 10 本を逐次) |
| **4-42** | **GUI アプリの窓が静かに出ない → 共有ライブラリが古い。** `hsync` は既定で `/sys` を外すので **`hsync sys` + リセット**。例外 0 件で窓だけ出ないのが目印 |
| 4-43 | (解決済み) キー注入の大文字。**CAPS LOCK が ON だと逆に小文字になる**点は残る |

**もう 1 つ、文書に無いが重要な教訓**:
**腐ったセッションで見たことを「そういう仕様」と書かない。** 2026-09-17 に実際にやってしまい、
「`source` は rshell では動かない」という誤った注記を残した。**開き直してから測り直すのが先。**

## 7. 未コミットの外部変更 (触らないこと)

`/home/hight/np21w-src` に**他人の未コミット作業が 13 件ある** (ネットワーク監視らしきもの)。
今日 `aidebug_keys.cpp` を直したが、**コミットしていない** (混ざるため)。
**`git` の変更操作をあのリポジトリで実行しないこと。**

## 8. 環境の注意

- **コーダーの worktree はセッション開始時のコミットに固定される。** ブランチの先端からは生えない。
  **`git worktree add <path> -b <branch> HEAD` で PM が用意する**こと。
- worktree に `.env` が無いので、ビルドは `make CROSS_DIR=/home/hight/opt/cross <target>`。
- `make check` は **152 秒**。打ち切らないこと (§4-40)。
