# エージェント運用体制 — 役割と起動

状態: **現行 (2026-09-09 発効、2026-09-14 に v1.4 の分担を追加 = §0)**。このファイルは**今の体制だけ**を書く。
過去の体制変更の経緯は [RETROSPECTIVE_2026-09-09.md](RETROSPECTIVE_2026-09-09.md) にあり、
このファイルからは辿らせない (入口を履歴で太らせないため)。

## 0. v1.4 からの分担 (ユーザー指示 2026-09-14、境界の確定は同日)

v1.4 (ROADMAP §1) のうち**アプリケーション層だけ**を別の AI エージェントが担う。基盤側は従来の 4 役 (§1) のまま Claude Code が PM。

| 範囲 | 実装 PM | Claude Code の役 |
|---|---|---|
| **基盤**: Host Services N1 (ゲスト受入・実装レビュー) / N2 (Agent の PRINT / CLIP / PUT) / N3 (`libos32host` とコマンド) / N4 の libos32gui `host_*` 末尾追記 / R2 (PEGC / Cirrus 8bpp) | Claude Code (§1 の PM) | PM |
| **アプリケーション層**: About ダイアログ / GUI テキストエディタ / N4 のアプリ側 (ファイラの「印刷」、端末のコピー / 貼り付け) | 別エージェント (ユーザーが立てる) | **設計者 + レビュアー**: 票の設計 (契約・使う API・受入項目、Fable サブエージェントの設計レビューを通す)、実装レビュー (Fable サブエージェント、§5 の網羅指示)、受入結果のレビュー。実装・着地・配備はしない |

境界の規則: アプリ側は libos32gui / libos32cfg / libos32host の**公開ラッパーだけ**を使う (KAPI 直呼び・カーネル・shlib 本体・ビルド系の変更は基盤側の票にする)。アプリ側が基盤に API を要求するときは、票に「要求 API」として書き、Claude Code が基盤票に起こす。
引き継ぎの入口は [HANDOVER_v14.md](HANDOVER_v14.md)。

## 1. 4 役 (基盤側は v1.4 でもこのまま)

| 役 | 実体 | できること | してはいけないこと |
|---|---|---|---|
| **PM** | Claude Code 対話セッション (`claude-fable-5-1`) | 分解・受入判定・git 操作・[D1]〜[D3] の承認取得・エミュレータの**観測** (`gui_gate.py` 等の PM 所有ツール) | 実装。**ビルド・試験・配備の実行** (テスターの領分。PM は結果の合否だけ)。手を動かし始めたら分解に戻る |
| **コーダー** | Claude Code サブエージェント (`claude-opus-5`)、worktree 隔離 | 実装 + ホスト TDD (`tools/tests/`、新挙動ごとの RED→GREEN の記録)。コンパイル確認の最小ビルドは可 | 配備・コミット・エミュレータ操作。**全体ゲート (`make all` / `make external` / `make check`) の実行**はテスター |
| **レビュアー** | **当面は Fable 5.1 サブエージェントのみ** (`Agent(model: "fable", isolation: "worktree")`、新規・fork 不可、読むだけ) — ユーザー指示 2026-09-14。設計レビューも実装レビューも Fable。Codex (`codex exec`) は使わない (N1 で 2 回続けて不安定 → 判定を出さずハング)。コーダー (Opus 5) とは別モデルであることが独立性の要件は満たす | 独立レビュー、所見の列挙 | — (PM はレビューを自分で代行しない。材料は §5 の書式 + 決裁表で出す) |
| **テスター** | ローカル AI (FastFlowLM, `127.0.0.1:52625`、`tools/emu_agent/`) | **ビルド (`make …`)・ホスト試験 (`make check`)・配備 (`os32-cycle deploy`)・実機操作・回帰の実行** (ユーザー指示 2026-09-09/11) | 合否の判断。台本にない操作。ini |

**NP21/W の ini を変更してよいのは PM だけ** (操作ごとに [D2] の承認)。
コーダー・テスター・レビュアーには権限が無い。手順はスキル **`os32-emu-config`**。
触る前に、ゲスト側の設定 (`gfxmode` など) で足りないかを先に確かめる。

## 2. 起動 (2026-09-09 に実行して確認した形)

```bash
# PM — 対話セッションで /model claude-fable-5-1
#      (modelUsage に claude-fable-5-1 が出ることで確認する)

# コーダー — Claude Code の Agent tool
#   subagent_type: 任意, model: "opus", isolation: "worktree"
#   worktree は .gitignore 済みの .claude/worktrees/ に作られる
#   CLI から直接叩く場合: claude -p --model claude-opus-5 "<指示>"

# レビュアー — 起動コマンドは無い。PM がレビュー依頼をまとめてユーザーに報告し、
#   ユーザーが ChatGPT に渡す。§5 の書式で出す。

# テスター — 単発 / 定型回帰 / ログ確認
python3 tools/emu_agent/agent.py run "<タスク>"
python3 tools/emu_agent/agent.py suite tools/emu_agent/tasks/regress.txt
python3 tools/emu_agent/agent.py tail
```

**ビルド・ホスト試験・配備・実機検証は PM が手で回さず、この役に投げる。**
投げ方と結果の読み方はスキル **`os32-local-ai`**、何を確かめるべきかの選択は
スキル **`os32-build-verify`** が正典。

厳格版が要る操作 (GUI 入力など、誤操作が実機を壊す経路) は `tools/emu_agent/playbook.py` を使う。
承認済み JSON 台本と操作・引数・順序が完全一致したものだけを実行し、既定は dry-run。

**FLM のモデルは、ユーザーがロード済みのものだけに投げる。**
`/v1/models` に並んでいることはロード済みを意味せず、未ロード名を投げると自動ダウンロードが始まる。

## 3. 規約 (3 行)

1. **PM が止まってよいのは 4 つだけ** — [D1]/[D2]/[D3] の承認、仕様の分岐、スコープ拡大、
   そして**独立レビューが要る地点** (§5 の書式でユーザーに報告して渡す)。
   それ以外は承認済みスコープを最後まで進める。各ステップの終わりに「次に進んでよいか」を訊かない。
2. **ブロックできるのは到達可能な反例を伴う所見だけ**。現契約の違反はブロック、将来要件は別ゲートに落とす。
   反例を示せない指摘で、正しいコードを変えない。誤検出はレビュー記録側を訂正する。
3. **エミュレータは同時に 1 オペレータ**。コーダーは触らない。共有ファイルの編集は PM が直列化する。

## 4. 合否の判定

テスターの `RESULT: ok` は**自己申告**として扱う。合否は PM が
`tools/emu_agent/logs/<session>/steps.jsonl` の `obs` を読んで決める。
配備の反映確認は [08_build.md §8-4](../../08_build.md#配備3経路) の経路と、
`os32-cycle deploy` のゲスト側サイズ照合による ([V1]/[V4])。

## 5. レビュー依頼の出し方

> **当面のレビュアー (ユーザー指示 2026-09-14)**: **Fable 5.1 サブエージェントのみ**。設計・実装とも `Agent(subagent_type: general-purpose, model: "fable", isolation: "worktree")` に、下記の書式 + 網羅指示 + 「読むだけ。編集・ビルド・配備・コミット禁止」を渡す。**Codex は当面使わない** (N1 の実装レビューで 2 回続けて判定を出さずハング / テンプレート丸写し)。下の「2026-09-12 Codex 決裁」「Codex 枯渇時」の記述は経緯として残すが、現行は Fable 単独。往復は 3 回 + 追加 1 往復まで、収束しなければユーザーへ。

レビュアーは**ユーザー経由**なので、PM が投げっぱなしにできない。
レビューが要ると判断したら作業を止め、次の 5 点を揃えて報告する。
自分でレビューを代行して「問題なし」と結論づけない。

| 項目 | 内容 |
|---|---|
| 対象 | コミット SHA か `git diff` の範囲、主なファイル |
| 変更の要旨 | 何を、なぜ変えたか (3 行以内) |
| 見てほしい観点 | 正しさ / C89・[C1]〜[C4] 準拠 / ABI / 資源回収 など、絞って挙げる |
| 判定基準 | **到達可能な反例を伴う所見だけがブロック要因**。将来要件は別ゲート |
| 既知の未確認 | ゲスト未検証の範囲、呼び出し元が無い API など |

所見が返ってきたら、**鵜呑みにせず自分で反例の到達可能性を確かめてから**着手する
(2026-09-09 の PEGC 回帰は、実機で `sys_frozen_hot` を読んで確定させてから直した)。
誤検出だった場合はコードではなくレビュー記録側を訂正する。

**2026-09-12 ユーザー決裁**: Codex (`codex-cli`、`gpt-6-astra`) のクォータが回復したので、設計票・実装の独立レビューは
**PM が `codex exec -s read-only` で自動的に依頼し、所見の反例を確かめて票を直し、承認が出るまで往復する** (枯渇したら
ユーザー経由に戻す)。起動は `codex exec -s read-only "<依頼文>"` (読むだけ、コードは直さない、リポジトリは trusted 登録済み)。
依頼文には対象の票と参照コードのパス、求める出力の書式 (判定 / blocker と到達可能な反例 / 非 blocker) を書く。
**往復は 3 回まで**: 3 回のレビューで Approve に至らなければ、争点を整理してユーザーに判断を求める (ユーザー指示 2026-09-12)。
**網羅性の要求 (ユーザー指示 2026-09-13)**: T9 の実装レビューで往復ごとに別領域の blocker が 1 件ずつ出て 5 往復しても
収束しなかった。依頼文には必ず「**今回の往復で見つかる到達可能な欠陥をすべて挙げる** (1 件見つけて止めない)。
対象の経路を列挙し (例: CPL=3 に持ち込んだコードが呼ぶ KAPI 全部、カーネル帯のポインタを渡す箇所全部、
端末モデルに依存する出力全部)、経路ごとに見た / 見ていないを明記する。次の往復で新しい領域の指摘を出すなら、
今回なぜ見えなかったかを書く」を入れる。往復数を数える単位は「指摘の集合」であって「1 件」ではない。
**Codex 枯渇時のレビュアー (ユーザー決裁 2026-09-13)**: `Agent(subagent_type: general-purpose, model: "fable", isolation: "worktree")` に、Codex 向けと同じ依頼文 (対象・参照コード・網羅指示・判定基準・出力書式) + 「読むだけ。編集・ビルド・配備・コミット禁止」を渡す。worktree 隔離なので誤って編集しても本線に触れない。往復の規則 (3 往復 + 追加 1 往復) は同じ。候補の比較: Opus 5 はコーダーと同じモデルで盲点を共有しやすいので次点、Sonnet 5 は「直ったかの確認だけ」の往復に、ローカル AI は不可。
