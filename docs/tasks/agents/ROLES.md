# エージェント運用体制 — 役割と起動

状態: **現行 (2026-09-09 発効、2026-09-14 に v1.4 の分担を追加 = §0)**。このファイルは**今の体制だけ**を書く。
過去の体制変更の経緯は [RETROSPECTIVE_2026-09-09.md](RETROSPECTIVE_2026-09-09.md) にあり、
このファイルからは辿らせない (入口を履歴で太らせないため)。

## 0. 実装体制 (2026-09-14 更新)

> **2026-09-25 のユーザー指示 (最新、下の記述より優先)**: **レビュアーは Codex だけ** (`codex exec -s read-only`)。**コーダーは、Codex が重大 (P1 / major) と判定した指摘を含む修正なら Fable 5.1 サブエージェント** (`Agent(model: "fable")`)、**それ以外は Opus 5.5** (`Agent(model: "opus")`)。Fable / Opus のレビューは行わない。3 ラリーで決着しない争点はユーザーへ (従来どおり)。
>
> **2026-09-23 夕のユーザー指示 (最新、下の記述より優先)**: Fable の枯渇に伴い **PM = Opus 5.5** (`claude-opus-5-5`)。
> **レビュアー = Fable 5.1 サブエージェント + Codex の 2 者**で、実装方針を**両者に突き合わせさせる**。
> **3 ラリーで決着がつかない争点はユーザーに判断を仰ぐ**。コーダーは従来どおり Opus 5.5 サブエージェント。
> **同日、Fable が月間上限に達した** → ユーザー決裁で **Fable の代わりに Opus 5.5 サブエージェント** (`Agent(model: "opus")`、読むだけ) を 2 人目のレビュアーにする。コーダーと同じモデルなので、依頼文でコーダーの報告を渡さず独立に読ませる。

**2026-09-14 のユーザー指示で、アプリ層を別エージェントに渡す方針は取りやめ**。基盤もアプリ層も
**実装は Claude Code (PM) が Opus 5.5 サブエージェント (`Agent(model: "opus")`、worktree は PM が明示的に切る) で行う** (§1 の 4 役に一本化。**2026-09-23 ユーザー指示: コーダーは Opus 5.5**。`model: "opus"` は最新の Opus に解決されるので、起動時にコーダーへ自分のモデル ID を報告させて 5.5 であることを確かめる)。
レビューは当面 Fable サブエージェント単独 (§5)。以前ここにあった「アプリ層 = 別エージェント」の分担表は撤回した。

## 1. 4 役 (基盤側は v1.4 でもこのまま)

| 役 | 実体 | できること | してはいけないこと |
|---|---|---|---|
| **PM** | Claude Code 対話セッション (**`claude-opus-5-5`、2026-09-23 夕〜**。それまでは `claude-fable-5-1`) | 分解・受入判定・git 操作・[D1]〜[D3] の承認取得・エミュレータの**観測** (`gui_gate.py` 等の PM 所有ツール) | 実装。**ビルド・試験・配備の実行** (テスターの領分。PM は結果の合否だけ)。手を動かし始めたら分解に戻る |
| **コーダー** | Claude Code サブエージェント (**Opus 5.5**、2026-09-23〜)、worktree 隔離 (PM が基点を指定して切る) | 実装 + ホスト TDD (`tools/tests/`、新挙動ごとの RED→GREEN の記録)。コンパイル確認の最小ビルドは可 | 配備・コミット・エミュレータ操作。**全体ゲート (`make all` / `make external` / `make check`) の実行**はテスター |
| **レビュアー** | **Codex** (`codex exec -s read-only`、§5) — **2026-09-14 に Fable が枯渇したためユーザー指示で復帰**。Fable サブエージェントは枯渇時の代替として §5 に残す。Codex は N1 で 2 回ハングした実績があるので、**依頼は範囲を絞る** (巨大 diff を丸ごと読ませない、対象ファイルを列挙、timeout を付ける) こと | 独立レビュー、所見の列挙 | — (PM はレビューを自分で代行しない。材料は §5 の書式 + 決裁表で出す) |
| **テスター** | ローカル AI (FastFlowLM, `127.0.0.1:52625`、`tools/emu_agent/`) | **ビルド (`make …`)・ホスト試験 (`make check`)・配備 (`os32-cycle deploy`)・実機操作・回帰の実行** (ユーザー指示 2026-09-09/11) | 合否の判断。台本にない操作。ini |

**NP21/W の ini を変更してよいのは PM だけ** (操作ごとに [D2] の承認)。
コーダー・テスター・レビュアーには権限が無い。手順はスキル **`os32-emu-config`**。
触る前に、ゲスト側の設定 (`gfxmode` など) で足りないかを先に確かめる。

## 2. 起動 (2026-09-09 に実行して確認した形)

```bash
# PM — 対話セッションで /model claude-fable-5-1
#      (modelUsage に claude-fable-5-1 が出ることで確認する)

# コーダー — Claude Code の Agent tool
#   subagent_type: 任意, model: "opus", isolation: "worktree"
#   worktree は .gitignore 済みの .claude/worktrees/ に作られる
#
#   **基点の確認 (2026-09-15 追加、2 度踏んだ)**: worktree は作られた時点の HEAD から始まり、
#   `main` 側の古いマージ (`fac0d89` 等) になっていることがある。B8 と順序 2 のコーダーが
#   これを踏み、後者は「HEAD は 086bda5」と**確かめずに**報告した。規則:
#     - PM は依頼文に**期待する基点の SHA** を書く。
#     - コーダーは最初に `git log --oneline -1` を実行し、**その出力を報告の先頭に書く**。
#       期待と違えば `git checkout <SHA>` で合わせる (`--hard` は使わない)。git が
#       ガードで弾かれて確かめられないなら**その旨を報告し、推測で書かない**。
#     - PM は着地前に worktree の `git log -1` を自分で確認する。
#     - 参照すべき文書やファイルが「無い」と報告されたら、PM はまず基点を疑う。
#   CLI から直接叩く場合: claude -p --model opus "<指示>" (最新の Opus = 5.5)

# レビュアー — 起動コマンドは無い。PM がレビュー依頼をまとめてユーザーに報告し、
#   ユーザーが ChatGPT に渡す。§5 の書式で出す。

# テスター — 単発 / 定型回帰 / ログ確認
python3 tools/emu_agent/agent.py run "<タスク>"
python3 tools/emu_agent/agent.py suite tools/emu_agent/tasks/regress.txt
python3 tools/emu_agent/agent.py tail
```

**ビルド・ホスト試験・配備・実機検証は PM が手で回さず、この役に投げる。**
投げ方と結果の読み方はスキル **`os32-local-ai`**、何を確かめるべきかの選択は
スキル **`os32-build-verify`** が正典。

厳格版が要る操作 (GUI 入力など、誤操作が実機を壊す経路) は `tools/emu_agent/playbook.py` を使う。
承認済み JSON 台本と操作・引数・順序が完全一致したものだけを実行し、既定は dry-run。

**FLM のモデルは、ユーザーがロード済みのものだけに投げる。**
`/v1/models` に並んでいることはロード済みを意味せず、未ロード名を投げると自動ダウンロードが始まる。

## 3. 規約 (3 行)

1. **PM が止まってよいのは 4 つだけ** — [D1]/[D2]/[D3] の承認、仕様の分岐、スコープ拡大、
   そして**独立レビューが要る地点** (§5 の書式でユーザーに報告して渡す)。
   それ以外は承認済みスコープを最後まで進める。各ステップの終わりに「次に進んでよいか」を訊かない。
2. **ブロックできるのは到達可能な反例を伴う所見だけ**。現契約の違反はブロック、将来要件は別ゲートに落とす。
   反例を示せない指摘で、正しいコードを変えない。誤検出はレビュー記録側を訂正する。
3. **エミュレータは同時に 1 オペレータ**。コーダーは触らない。共有ファイルの編集は PM が直列化する。

## 4. 合否の判定

テスターの `RESULT: ok` は**自己申告**として扱う。合否は PM が
`tools/emu_agent/logs/<session>/steps.jsonl` の `obs` を読んで決める。
配備の反映確認は [08_build.md §8-4](../../08_build.md#配備3経路) の経路と、
`os32-cycle deploy` のゲスト側サイズ照合による ([V1]/[V4])。

## 5. レビュー依頼の出し方

> **現行のレビュアー (2026-09-14 更新)**: **Codex** (`codex exec -s read-only "<依頼文>"`)。Fable 5.1 サブエージェントが枯渇したため復帰 (ユーザー指示)。**Codex は過去 2 回ハングした**ので、依頼は対象ファイルを列挙して範囲を絞り、`timeout` を付け、判定は `## 判定` を `rfind` で拾う。Fable が回復したら代替として使える。往復は 3 回 + 追加 1 往復まで、収束しなければユーザーへ。

レビュアーは**ユーザー経由**なので、PM が投げっぱなしにできない。
レビューが要ると判断したら作業を止め、次の 5 点を揃えて報告する。
自分でレビューを代行して「問題なし」と結論づけない。

| 項目 | 内容 |
|---|---|
| 対象 | コミット SHA か `git diff` の範囲、主なファイル |
| 変更の要旨 | 何を、なぜ変えたか (3 行以内) |
| 見てほしい観点 | 正しさ / C89・[C1]〜[C4] 準拠 / ABI / 資源回収 など、絞って挙げる |
| 判定基準 | **到達可能な反例を伴う所見だけがブロック要因**。将来要件は別ゲート |
| 既知の未確認 | ゲスト未検証の範囲、呼び出し元が無い API など |

所見が返ってきたら、**鵜呑みにせず自分で反例の到達可能性を確かめてから**着手する
(2026-09-09 の PEGC 回帰は、実機で `sys_frozen_hot` を読んで確定させてから直した)。
誤検出だった場合はコードではなくレビュー記録側を訂正する。

**2026-09-12 ユーザー決裁**: Codex (`codex-cli`、`gpt-6-astra`) のクォータが回復したので、設計票・実装の独立レビューは
**PM が `codex exec -s read-only` で自動的に依頼し、所見の反例を確かめて票を直し、承認が出るまで往復する** (枯渇したら
ユーザー経由に戻す)。起動は `codex exec -s read-only "<依頼文>"` (読むだけ、コードは直さない、リポジトリは trusted 登録済み)。
依頼文には対象の票と参照コードのパス、求める出力の書式 (判定 / blocker と到達可能な反例 / 非 blocker) を書く。
**往復は 3 回まで**: 3 回のレビューで Approve に至らなければ、争点を整理してユーザーに判断を求める (ユーザー指示 2026-09-12)。
**網羅性の要求 (ユーザー指示 2026-09-13)**: T9 の実装レビューで往復ごとに別領域の blocker が 1 件ずつ出て 5 往復しても
収束しなかった。依頼文には必ず「**今回の往復で見つかる到達可能な欠陥をすべて挙げる** (1 件見つけて止めない)。
対象の経路を列挙し (例: CPL=3 に持ち込んだコードが呼ぶ KAPI 全部、カーネル帯のポインタを渡す箇所全部、
端末モデルに依存する出力全部)、経路ごとに見た / 見ていないを明記する。次の往復で新しい領域の指摘を出すなら、
今回なぜ見えなかったかを書く」を入れる。往復数を数える単位は「指摘の集合」であって「1 件」ではない。
**レビュアーの在庫 (2026-09-16 時点。枯渇したら次へ)**

| 順 | レビュアー | 起動 | 状態 (2026-09-16) |
|---|---|---|---|
| 1 | Codex | `codex exec -s read-only "…" < /dev/null` | **クォータ切れ (9/19 まで)** |
| 2 | Antigravity CLI (`agy`) | 下の節 | **クォータ切れ (72 時間)** |
| 3 | Fable 5.1 サブエージェント | `Agent(model: "fable", isolation: "worktree")`、読むだけ | **上限に達した (429)** |
| 4 | Opus 5 サブエージェント | 同上、`model: "opus"` | 使える。ただしコーダーと同じモデルなので盲点を共有する |
| 5 | **ローカルモデル直接方式** (`tools/review_local.py`) | スキル **`os32-local-review`**。対象を行番号つきで埋め込み 1 回で返させる。**opencode は使わない** | **使える (2026-09-16 に実測)**。既定 **gemma4:31b** が既知 9 件を 43 秒で満点・作り話 0。実装レビューで設計の穴を 1 件検出した実績あり。**範囲は 800 行前後に絞る** (広げると落ちる) |

4 つとも尽きたら、レビューが要る地点で止まってユーザーに報告する (§3 の規約 4 番目)。

**ローカルモデルによる補助レビュー (2026-09-16 に実測)**

手順と落とし穴の正典はスキル **`os32-local-review`**、道具は `tools/review_local.py`。要点だけ:

- **opencode は使わない** (初期化後に固まる / 文脈溢れで同じ読み直しを 77 回繰り返した)。
  対象ファイルを**行番号つきで依頼文に埋め込み、1 回の要求で返させる**。
- 実力: 挙げた所見は信用できるが (作り話 0)、**挙げなかったことは「無い」の証拠にならない** (既知 9 件中 5 件)。
  主レビュアーの代わりにはならない。所見は PM が反例の到達可能性を確かめてから採る。
- 票の往復記録に**どのモデルが見たかを書く**。

**Codex 枯渇時のレビュアー (ユーザー指示 2026-09-16、優先)**: **Antigravity CLI (`agy`)** に投げる。読み取り専用で回す形は
2026-09-16 に実地で確認した:

```bash
# レビュー依頼 (読むだけ。--dangerously-skip-permissions は使わない)
cd /home/hight/os32 && timeout 1500 agy --mode plan --print-timeout 4m \
    --print='<依頼文>' < /dev/null
```

- **プロンプトは `--print=` に貼り付ける** — `--print` を裸で置くと次のフラグをプロンプトとして飲み込む。
- ヘッドレスは許可を聞けないので、`~/snap/antigravity-cli/common/.gemini/antigravity-cli/settings.json` の
  `permissions.allow` に**読み取り系だけ**を入れてある (`read_file(*)` `list_directory(*)` `grep_search(*)`
  `find_filepath(*)` `view_code_item(*)`)。**`--dangerously-skip-permissions` は使わない** (全ツールが通ってしまう)。
  書き込み系を許可に足さない限り、`agy` はファイルを変更できない。
- 依頼文の中身 (対象・参照コード・網羅指示・判定基準・出力書式) は Codex と同じものを使う。往復の規則も同じ。

**Codex 枯渇時のレビュアー (ユーザー決裁 2026-09-13、次点)**: `Agent(subagent_type: general-purpose, model: "fable", isolation: "worktree")` に、Codex 向けと同じ依頼文 (対象・参照コード・網羅指示・判定基準・出力書式) + 「読むだけ。編集・ビルド・配備・コミット禁止」を渡す。worktree 隔離なので誤って編集しても本線に触れない。往復の規則 (3 往復 + 追加 1 往復) は同じ。候補の比較: Opus 5 はコーダーと同じモデルで盲点を共有しやすいので次点、Sonnet 5 は「直ったかの確認だけ」の往復に、ローカル AI は不可。
