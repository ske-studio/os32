# 引き継ぎ 2026-09-22 — 実機 (PC-9821Ra266) 初日: FD 起動・シリアル 115200・LAN の土台

> 発行: PM (Claude Code `claude-fable-5-1`、2026-09-22)。前の引き継ぎは [`HANDOVER_2026-09-18.md`](HANDOVER_2026-09-18.md)。

## 今日の到達点

| 件 | 状態 | 票 |
|---|---|---|
| 実機の FD 起動 | **合格**。原因 3 つ (FDC の IRQ 待ちがエミュレータ基準 / `0439h` bit2 の DMA 1MB 制限 / FRY 未設定) はどれも NP21/W では出ない | [`../realhw/TASK_FDC_REALHW.md`](../realhw/TASK_FDC_REALHW.md) |
| kprintf の属性 | PC/AT 流の 0x07 などが PC-98 では**見えていなかった** (エミュレータでも)。入口で変換 | POLICY_DEBUG §4-52 |
| シリアル | 9600 = 回線上限 952 B/s (校正の丸めを修正)、V･FAST 115200 で 3.2 KB/s (残る壁は出力側)。番犬は明示の `serial ack`。ホスト道具 `tools/rshell_serial.py` (`--fast N`) | [`../realhw/TASK_SERIAL_VFAST.md`](../realhw/TASK_SERIAL_VFAST.md) |
| LAN の土台 | L-A PCI 列挙 (`lspci` / `pcidump`、KAPI **v58**) と L-D Linux の橋 (`tools/lan_bridge.py`) を着地。実機・実 NIC は未確認 | [`../realhw/TASK_LAN_82557.md`](../realhw/TASK_LAN_82557.md) |

KAPI: v55 → **v58** (v56 serial_init_vfast / serial_get_status、v57 serial_putchar の戻り値 + kbd_trygetchar_local、v58 pci_*)。
カーネル本体 **446.9KB / 468KB (残り 21.1KB)**。

## 決裁 (ユーザー、2026-09-22)

- LAN は v3 の順序 (動的読み込みが先) を待たず、PCI 列挙 → 82557 を静的で入れる (足りなければ SHM を 16KB 削る)。
- 実機のホストは **Ubuntu ノート** (スイッチ直結)。橋は AF_PACKET。この Windows/WSL 機は手で操作する場面だけ。
- シリアルの保険 (番犬) は往復を重ねて直す (Codex 4 往復で打ち止め、最後は設計変更で閉じた)。

## 2026-09-23 の追記 — 結線の土台の設計票

- [`../v3/TASK_HAL_WIRING.md`](../v3/TASK_HAL_WIRING.md) を v4 まで起こした (割り込みの動的登録 / 8237 共通部 / DMA プール / PCI 結線表 / µs 時計)。
  Codex の設計レビューは **3 往復とも Request changes** (v1: 14 件、v2: 9 件、v3: 7 件。件数は減り、v4 で全部反映済み)。
  その後ユーザー許可で往復 4〜7 を実施 (v5 で IRQ0 の汎用 tick 回収を取り下げ、v6/v7 で共有 IRQ の境界を詰めた)。
  **往復 7 (最終) は Request changes だが、残りは「実装前に決める設計判断 2 点」(登録拒否 = 利用不可、動的 IRQ の全登録者に定期回収) と
  「実装レビューで拾う 1 点」(NE2000 の IMR shadow 同期) で、PM が決めて v8 に固定した。実装着手はユーザー判断**。
  ユーザーは v8 を了承し、実装 A (1-1 + 1-5) / B (1-2 + 1-3 + 1-4) を Opus 5 コーダー 2 本で並行開始、設計レビューをさらに 3 往復許可。
  **往復 8〜10 (v9〜v11) で終了**: 往復 10 の残 blocker は R10-1 (CR0.WP = 0 なので出力引数の KAPI が共有ライブラリの .text に書けてしまう) の 1 件で、
  PM が「新設の出力 KAPI は `ring3_user_range_writable` を通し、通らなければ kill」と決めて v11 に固定。以後は実装レビュー。
  **既存の出力 KAPI にも同じ穴がある** (Codex 往復 10) — 別票の残件。
  ユーザー指示「解決までレビュアーを使ってよい」で往復 11〜12 を実施、**往復 12 で Approve** (v13)。指摘数の推移: 14 → 9 → 7 → 6 → 4 → 3 → 3 → 3 → 3 → 1 → 1 → 0。
- 決裁待ち 2 点: (1) DMA プールを SQLite 帯の予約域 **0x2E8000〜0x2F7FFF** に固定 (SHM・GUI・SDK・アプリ無変更、SQLite の余地 120KB → 44KB)、
  (2) **PIT の分周が 1.9968MHz 決め打ちの既存バグ** (実機 Ra266 は 2.4576MHz なので tick = 8.125ms、待ち・番犬・校正が全部 23% 速い) を票 1-0 として先に単独で着地。
- **1-0 (PIT のクロック修正) は着地済み** (7269639、Opus 5 コーダー + `make check`)。NP21/W では `kselftest_fail` = 0、tick 99.3Hz で不変。
  **実機の確認が残件**: FD を書き直し → 起動画面に `PIT 2.4576M` → `uptime` を 120 秒あけて 2 回 (直る前は 148 秒進む)。POLICY_DEBUG §4-54。
- **実装 A/B/v60/修正を合流し NP21/W で受入** (票 §3-1): kselftest 187/187、`time_test` (CPL=3) 合格、FD 書き戻し md5 一致、予算 459.8/468KB。
  切り分けに 1 時間かかった罠: kselftest の tick 待ちに `io_wait()` を使うと以後の FD 読みが古くなる (§4-55)。
  実機の残件: W0 (tick)、W4 (2.4576MHz、クランプ回数)、W7 (キャッシュ整合)、82557 の実 IRQ。次の票: TASK_KAPI_OUTPUT_GUARD (kernel 不具合) → L-B 82557 → §5-5 P1 (CS4231 PCM、NP21/W の ini で WSS 系を有効にする [D2])。
- **TASK_KAPI_OUTPUT_GUARD 着地** (8108ab4 + 7ae93c7): 既存 45 本の出力ポインタに書き込み保護 (生成器、`check-kapi-out`)、`kout_test` PASS、残り 6.2KB。
- 出力保護の実装レビュー (Codex) の 5 件を反映済み (dev_get_info / dev_blk_read / sys_redirect_fd_buf の拒否、リダイレクト先の書き込み時再検査、np2_recv_str)。実物の `fd_redirect.c` を取り込むホスト試験 3 本 (multiapp_impl / owner_reclaim / fstat_redir) にスタブが要った。`make check` 通過。
- **TASK_PCM_CS4231 (§5-5 P1) 設計 v10 = Codex 往復 9 で Approve** (指摘 15/11/8/8/6/7/2/1 → 0。v4 でステージング方式、v9 で連続性は位置だけ)。実装は [D2] (trial ini の SNDboard=0x64) と E0 (NP21/W フォークの /api/sound 拡張 = 停止→配備→起動) の承認待ち。カーネル残り 6.2KB が制約。データシートは `docs/hw/crystal/cs4231a.pdf` (gitignore)。実装前に [D2] (trial ini の SNDboard=0x64) と E0 (NP21/W フォークの /api/sound 拡張、停止→配備→起動)。
- 票の外で見つけた不整合: `tools/memmap_audit_live.py` の GUI 基点 `+0x30000` (正典は `MEM_SHM_GUI_OFFSET` = 0x28000)。別の小票で直す。

## カーネル予算の切り直し (2026-09-23、ユーザー指示「順に実行」)

- **1. KHEAP 320KB → 192KB**: 実測は FD 起動 + kout_test 後で `kmalloc_peak_bytes` = 65,552 (64KB の一時確保が起動時に 1 回)。
  予算 468KB → **596KB (残り 134KB)**。写し: `build/os32.ld` の `MEM_KERNEL_IMAGE_MAX = 0x95000`、`tools/tests/test_memmap_{gen,boot}.py` の
  予算定数、起動行の HEAP 表示は定数から。`make check` 通過 (aa536e9 以降)。GUI/NHD 環境での peak は次に NHD で起動したときに読む (kernel.map の `kmalloc_peak_bytes`)。
- **2. 静的なページ表 (`paging.c` の pd_raw 8KB + pt_raw 36KB + page_tables 4KB = 48KB) を画像の外へ**: 置き場は SQLite 帯の予約域の残り
  (0x2DD000〜0x2E7FFF、44KB、うち 36KB が要る) しか無く、SQLite の伸び代が 8KB になるので**単独ではやらず 4 に含める**。
- 3 (動的読み込み) は v3 §3 のとおり。**4 (メモリマップの見直し)** は v3 の票として起こす: カーネル帯 2MB (SQLite を上へ)、ページ表と DMA プールの固定配置、
  KHEAP/SHM の再配分、PCM ステージングをプール外へ。
- Codex 往復 1 (TASK_MEMMAP_V3) で 9 件: 2-2 (カーネル帯 2MB) はシェルの PDE 1 写像・pgalloc の配布域・予算式・ビルド経路 5 つ・NHD 移行・アプリ帯の PDE・ローダ 508KiB を先に決める大きい票 → **保留** (134KB で PCM と 82557 は入る)。2-3 の小さい整理 (地図の文言、ページ表の整列 8KB、PCM ステージングを KHEAP へ) は着地。`fat12.o` は未リンクの残骸 (FAT は FatFs だけ)。

## PCM の着地 (2026-09-23、承認後)

- `wt/pcm` を合流 (-Os は外した)、NP21/W フォーク `wt/pcm-api` を main へ合流して配備 (稼働中の exe は LAN 実装込みの未コミット状態だったので先に `wip(net)` で保全)。
  新 trial ini `np21w-trial-pcm-sndboard64.ini` (SNDboard=64 だけ、原本無変更) で起動中。E0〜E3 合格 (票 §3-1)。E4/E5 は残件、E6 は実機。
- 直した欠陥: close の待ちループの volatile 欠落 (§4-56)、pcm_test の位相刻みの溢れ。カーネル 468KB / 596KB。
- スクラッチビルド先 `C:\Users\hight\Documents\np21w-src-pcmapi` はユーザー承認で削除済み (2026-09-23)。
- PCM の実装レビュー: 往復 1 で 5 件 (foreground と tick の競合) → Opus 5.5 コーダーが交互試験つきで修正 (e301a61) → **往復 2 で Approve**。NP21/W で E3 再確認済み。
- 次: E4/E5 (kselftest から `pcm_reclaim` を呼ぶ形)、E3 の未実施項目、P3 (`snd_engine` のバックエンド)、L-B (82557)。

## 中断時点の状態 (2026-09-23、ユーザー指示「一度中断」)

- 本線 `feat/gui` は 17e3f46 まで全部コミット済み、`make check` 通過 (直近は 828daeb の時点。その後は文書だけ)。
- **走っていたコーダー 2 本** (未コミットの差分は worktree に残る。セッションが切れていたら worktree の `git status` を見て「仕上げ」のコーダーを同じ worktree に出す):
  - `.claude/worktrees/pcm` (branch `wt/pcm`、基点 17e3f46): **完成してブランチにコミット済み (未合流)**。ドライバ + KAPI v61 + ホスト試験 21 ケース (変異 24 本 RED) + `pcm_test`。+5,280B。基点が旧予算 (468KB) なので `pcm_cs4231{,_math}.c` だけ `-Os` — **合流後 (596KB) は `build/kernel.mk` の 2 行を消して -O2 に戻す**。逸脱 5 点 (ステージングは KHEAP、BUSY/NOMEM は FULL/NOSPC に写像、完了条件の staged==0 は到達不能、ack_tc は tc のときだけ、-Os) は票の進捗注記と報告にある。合流手順: merge → -O2 に戻す → `make clean` → `make all` → `make external` (KAPI v61) → `make check` → NP21/W で検出失敗が無害 (`[pcm] none`)、kselftest。E2〜E5 は承認 2 点の後。
  - `/home/hight/np21w-src/.worktrees/pcm-api` (branch `wt/pcm-api`): E0 は**完成してブランチにコミット済み** (main 未合流、**未配備**)。MSBuild はスクラッチ `C:\Users\hight\Documents\np21w-src-pcmapi` で通した (exe は `src\bin\np2vs2019\x64\Release\`)。正規の配備は main に合流 → `make build` → NP21/W 停止 → `make deploy` → 起動、スクラッチは削除 ([D2])。`pu_count` は NP21/W に PUR/PU を立てる箇所が無いので無し。票 E0 の「cs4231g.c」は `cs4231c.c` の誤り (後で直す)。
- 合流後の手順: PCM は `make clean` → `make all` → `make external` (KAPI v61) → `make check` → NP21/W で検出失敗が無害なこと (現構成に CS4231 は無い)。E2/E3 は下の承認 2 点の後。
- **ユーザーの承認待ち**: (1) [D2] trial ini の複製に `SNDboard=0x64`。(2) E0 の配備 (NP21/W 停止 → フォーク exe 配備 → 同じ trial ini で起動)。
- NP21/W は trial ini `np21w-trial-a2dd2cc4330f44aa965b57265b143c62.ini` + `os32_boot.d88` で起動中 (最新ビルド)。debug server が死んだら `np21x64w.exe "/i<ini>" "<d88>"` を PowerShell Start-Process で上げ直す。
  - **2026-09-25 以降は `python3 tools/np21w_ctl.py stop` → `start --ini <ini> --fd <d88> --wait-ready`** (手で Start-Process しない。[`docs/POLICY_DEBUG.md`](../../POLICY_DEBUG.md) §5・§4-60)。
- 実機の残件は変わらず: W0 (tick)、W4 (2.4576MHz、クランプ回数)、W7、82557 の実 IRQ。

## 実機ホスト (Ubuntu ノート) との疎通 (2026-09-23 12:07)

- 実機ホストは Ubuntu 24.04 の `hight-PC-VJ24LFWZ1SRG` (FTDI FT232 = `/dev/ttyUSB0`)。Remote Control の Claude セッション経由で操作できる
  (`ListAgents` → `SendMessage`。返信は cross-session-message で届く。セッション名はそのマシン側で変わる)。
- そのマシンに git が無く dialout 未所属だった → ユーザーが導入。再ログイン前は `sg dialout -c "..."` で開ける。OS32 は `feat/gui` (cd928b7) を
  `/home/hight/os32` に clone (origin へは同日 push 済み)。
- **実機は今日のビルド (API v61、Build Sep 23 10:20:46) で起動している** = ユーザーが FD を書き直し済み。`ver` と `uptime` が返る。
  `rshell_serial.py` の最初の 1〜2 回は desync (前に残ったゴミ / 行頭の 'S')。再試行で通る。
- **計測結果 (同日 12:30〜13:20、相手セッションが実行)**:
  - **W0 合格**: ホスト 120.29 秒で `uptime` +120 秒 (修正前なら +148 秒)。→ TASK_HAL_WIRING §3-1
  - **R1〜R4 取得**: 82557 = `0:11.0` irq **3** pin A、io 0x6000、Command 0x0147。PCMC = 82441FX。C バスブリッヂは**デバイス 6**。
    PCI スロットに **SB Live! 24-bit (CA0106、irq 6)** が挿さっている。→ TASK_LAN_82557 §6
  - **`--fast 115200` 合格**: `hexdump` 189KB を 48.7 秒 (3.9 KB/s)。**道具の欠陥**: 終了後ゲストが 115200 に残る → 同日修正
    (`restore_speed`、`--keep-fast`)。長い出力は `--timeout 60` 以上。→ TASK_SERIAL_VFAST
  - 未: W4 実機 (`time_test` を FD に `/bin/timetest.bin` として足した (8.3) = **次の FD 書き直しで**、kselftest の `clamped N` 行は画面で読む)、W7、PCM E6。

## CI の本体ビルド (2026-09-23 13:45、ユーザー依頼「git actions でイメージとバイナリを作り、実機ホストから取る」)

- 動機: 開発ホストの回線が従量課金のことがある → push だけにして、イメージの受け渡しは GitHub → Ubuntu ノート。
- 着地: `.github/workflows/build.yml` + `tools/ci/build_cross.sh` + `tools/ci_fetch.sh`、文書 08_build.md §8-6 (717760e、f6d8467)。
  コーダー = Opus 5.5 (`claude-opus-5-5[1m]`)。手元でツールチェーン構築 14 分、userland .bin 97 本中 95 本が本体とバイト一致 (残り 2 本は Rust の panic パス差)。
- **初回 run は落ちた**: `apps/` `game/` が private repo で checkout の submodule clone が `Repository not found`。→ secret `SUBMODULE_TOKEN` があるときだけ取り、
  無ければ warning + core だけ作る形に直した (f6d8467)。**ユーザー決定 (13:55): apps/game は含めない = core だけ**。ノート側は gh ログイン済み (ssh key)。
- run 2 (35819997848) はツールチェーン構築 17 分のあと**検証で落ちた**: `echo "$SYMS" | grep -q` が pipefail の下で SIGPIPE → 失敗扱い (手元では運良く通っていた)。ファイルに落として grep する形に修正 (527255b)。
- **run 3 (35822191507) 成功**: 05:23 → 05:50 の 27 分 (ツールチェーン 24.9 分、本体ビルド 0.9 分、apt 0.4 分)。artifact `os32-feat-gui-527255b` 13.8MB (zip)、キャッシュ保存も成功。
- **run 4 (35825403508、手動起動) 成功、1 分 50 秒**: ツールチェーン段は skipped (キャッシュ 335MB が hit)。以後の push は約 2 分で artifact が出る。
- **ノート側の `gh` は未ログイン** (相手セッションの `gh auth status`: "You are not logged into any GitHub hosts")。git の ssh key では gh は認証されない → ユーザーが 16:24 にログインし直して解決。**ノートで `tools/ci_fetch.sh` が通った** (run 35825403508 の artifact、13 ファイル sha256 一致、合計 25MB、`./os32-ci/os32-feat-gui-527255b/`)。`os32-ci/` は .gitignore に足した。以後の実機の回は、ノートで `git pull && tools/ci_fetch.sh` → `os32_boot.d88` / `os32_boot144.img` を FD に書く、が手順。
- ノート側の 1 回だけの準備: `gh` の導入と `gh auth login`。以後は `tools/ci_fetch.sh` で最新の成功 run を取って sha256 照合。

## 次の実機の回でやること (順に、2026-09-23 13:20 更新: W0・R1〜R4・115200 は済)

1. ~~FD で起動、`--fast 115200`~~ 済 (2026-09-23)。~~R1〜R4~~ 済。
2. ~~FD を書き直す~~ 済 (CI の artifact 527255b の FD で起動、16:38 に `timetest` **TIME PASS**、`ver` Build Sep 23 06:09:44 / API v61、82557 irq 3 は再現)。**画面も確認 (写真 3 枚)**: `PIT 2.4576M`、`clamped 0 of 10000`、`186/186 passed`、`[pcm] CS4231 v=101 irq 10 dma 1` (実機で検出)、`0439h VERIFIED`、8GB HDD identify=0。→ **W0/W4 は実機で合格、TASK_HAL_WIRING §3-1**。元の手順: FD を書き直す (`timetest` 入り、`images/os32_boot.d88` / `os32_boot144.img`) → 起動画面の `PIT 2.4576M` と
   `[selftest] time: clamped N of 10000` を読む (W4 の実機側)、シリアルで **`timetest`** (FD では 8.3 名。CPL=3、NULL / 範囲交差 / heap / stack)。
3. L-B (82557 最小ドライバ、irq 3 / io 0x6000 は PCI から読む) → R5 (EEPROM から MAC)。
4. PCM E6 (実機で音、XTAL2): FD に `pcmtest` を足した (次の CI artifact から) → `pcmtest short` を耳で。W7 (キャッシュ整合)。

## 罠 (今日踏んだもの)

- **エミュレータが模擬しない量**: FDC のシーク時間、DMA の 1MB 制限、FRY、シリアルの速度、CPU 校正の丸め (NP21/W でも 1 周 < 1 tick)。POLICY_DEBUG §4-49〜§4-53。
- **「画面に出ている」は見た目で確かめる** (`/api/screenshot`)。tvram の文字は属性を見ない。
- コーダーの worktree が別系統の基点 (origin/main) で切られることがある — 着手前に `git log -1` の確認を必ず指示に入れる (2 回起きた)。
- セッション切断でバックグラウンドのコーダーが止まる — worktree の未コミット差分は残るので、新しいコーダーに「仕上げ」を頼むほうが速い (transcript の再開より軽い)。
- `make check` 中に票を編集すると `check_tree_unchanged` が落ちる。

## 道具

- 実機: `rshell_serial.py --port COM3 cmd <line>` / `repl` / `--fast 115200` (Windows 側 venv に pyserial 導入済み。Ubuntu では `/dev/ttyUSB0`)。
- NP21/W: trial ini (`np21w-trial-a2dd2cc4…ini`、FD 挿入、原本未変更) で `os32_boot.d88` から起動中。`/api/reset` で最新の D88 が起動する。
- Intel 8255x SDM: `docs/hw/intel/8255x_open_source_sdm.{pdf,txt}` (gitignore)。

## 2026-09-23 夜〜24 の状態 (PM = Opus 5.5)

- **体制**: PM = Opus 5.5、レビュアー = Codex + Opus サブエージェント (Fable は月間上限)。3 ラリーで決着しない争点はユーザーへ (ROLES §0)。
- **着地 (すべて 2 者レビュー Approve)**: キーボード 8251 のコマンド語 0x16 + ISR のステータス先読み + kbdstat (KAPI v62、bda95fa / f924275)、HDD 段 0 (BIOS 幾何を 0x7E00 へ、431822e)、ext2 の名前の無い項目 (fc5ce67)。
- **実機テスト 2026-09-24 午前**: CI の最新 artifact (fc5ce67 以降) の FD で起動 → 画面の `[kbd]` `[hdd] bios` `[hdd] ata0` `[selftest]` 行 (写真)、本体キーボードで打鍵、シリアルで `ver` / `kbdstat` (打鍵の前後) / `uptime`。桁ズレの切り分け: 液晶の自動調整、ずれ方の写真 (古い行も傾くか)、`gfxmode pc98` + リブート。
- **決裁待ち**: NP21/W の NHD 修復 ([D2]、写しを取って `e2fsck -fy` → `deploy-kernel`。NHD は現状 Linux で読めないので deploy が止まる)、シリアル /host 票の 5 争点 (推奨は 1B / 2A / 3A / 4A / 5C)。
- **別票 (未着手)**: TASK_KAPI_DATA_FIELDS、TASK_VFS_FD_PATH (カーネル層、優先)。LZ4 高圧縮はカーネル 511,602B / 上限 520,192B で早めに。

## 2026-09-24 午前〜昼の状態 (PM = Opus 5.5)

- **着地**: KAPI v63 (データ欄 0x4B8 固定、両者 Approve)、VFS の FD / パス / 名前の規則 (3 ラリー + 決裁)、LZ4 高圧縮 (437KB、上限まで 81KB)、CD パッケージを配備マニフェストのタグから生成 (4f81be4)。
- **NP21/W**: v63 に CD の新規インストールで移行 → kselftest 210/210。新しい CD (Normal) で入れ直し、以前漏れていた GUI 一式・フォント・hsync・cfg・ime が入り、`cfg list` / `ime` が HDD 上のコマンドで動く、e2fsck clean。IME 辞書の rm → 作り直し → 変換は合格、開いている辞書の rename は BUSY。
- **実機の回の手順**: docs/tasks/realhw/CHECKLIST_2026-09-24.md (CI の c7f1ba2 を使う、HDD には書かない)。
- **進行中**: HDD 段 1 (区画表の標準化、LBA28、KAPI v64 の ext2_format_at / dev_mount_count / sys_umount_checked、hdprep、migrate-pt)。
- **未確認**: FAT 上で loop を固定しての BUSY (実地の空きが無い、ホスト試験で確認済み)、分割 PKG (NORMAL2) を iso9660 が開けるか。FD の最小コマンド集合と CD の MINIMAL の差 (FD にだけ diff / du / cal / man / cfg) は PM 判断待ち。


## 2026-09-24 午後〜夕の状態 (PM = Opus 5.5)

- **体制**: レビュアー = Codex + **Fable (午後に復帰、ユーザー連絡)**。Opus のレビューは使わない。
- **実機の結果 (Ra266、CI の FD)**: キーボード合格。桁ズレは PEGC の起動時 init→shutdown が同期を送り直していたのが原因と見て、prepare フックで同期に触らない形に直し**合格** (`[pegc] hsync=` の値は未確認)。`[hdd]` は BIOS / ATA とも 16382/16/63・LBA 可・total 16514063 (段 1/2 の設計値)。シリアルは FD を入れ直して疎通が戻った (09-23 17:08〜の無応答はソフト要因なし)。**FD 起動は劇的に改善** (2042fa0)。**シリアル中のビープは修正の実機確認が未了**。
- **着地 (すべて 2 者 Approve、feat/gui)**:
  - CD/FD パッケージ: MINIMAL = 起動 FD と同じ集合 (CUI のみ、FD の一覧は packages.yaml の fd: から生成)、GUI.PKG を分離、8.3 名は FAT のときだけ、install は正規名に戻す (a26d808)。
  - 桁ズレ + kselftest の IRQ3 (IF=0、線の状態を戻して検査、IRR に要求があれば unmask 部を飛ばす) (8d17716)。
  - シリアルのビープ: 0035h を全体で書かず 0037h の BSR、BSR_BUZ の名前を UNDOCUMENTED に (c99f2f3)。
  - FD 読み出しの高速化: シーク完了 SIS の取りこぼし (1.5 秒待ち × 毎セクタ)、1KB ごとの開き直しで 4 トラック巡回、を直し、同じシリンダのシーク省略 + トラック先読み + 8 セクタ LRU + 2 秒規則。NP21/W でフォント 187 秒 → 3 秒、FD 起動 15 秒 (03c7dbc)。
  - **HDD インストール段 1+2 (KAPI v64)**: 区画表の標準配置、LBA28、hdprep、migrate-pt、cdinst/install を BIOS 幾何で空か再作成モードだけ、全検査 → format → 表 → 読み戻し → マウント確認 → ローダ・IPL → 展開。`vfs_devname` / `path_get_drive` / `path_get_cwd` が CPL=3 にカーネル番地を返していた (トランポリンの写しへ)。NP21/W で空 NHD に CD インストール → HDD 起動 → kselftest 214/0 → e2fsck clean (28a81fb、3e22825)。
- **NHD の注意**: v64 からカーネルは標準配置の区画表しか読まない。NP21/W の os32.nhd は段 2 の cdinst で入れたので標準配置。**本体の `build/nhd/os32.nhd` (ローカルの写し) は旧配置のまま** — 次に NHD へ配るときは NP21/W 停止中に `make nhd-pull` で取り直す (旧配置のまま配ろうとすると legacy_pt_guard が断る)。
- **進行中**: 部品 A-4 (VK32 の CRC32 表、両ローダの検査、FD の ASM デコーダの境界・スタック、ローダ → カーネルへ CRC、`ver` に Image CRC と Commit ID、KAPI v65)。**実機の最初の HDD インストールの前に入れる** (ユーザー決裁 4)。
- **次の実機の回**: A-4 の着地後の CI の FD / CD で、HDD へ CD インストール (段 2) → HDD 起動 → kselftest → (可能ならホストで e2fsck)。ビープの確認、`[fdc] font:` の 3 行。
- **残りの minor**: FDC (書き込みの NR の非対称、fdc_track.h のコメントの節番号、TASK_FDC_REALHW の古い行)、段 2 (hdd_stage2_tdd の変異数の記述、SDK 側に「文字列 KAPI の返り値は次の呼び出しで無効」)。
- **決裁待ち**: GUI 1.4 の残り (About、R2 計測) を v2.1 の前か後か。

## 2026-09-24 夜の追記

- **着地**: 部品 A-4 (VK32 v2 の CRC32、両ローダの長さ・範囲・CRC 検査、FD の LZ4 デコーダの境界とスタック、FAT チェーン検査、KAPI v65 `boot_image_info`、`ver` と起動画面に `Commit:` / `Image CRC:`) を e146022 で着地。Codex / Fable とも Approve。NP21/W: 2HD / 1.44MB の FD 起動と CD インストール → HDD 起動で kselftest 217/0、1 バイト壊したイメージで FD ローダが `VK32: image CRC mismatch` で停止。CI (build_id を GITHUB_SHA と照合する段を含む) 成功、artifact `os32-feat-gui-e146022`。
- **NP21/W の不具合を修正 (フォーク /home/hight/np21w-src 0f6e62d、deploy 済み)**: `pit_o77()` がカウンタ #0 のラッチ命令でも IRQ0 の IRR を消していた → IF=0 で IRR→ラッチ→IRR と読む OS32 の時刻補間が tick を落とし、`time:1ms delay plausible` が位相次第で FAIL (1.44MB 起動で毎回、2HD でも時々)。実機の 8254 では起きない。旧 exe は `np21x64w.exe.bak-rev101`。
- **NHD の配備**: v65 からローダ (v2 形式を読む) と vmkernel.lz4 は**同時に**入れ替える。旧 HDD ローダは v2 イメージを止まらずに起動して `Image CRC: none` になる (静かな退行)。`make deploy-kernel` だけにしない。
- **次の実機の回 (HDD インストール)**: CI e146022 の FD (生イメージ `os32_boot.img`) と `os32_install.iso` を用意 → FD 起動 → `ver` の Commit が e146022 → CD から `cdinst` (Normal) で 8GB HDD の先頭 256MiB に OS32 の区画を作る (他 OS の区画があれば「対象外」で断る) → HDD 起動 → `ver` の Image CRC が HDD ローダで記録されていること、kselftest。あわせてシリアル中のビープ、`[fdc] font:` の 3 行。

## 2026-09-25 の状態

- **実機 Ra266 に HDD インストールして HDD 起動に成功** (CI e146022、cdinst Full)。`ver` で v65 / e146022 / HDD ローダの Image CRC を確認。v2.1 の条件のうち「実機での HDD 運用」に到達。
- **着地**: NP21/W フォークの API (instance/quit/fdd/dialog/appshot、トークン、Origin 拒否、停止要求の参照数、SO_EXCLUSIVEADDRUSE) を main へ・本番 exe へ deploy。`tools/np21w_ctl.py` (ロック待ち付きの停止・起動、/api/quit で正規停止、強制終了は exe パス一致だけ)。SerialFS (部品 B、KAPI v66、NP21/W で `sfs run ls/cat/hsync --verify` 合格)。`~/.local/bin/os32-cycle` は np21w_ctl を使う形へ (元は .bak-20260925)。
- **体制 (09-25〜)**: レビュアーは Codex だけ。コーダーは Codex の重大指摘を含む修正なら Fable、それ以外は Opus。
- **進行中**: CD の読み出しの高速化 (wt/cd-fast)、`lspci -v` (GV-MVP/HX2 の識別用、wt/lspci-v)、cdinst の `ERASE` 強制オプション (wt/cdinst-wipe)。
- **次**: SerialFS で実機の HDD のカーネルを更新 (T3: `sfs run hsync boot` → `ver` の Image CRC が変わる)、v2.1 のタグの判断。
