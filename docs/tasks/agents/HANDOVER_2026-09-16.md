# 残件の引き継ぎ (2026-09-16) — 別モデルが PM として進めるための文書

> 発行: PM (Claude Code `claude-fable-5-1`、2026-09-16) / 状態: **計画 (2026-09-16)**

読み手: このリポジトリで **PM を引き継ぐ別のモデル** (Claude Code の別モデル、Codex CLI、その他)。
書いてあるのは「今どこにいるか」「残件は何で、どの順に、どう進めるか」「踏むと痛い所」だけ。
規則・手順・技術仕様の本文はここに複製せず、正典へのリンクで済ませる (更新先は
[`docs/INDEX.md`](../../INDEX.md) 冒頭の表が決めている)。

基点: `feat/gui` = `a8a9a42`、`main` = `f25607d` (両方 `origin` と一致、作業ツリー clean、
`make all` / `make check` 46 目標すべて通過、2026-09-16)。

## 0. 引き継ぐ前に読むもの (順)

1. [`CLAUDE.md`](../../../CLAUDE.md) — 入口。制約 [C1]〜[D3] の規則行と Gotchas。
2. [`docs/tasks/agents/ROLES.md`](ROLES.md) — 4 役の境界、コーダー / レビュアーの起動、
   **基点 SHA の確認規則** (§2)、レビュー依頼の書式 (§5)、合否の判定 (§4)。
3. [`docs/POLICY_DEV.md`](../../POLICY_DEV.md) §1 — **カーネル層の既知の不具合は新機能より先に直す**。
   今の残件にカーネル層の未修正バグは無い (B8 で閉じた)。新しく見つけたら残件より先。
4. スキル `os32-build-verify` (何を組んでどう確かめるか)、`os32-emu-debug`、`os32-kapi-add`、
   `os32-local-ai`。`*.ini` に触るなら `os32-emu-config` ([D2])。
5. [`docs/POLICY_DEBUG.md`](../../POLICY_DEBUG.md) §4-29〜§4-36 — 直近 1 か月の教訓。

**PM が止まってよいのは 4 つだけ**: [D1]〜[D3] の承認、仕様の分岐、スコープ拡大、独立レビューが要る地点
(ROLES §3)。それ以外は承認済みスコープの中で最後まで進める。

**モデルが変わっても変えないこと**:
- コーダーは Opus 5 サブエージェント (`model: "opus"`, `isolation: "worktree"`)。コーダーに
  git commit / push、配備、エミュレータ操作、`*.ini` / `.env` の編集、`make` の実行はさせない。
- レビュアーは Codex (`codex exec -s read-only "<依頼文>" < /dev/null` — **stdin を閉じないとハングする**)。
  Codex が枯渇したら Fable 5.1 サブエージェント (`model: "fable"`, `isolation: "worktree"`、読むだけ)。
  PM 自身がレビューを代行して「問題なし」にしない。往復は 3 回 + 1 回、収束しなければユーザーへ。
- コミットは PM が行い、**着地のたびに push する** (push 忘れで Codex が票を見つけられなかった前例)。
  `feat/gui` で作業し、区切りで `main` へ `--no-ff` merge して両方 push。

## 1. 今どこにいるか

| 領域 | 状態 (2026-09-16) | 根拠 |
|---|---|---|
| カーネル 2.0 / GUI 1.x | 出荷済み。版数表は [`ROADMAP.md`](../../ROADMAP.md) §0。次期カーネルは **v3** と呼ぶ (v2 は出荷済み 2.0 と衝突) | ROADMAP §0 |
| hsync | H1 (同サイズ内容比較 + CRC 読戻し) と H3 (mtime 前置フィルタ、`sys_set_mtime` = KAPI v52) は**実機受入済み**。H2 / H4 未着手 | [`shell/HSYNC_IMPROVEMENT_PLAN.md`](../shell/HSYNC_IMPROVEMENT_PLAN.md) §8、[`TASK_H1.md`](../shell/TASK_H1.md)、[`TASK_H3.md`](../shell/TASK_H3.md) |
| ext2 / VFS / HostDrv (B8) | 読み取り失敗の読み替え経路を 6 往復で閉じ、実機受入済み。remount-ro 相当 (`OS32_ERR_ROFS` = -15)、`e2fsck -fn` を試験の正解に組み込み済み | [`shell/TASK_FS_TYPE.md`](../shell/TASK_FS_TYPE.md) §2-8 |
| 移植準備 | 順序 1〜4 着地 (ARM 計測器 55/93、`hlt`/`cli`/`sti` を `io.h` 経由に、`arch/x86` + `platform/pc98` の骨格、kstring の C 版、LE アクセサ)。**KAPI 生成器の arch 対応は v3 まで保留** (ユーザー決裁) | [`portability/ARM_GAUGE.md`](../portability/ARM_GAUGE.md) §9 |
| 文書 | 整理 A〜F 完了。状態行の書式統一、`docs/TESTS.md` は生成物 (`tools/gen_tests_inventory.py --write`)、`make check` にリンク切れ / 孤児 / 試験一覧の 3 検査 | [`INDEX.md`](../../INDEX.md)、[`08_build.md`](../../08_build.md) §8-4 |
| ネットワーク | LGY-98 は既定ビルド。M0〜M3 はエミュレータで合格。**M4 / M5 の実機項目は実カードが無いと進まない** | [`network/PLAN.md`](../network/PLAN.md) §6、§9 |
| 動いているもの | サブエージェント無し、worktree 無し、NP21/W は最後の受入 (2026-09-15) のカーネルで稼働中の可能性あり。**配備前に必ず状態を確かめる** ([D1]) | — |

## 2. 残件と推奨順

上から順に。番号は本書の節番号。

| # | 件 | 層 | 依存 | 大きさの目安 (B8 = 6 往復 ≈ 2 日を 1.0 とする) | 決裁の要否 |
|---|---|---|---|---|---|
| 3 | **H2** hsync の置換安全化 (一時ファイル + 検証 + 置換) | VFS / ext2 / KAPI / hsync | 無し | 0.6〜0.8 (KAPI 追加あり、失敗注入試験が主) | §3-4 の 3 点 |
| 4 | **H4** 配備マニフェストと世代 | ホスト側ツール + hsync | H2 が終わってから (置換が安全でないと世代の「全成功」を保証できない) | 0.5 | §4-4 の 2 点 |
| 5 | `gdt` / `tss` / `cr3` と CPL=3 降下 asm の `arch/x86/` 移設 (移植準備 順序 5) | カーネル | 無し。H2 と並行可 | 0.3 (機械的、`.o` の md5 で等価を示す) | 無し |
| 6 | kstring を x86 でも C 版にするかの判断 (速度実測) | lib | 無し | 0.1 (実測だけ。切り替えるなら +0.1) | 実測値を見て 1 点 |
| 7 | ゲスト側の一括試験ランナー (シェルの `$?` が前提) | シェル + exec + 道具 | 無し。v3 の ABI 整理より**先** | 0.5 | 結果チャネルの形 1 点 |
| 8 | ARM 実装 (`arch/arm/`) | 全層 | 5 が済んでから。KAPI 生成器 (v3 保留) が無いと外部プログラムは動かない | 大 (別計画が要る) | 着手自体が決裁 |
| 9 | LAN の実機項目 M4 / M5 | ドライバ | 実カードと実機 | ユーザーのハード次第 | ユーザー |
| 10 | 小物 5 件 | — | 無し。空いた時間に | 各 0.05 | 無し |

H2 → H4 の順は動かさない。5 と 6 は H2 のレビュー待ちのあいだに入れると無駄が無い。
7 は「試験をゲストで一括して回せる」ことが v3 の ABI 整理の前提になるので、ARM や v3 に手を付ける前に終える。

## 3. H2 — hsync の置換安全化

**目的**: 新内容の書き込みと検証が済むまで**旧宛先を残す**。今の hsync は宛先を直接
`O_TRUNC` で開いて上書きするので、書き込み途中の失敗で旧内容が消える
(POLICY_DEBUG §4-36 の末尾、`hsync.1` にも制限として明記済み)。

**正典**: [`HSYNC_IMPROVEMENT_PLAN.md`](../shell/HSYNC_IMPROVEMENT_PLAN.md) §6 (手順 6 段と保証の限界)、
§9 の受入 A14 / A15 / A17、§10 の未確定 (1)。

### 3-1. 確認済みの事実 (2026-09-16、`a8a9a42`)

- `O_EXCL` 相当は無い。open フラグは `O_RDONLY / O_WRONLY / O_RDWR / O_CREAT / O_TRUNC` だけ
  (`sdk/include/os32/os32_kapi_shared.h:484-488`)。「stat してから固定名を `O_TRUNC` で開く」は
  計画 §6 が不可としている。排他的作成を VFS と ext2 / HostDrv に足すか、専用の一時作成口を作る。
- `ext2_rename` の**ファイル**の置き換えは「宛先を `ext2_unlink` してから新しい名前を載せる」順
  (`fs/ext2_dir.c:828` 以降。ディレクトリ側 `ext2_rename_dir` は B8 で旧名を先に消す順に決裁済み)。
  つまり rename の途中で失敗すると**宛先が無い状態**が残る。H2 の置換は rename に頼るなら
  この非原子性を先に直すか、置換手順の側で「宛先が消えている状態からの復旧」を契約に入れる。
  どちらにするかは §3-4 の決裁。
- hsync 本体は `userland/system/hsync.c` (1248 行) + `hsync_protect.inc` (269 行)、
  ホスト試験は `tools/tests/hsync_h1_host.c` (1758 行) と `hsync_h3_host.c` (859 行)。
  試験は VFS を偽物に差し替えて hsync のロジックだけを走らせる形なので、H2 の失敗注入も同じ枠で書ける。
- ext2 の失敗注入の枠 (`tools/tests/b8_open_host.c`、once / sticky の掃引、`media_check`、
  `b8_e2fsck.py` の本物 `e2fsck -fn`) が B8 で出来ている。**H2 の ext2 側の変更はこの掃引に必ず通す**。
- `hsync` は `/sys` を既定で除外し、`-f` でも解除しない。H2 でも変えない。
- 一時ファイルは最大ファイル 1 本分の空きが余分に要る。ext2 の `OS32_ERR_FULL` を H2 の
  失敗経路として扱う (満杯で旧宛先を壊さない)。

### 3-2. 進め方

1. **票を書く** `docs/tasks/shell/TASK_H2.md` (H1 / H3 の票と同じ構成: 目的 / 範囲 / 設計 / 受入 /
   往復記録)。設計に §3-4 の決裁を**選択肢と推奨つき**で書き、ユーザーに聞く。
2. 設計レビュー (Codex、範囲を絞る: `fs/vfs.c` `fs/vfs_fd.c` `fs/ext2_dir.c` `fs/ext2_file.c`
   `fs/hostdrvfs.c` `userland/system/hsync.c`)。3 往復まで。
3. KAPI を足すなら `os32-kapi-add` の手順 ([ABI1]〜[ABI3])。v52 の次は **v53**。追加は末尾のみ。
   `make clean` → `make all` → `make external` を忘れない。
4. コーダーへ。依頼文に**基点 SHA** を書き、最初に `git log --oneline -1` を報告させる (ROLES §2)。
   ホスト TDD (`tools/tests/hsync_h2_host.c` + `hsync_h2_tdd.md`) を RED → GREEN で。
   `build/sdk.mk` に `check-hsync-h2-host` を足し、`tools/emu_agent/agent.py` の `MAKE_TARGETS` にも足す。
5. 実装レビュー (Codex、網羅指示つき)。
6. 着地 (`git diff` は**基点と**比較、D が無いこと、`make check` 全通過)。push。
7. ゲスト受入 ([D1]、`os32-build-verify`): `make deploy` → ゲストで `hsync -n` → `hsync` →
   意図的に壊した配備元 (途中で切ったファイル、空き不足) で旧内容が残ることを **`ls -l` とサイズ・CRC で**
   確かめる。`hsync sys` の所要時間が H3 の 0.26 s から悪化していないことも測る (ゲストの `time` 組み込み)。
8. 制限の記述を 3 か所書き換える: `docs/manpages/hsync.1:175-178` (直接上書き、H2 で置換方式へ)、
   `docs/POLICY_DEBUG.md` §4-36 の末尾、`CLAUDE.md:157` の Gotcha 行「失敗時に旧内容は残らない (H2)」。
   票を受入完了にして `tools/move_docs.py --into docs/archive/shell/` で移す (`docs/archive/shell/` は
   まだ無いので新設し、`docs/archive/README.md` の区画表に足す)。

### 3-3. 受入 (計画 §9 から H2 に関わるもの)

| ID | 反例 | 期待 |
|---|---|---|
| A14 | 空き不足、一時名衝突、write / verify / rename の失敗 | 旧宛先維持、自分が作った一時ファイルだけ片づける、非ゼロ終了 |
| A15 | 各永続化境界で停止 → 再起動 | 定めた復旧契約どおり。**電源断の原子性は保証しないと明記** (ジャーナルは無い) |
| A17 | コピー中の src / dst 変更、hardlink | 検出できる競合は報告。`st_nlink > 1` は初期版では拒否 |
| 追加 | ext2 失敗注入の掃引 (once / sticky) を H2 の経路に | `media_check` と `e2fsck -fn` が両方 clean、または「漏れ」だけ (B8 §2-6 の交換規則どおり) |

### 3-4. 決裁が要る点 (票に選択肢と推奨を書いてユーザーに聞く)

1. 排他的作成の口: (a) `O_EXCL` を KAPI フラグとして足す (汎用、HostDrv 側の NTSTATUS 対応も要る) /
   (b) hsync 専用の `sys_mktemp` 相当。推奨は (a)。フラグ追加は `kapi.json` の構造体を変えないので
   版数 bump だけで済むかを `os32-kapi-add` で確認する。
2. 置換の手段: (a) `ext2_rename` のファイル置き換えを「新名を先に載せ、旧宛先の inode を後で外す」順に
   直してから rename で置換 / (b) rename に頼らず「旧宛先を `.old` に退避 → 一時を本名に → `.old` を消す」
   の 3 段。推奨は (a) (B8 でディレクトリ側に同じ規則を入れたので整合する)。(a) は SQLite の
   rename 利用者 (`lib/sqlite3/os32_sqlite_vfs.c`) にも効くので別途レビュー (計画 §8 の注意)。
3. 電源断の復旧契約をどこまで書くか: 推奨は「I/O 失敗時の旧ファイル維持」だけを保証し、電源断は
   「一時ファイルが残り得る。次回 hsync が自分の名前規則の一時ファイルだけ掃除する」に留める。

## 4. H4 — 配備マニフェストと世代

**目的**: 「古い `/host` から NHD へ戻してしまう」「途中で失敗した配備を完了扱いにする」を検出する。
CRC と mtime では**どちらが意図した版か**は分からない (計画 §7-1)。

**正典**: [`HSYNC_IMPROVEMENT_PLAN.md`](../shell/HSYNC_IMPROVEMENT_PLAN.md) §7-1〜§7-3、§9 の A18。
配備の 3 経路は [`08_build.md`](../../08_build.md) §8-4、ホスト側は `tools/hostdrv_deploy.py`、
対象の正典は各層の `deploy.yaml` ([V2])、古い物の掃除は `tools/prune_stale.py`。

### 4-1. 確認済みの事実

- `make deploy` は `deploy.yaml` を読んで `C:\os32` (HostDrv) へコピーし、`prune_stale.py` で登録外を消す。
  マニフェストを書く場所はこの経路の**最後**、全コピー成功の後 (失敗を握り潰して成功マニフェストを
  書く経路を禁じる、計画 §7-1)。
- hsync 側の集計 `copied / unchanged / excluded / protected / metadata_updated / errors` と理由コードは
  H1 で固定済み。H4 はここに `generation` (検出した世代) と `expect_mismatch` を足す。
- CRC-32 のストリーム版 `lib/crc32_core.inc` はホスト試験からも同じソースで使える
  (H1 でホスト計算と同値を確認済み)。マニフェストの CRC はこれで出す。8 桁固定の 16 進で保存。

### 4-2. 進め方

票 `docs/tasks/shell/TASK_H4.md` → 設計レビュー → ホスト側 (`tools/hostdrv_deploy.py`、Python、
`tools/tests/test_hostdrv_manifest.py`) とゲスト側 (`hsync` の読み取りと検査) を**別コミット**で →
実装レビュー → ゲスト受入 (A18: 不一致 / 欠落 / 重複パス / `..` / 途中失敗)。

### 4-3. 受入

A18 の 4 反例に加えて: マニフェスト無し (H4 以前の配備元) でも今の hsync と同じ挙動 (後方互換)、
`--expect-build` 不一致は**変更前に**拒否、途中失敗した世代は「ファイル単位の成功 / 失敗一覧」だけ残す。

### 4-4. 決裁が要る点

1. マニフェストの形式と置き場: 推奨は `C:\os32\.deploy\manifest.txt` (1 行 1 ファイル、
   `path size crc8 mtime`、先頭に `format=1 build=<git SHA>[+dirty] generated=<UTC>`)。JSON はゲスト側に
   パーサが無いので使わない。
2. 世代別ディレクトリ (同期中に配備元が動かない) を H4 に含めるか: 推奨は**含めない** (運用契約
   「同期中に `make deploy` しない」で代替し、後続票に回す)。

## 5. `gdt` / `tss` / `cr3` と CPL=3 降下 asm の `arch/x86/` 移設 (移植準備 順序 5)

**目的**: ARM 計測で落ちる (a) 分類 5 本 (`exec/exec.c` `gfx/gfx_vram.c` `kernel/gdt.c`
`kernel/paging.c` `kernel/tss.c`) のうち x86 の仕組みそのものを `arch/x86/` へ寄せる。
順序 3 で作った骨格 (`include/io.h` = 契約、`arch/x86/arch_io.h` = 実装、`-Iarch/$(ARCH)` で解決) に
載せるだけで、**動作は変えない**。

**正典**: [`ARM_GAUGE.md`](../portability/ARM_GAUGE.md) §3 (a)、§7 「順序 3 で採るなら」、§9 の計測表。
骨格の説明は `arch/README.md` と `platform/README.md`。

**進め方**: 票 `docs/tasks/portability/TASK_ARCH_X86_MOVE.md` (小さい) → コーダー → 着地。
レビューは差分が機械的なら `.o` の md5 一致で代替できる (順序 2〜4 で使った証明方法:
移設前後で `make clean && make kernel` し、`kapi_sys.o` を除く全 `.o` の md5 が一致すること。
`kapi_sys.o` だけは `__DATE__` で毎回変わる)。**md5 が変わる変更は機械的ではない**ので、その時はレビューへ。
終わったら `make check-arm-compile` を回し、ARM_GAUGE §9 の表に**追記** (置き換えない)。
`tools/check_arch_asm.py` の `ARCH-ASM-OK` 許可コメントは、移した先で不要になったら外す。

**やらないこと**: `gfx/gfx_vram.c` の VRAM 直書きはデバイス層 (`platform/`) の話で、順序 5 には含めない。
`exec/exec.c` の降下 asm は**移すだけ**、リング遷移の順序や IF の扱い (POLICY_DEBUG §4-19) を変えない。

## 6. kstring を x86 でも C 版にするかの判断

**目的**: `lib/kstring_c.c` (順序 4-b、`ARCH != x86` で使う) を x86 でも使うかを**実測で**決める。
今は `build/kernel.mk:17` の `ifeq ($(ARCH),x86)` で asm 版 (`lib/kstring_asm.asm`) を選んでいる。

**確認済み**: 両版の等価は `tools/tests/test_kstring_c.py` (同じ実行ファイルに両方リンクして照合、
unsigned 比較、pad 無し) で保証済み。`kstrcmp` の 0x80 以上のバイト = 日本語ファイル名の並び順は
`-fsigned-char` で固定済み (ARM_GAUGE §6)。

**進め方**: ゲストで測る。`kselftest` に kstring の計時ケースを足すか、`userland/tests/` に
`kstr_bench` (memcpy / strlen / strcmp を 1MB 級で N 回、`kapi->get_tick()` で計る。
`userland/tests/blit_test.c:168` が同じ測り方をしている) を置く。
ASM 版と C 版のカーネルを NP21/W (i386 相当と、`np21x64w.ini` の CPU 設定を上げた P100 相当の 2 条件) で
比べる。**エミュレータの絶対時間は実機の根拠にしない** — 比を見る。
判断の目安: C 版が asm 版の 1.5 倍以内なら C 版に統一 (保守 1 本化)、それ以上遅いなら asm 版を残す。
どちらでも ARM_GAUGE §9 に数字を追記する。切り替えるなら `build/kernel.mk` の分岐を消し、
`kselftest` の kstring ケースが `kselftest_fail = 0` のまま通ることを新しい `kernel.map` の番地で読む
(POLICY_DEBUG §2)。

## 7. ゲスト側の一括試験ランナー

**目的**: 所定の試験 (kselftest、`userland/tests/` の各バイナリ、hsync / ext2 の実機項目) を
ゲストで一括実行し、**合否を機械が読める形でホストへ返す**。v3 の ABI 整理は「変更のたびに全試験を
ゲストで回せる」ことが前提なので、その前に作る (2026-09-15 のユーザーの思考、PM 評価: ランナーが先)。

### 7-1. 確認済みの事実 (ここが着手点)

- シェルに **`$?` が無い**。`env_expand()` (`userland/shell/cmd_env.c:97`) は環境変数の展開だけ。
- 外部プログラムの終了コードは**シェルが捨てている**: `userland/shell/sh_launch.inc:126` は
  `LAUNCH_ST_DONE` なら状態の下位バイトを見ずに `EXEC_SUCCESS` を返す。カーネル側は
  `exec_exit_status` (`exec/exec.c:169`、`kapi_sys_exit` が設定) を持ち、`exec_run` の CUI 経路は
  それを返している (`exec/exec.c:1552`) ので、**捨てているのは launch 表を経由する経路**。
- スクリプト実行 `script_source_file()` (`userland/shell/cmd_script.c:211`) は行を順に実行するだけで、
  失敗で止まる仕組み (`set -e` 相当) も無い。
- ホスト側の道具: `/api/cmd` は **POST の生ボディ**、画面は `/api/tvram` の `lines`、
  シェルの `time CMD` 組み込みが `real N.NNNs` を出す。テスターの回帰台本は
  `tools/emu_agent/tasks/regress.txt` (合否は `steps.jsonl` の `obs` を PM が読む)。
- 結果チャネルの候補は **HostDrv** (`/host` に結果ファイルを書けばホストの `C:\os32` に即座に見える)。
  画面スクレイプ (tvram) より確実で、[V4] の「文言で判断しない」にも合う。

### 7-2. 進め方 (3 段、各段を別票に)

1. **`$?` と終了コードの配線** (`docs/tasks/shell/TASK_EXIT_STATUS.md`): launch 表の状態語は
   `exec/launch.c:266` で `LAUNCH_ST_DONE` を**そのまま**返し、終了コードを載せていない
   (`r->rc` は FAILED の側でだけ使う)。DONE の下位バイトに `exec_exit_status` を載せ、
   `sh_launch.inc:126` でそれを返す。`env_expand` に `$?` を足す。スクリプトに
   `set -e` 相当 (`script_abort_flag` は既にある) と `exit N`。ホスト TDD は `tools/tests/sh_launch_host.c` の枠。
   **注意**: 常駐シェルは `int` で受けると `-Wincompatible-pointer-types` (`sh_launch.inc:66` のコメント)。
2. **結果チャネル** (`TASK_TEST_RESULT.md`): 試験バイナリの約束事を決める —
   終了コードで合否、詳細は `/host/test/<name>.txt` に `PASS|FAIL <name> <理由>` を 1 行 1 件。
   `userland/tests/` の既存バイナリを順にこの約束に合わせる (全部ではなく、回帰台本に入っている物から)。
3. **ランナー** (`TASK_TEST_RUNNER.md`): ゲスト側 `runtests` (スクリプトで十分なら
   `/sys/etc/runtests.sh`) + ホスト側 `tools/guest_tests.py` (POST `/api/cmd`、`/host/test/` を集計、
   非ゼロ終了)。`make check-guest` として `make check` とは**別**にする ([D1] と NP21/W の稼働が要るため)。
   `tools/emu_agent/agent.py` の `MAKE_TARGETS` に足す。

### 7-3. 決裁が要る点

結果チャネルを HostDrv にするか (推奨)、tvram スクレイプで済ませるか。HostDrv だと NHD ブートでも
`/host` がマウントされている前提になる (今の運用では常にそう)。

## 8. ARM 実装

着手の前提が 3 つ揃うまで始めない: (1) §5 の移設が済んでいる、(2) KAPI 生成器の arch 対応
(**v3 まで保留**、ユーザー決裁 2026-09-15) の解除、(3) 対象ボードとツールチェーンの決定
(`make check-arm-compile` は `arm-none-eabi-gcc` で `-c` まで、リンクと実行は未定義)。
始めるなら `docs/tasks/portability/PLAN_ARM.md` を票より先に書き、ユーザーの決裁を取る。
`docs/tasks/arch_port/` は**別リポジトリ `pw-sh4-research` の調査の快照**で、ARM の計画ではない。

## 9. LAN の実機項目 (M4 / M5)

エミュレータでは確認できないもの (OVW、内部 loopback、RCR.MON) が
[`network/PLAN.md`](../network/PLAN.md) §9 末尾に列挙されている。実カード (LGY-98) と実機が
ユーザー側で用意できたときだけ。ドライバ側の準備は済んでいるので、票は M4 / M5 の実機手順書だけでよい。

## 10. 小物 (空いた時間に、各 0.05)

| 件 | 場所 | 内容 |
|---|---|---|
| `fs_is_dir` が「不明」を運べない | `userland/shell/cmd_file.c` の 5 箇所 | `cp` / `mv` が `fs_path_kind()` の負値を見て断るようにする。TASK_FS_TYPE §3 |
| NP21/W `hostdrvNT_getHostPath` の前方一致 | **別リポジトリ** `/home/hight/np21w-src/src/generic/hostdrvnt.c:368-390` | ルート検査が区切り境界を見ない。直したら `make build && make deploy` (np21w-src 側)。OS32 側は緩和済み |
| B8 の二重故障 | `tools/tests/b8_open_host.c` | 独立 2 箇所の失敗の組み合わせは専用試験 1 件だけ。TASK_FS_TYPE §2-6 |
| C コメントの枠線ずれ 17 箇所 | `tools/move_docs.py --rewrite-only` で書き換えた箇所 | パス長の差で `*` の枠が最大 6 桁ずれている。見た目だけ。`git log -1 --stat c1ea460` の 27 箇所を見る |
| 受入完了した 3 票の archive 移動 | `docs/tasks/shell/TASK_H1.md` `TASK_H3.md` `TASK_FS_TYPE.md` | 整理 F で意図的に残した (直近で参照が多い)。H2 / H4 の着地時に `tools/move_docs.py --into docs/archive/shell/` で移す (区画は新設、`archive/README.md` に足す)。INDEX の行も動く |

## 11. 作業の型 (毎回同じ)

```
票を書く (docs/tasks/<領域>/TASK_<ID>.md、状態行「計画」)
  → 設計レビュー (Codex、3 往復まで、blocker は到達可能な反例つきのみ)
  → 状態「設計中」→「実装中」
  → コーダー (Opus 5、worktree、基点 SHA を依頼文に、最初に git log -1 を報告させる)
  → ホスト TDD RED→GREEN、tools/tests/<id>_tdd.md に記録、build/sdk.mk に check-* 登録
  → 実装レビュー (網羅指示つき)
  → 着地: worktree の差分を基点と比較 → feat/gui にコミット → make all → make check → push
  → ゲスト受入 ([D1]、os32-build-verify、[V4] で正直に) → 状態「受入完了 (日付)」
  → tools/gen_tests_inventory.py --write (試験を足したら) → main へ --no-ff merge → push
  → 票を tools/move_docs.py で docs/archive/<領域>/ へ (INDEX の行も動く)
```

着地のたびに走らせるもの: `make all`、`make check` (46 目標、約 90 秒。**`make all` と別コマンドで**
— 1 つの 600 秒予算に両方入れて切られた前例)、`git status` が clean。

## 12. 踏むと痛い所 (この 1 週間で実際に踏んだもの)

- **worktree の基点が古い** (`main` 側の古い merge から始まる) — 2 度踏んだ。依頼文に SHA を書き、
  報告の先頭で確かめる。「文書が無い」と言われたら基点を疑う (ROLES §2)。
- **Codex は stdin を閉じないとハングする** — `< /dev/null`。
- **push 忘れ** — Codex はリモートを見るので、着地したら即 push。
- **消費側を直さずに供給側だけ直して出荷した** — `hdrv_list_dir` の上限エラーを通した結果、
  `fs_is_dir` / `vfs_path_kind` / `vfs_open_internal` の 3 か所が壊れた (B5〜B7)。述語を変えたら
  **受け手を実物で通す**。
- **`/api/cmd` は POST の生ボディ**、`/api/tvram` は `lines`。完了検知は画面の変化ではなく
  ゲストの `time` 組み込みか結果ファイルで。
- **KAPI を足したら** `make clean` → `make all` → `make external` ([ABI3])。`kapi_db.c` の
  `db_slot_layout_ok` は下限比較 (等号で縛ると次の追加で落ちる)。
- **`.inc` を変えても `.bin` は再ビルドされない** — `build/programs.mk` に明示依存。
- **NHD 配備の後は先に `make deploy`** — `hsync` は HostDrv の内容で NHD を上書きする (§4-33)。
- **`kernel.bin` の md5 は毎回変わる** (`kapi_sys.o` の `__DATE__`)。等価の証明は `.o` 単位で。
- **文書を動かしたら** `make check-docs-links` / `check-docs-orphans` / `check-tests-inventory` が落ちる。
  `tools/move_docs.py` を使えば参照は書き換わる。新しい文書は `docs/INDEX.md` に行を足す
  (足さないと孤児検査で落ちる。`docs/.orphans-allow` は「意図した例外」専用)。

## 13. 入口

`CLAUDE.md` → `docs/INDEX.md` (正典表 → §タスク) → 本書 §2 の順に票を起こす。
hsync は [`HSYNC_IMPROVEMENT_PLAN.md`](../shell/HSYNC_IMPROVEMENT_PLAN.md)、移植は
[`ARM_GAUGE.md`](../portability/ARM_GAUGE.md)、体制は [`ROLES.md`](ROLES.md)。
