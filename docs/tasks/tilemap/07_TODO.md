# タスク07: タイルマップエンジン TODO

## 完了済みフェーズ

| Phase | 内容 | 状態 |
|-------|------|------|
| 1 | 基本合成 (BtF, ダーティ追跡, demo_tile) | ✅ 完了 |
| 2 | FtB合成, 2タイルDWORD転送, BB直接書き込み | ✅ 完了 |
| 3 | ピクセルスクロール, HWスクロール, H/V反転, タイルセットローダー | ✅ 完了 |
| 4 | NASM高速パス (asm_tile_pair_opaque/masked) | ✅ 完了 |
| 5 | 差分スクロール (BBシフト + 端列/行描画) | ✅ 完了 |
| 5.5 | 列ストリーム描画 (asm_draw_column/row_tiles) | ✅ 完了 |
| 6 | リファクタリング (draw_one_tile_btf, scroll分割, tilemap_force_redraw) | ✅ 完了 |

---

## 性能ベンチマーク (NP21/W ~16MHz)

### 384×384 (24×24, 576タイル) — 実測値

| 処理 | 1層 | 2層 | 4層 |
|------|-----|-----|-----|
| 全面 btf | 290 ms | 310 ms | 380 ms |
| 全面 btf_fast | 182 ms | 210 ms | 283 ms |
| 全面 ftb | 192 ms | 252 ms | 390 ms |
| 差分更新 (1タイル) | 8.3 ms | — | — |
| scroll H-diff | 50 ms | — | — |
| scroll V-diff | 47 ms | — | — |

### 推定: 384×128 (24×8, 192タイル) — 面積比 1/3

| 処理 | 推定コスト | 推定fps |
|------|-----------|---------|
| 全面 btf_fast (1層) | ~61 ms | ~16fps |
| scroll H-diff | ~17 ms | ~60fps |
| scroll V-diff | ~16 ms | ~60fps |

---

## 今後のTODO

### 高優先度

- [ ] **`tilemap_dirty_rect(px, py, w, h)` API追加**
  - ピクセル矩形領域のタイルを一括 dirty 化するヘルパー
  - スプライト跡消去の効率化 (手動タイル座標計算を不要に)
  - 用途: タイルマップBG + スプライト重畳の描画パイプライン

- [ ] **TILEMAP_ROWS のコンパイル時可変化**
  - 現在 `#define TILEMAP_ROWS 24` でハードコード
  - 384×128 (ROWS=8) 等の低解像度モードに対応
  - `asm_tilemap.asm` 内の定数も連動が必要
  - BSS サイズ (`s_cov_mask` 等) が自動的に縮小する利点あり

### 中優先度

- [ ] **タイルマップBG + スプライト重畳のデモ**
  - 縦スクロールSTG風デモ (1層BG + V-scroll + スプライト数枚)
  - 384×128 解像度での実機fps計測
  - 描画パイプラインの実証: compose_scroll → sprite blit → present

- [ ] **8×8 タイルサイズ対応の検討**
  - 現在 16×16 固定 → 8×8 にすれば弾や小オブジェクトに対応
  - タイル数が4倍 (576→2304) になるためメモリ・走査コストが増加
  - 別途 `TILE_W`/`TILE_H` のパラメータ化が必要

### 低優先度

- [ ] **compose_scroll の BG 独立スクロール対応**
  - 現在 BG0 のスクロール差分のみ対応 (BG1以降が変化するとフォールバック)
  - パララックススクロール (BG0: 1x速度, BG1: 0.5x速度) の実現
  - 各BG独立にBBシフト+端列描画が必要なため、計算量は BG 数に比例

- [ ] **タイルアニメーション**
  - フレームごとにタイルIDを自動切り替える仕組み
  - 水面・炎等の環境アニメーション用
  - `tilemap_define_anim(id, frame_ids[], frame_count, tick_interval)`

- [ ] **ラスタスクロール連携**
  - `gfx_present_raster` との組み合わせで走査線単位のスクロール効果
  - 波打つ水面、ボス登場演出等

---

*作成: 2026-04-24*
