# TASK_SH_TRUNCATION — シェルが入力を黙って切り詰める欠陥を全部断つ

> 発行: PM (Claude Code `claude-opus-5`、2026-09-16) / 状態: **実装完了・レビュー待ち (2026-09-16、段 1〜段 4 着地)**
>
> 全 26 経路を 4 段で閉じた: 段 1 足場 (`0e60943`)、段 2 T1 + 打ち切り規則 (`2ef6f6a`)、
> 段 2b パイプの段 (`11705db`)、段 3 ルーター 8 件 + I1 (`3fd4da5`)、段 4 内蔵と入口 14 件 (`3c69b17`)、
> 段 4b rshell の EOT と印の漏れ (`9728039`)。試験 319 検査・変異 56 本。
> 教訓は [`../../POLICY_DEBUG.md`](../../POLICY_DEBUG.md) §4-38。

基点: `feat/gui` = `a6a6b0b`。
分割の経緯: [`TASK_EXIT_STATUS.md`](TASK_EXIT_STATUS.md) の設計レビュー 4 往復で同じ系統の欠陥が出続けた。
**ユーザー決裁 2026-09-16 で 2 票に分割**し、本票 (バグ修正) を先に緑にしてから終了コードの配線を載せる。
関連: [`../agents/HANDOVER_2026-09-16.md`](../agents/HANDOVER_2026-09-16.md) §7。

## 0. 目的

**シェルが入力を切り詰めたまま実行を続ける経路をすべて断つ。** 切り詰めたら実行せずに断る。

これは新機能ではなく**既存の不具合の修正**。`$?` の有無に関係なく今日のコードで起きる。
いちばん重いのは `if` の比較で、**条件が逆になって別の枝が走る**:

```
if "<256 文字以上で先頭 255 文字が同じ文字列 A>" == "<同 B>" rm -rf /data
```

`cmd_script.c` の `strip_quotes` が両方を 255 文字に切り詰めるので `A == B` が真になり、
**本来実行されない破壊的なコマンドが走る** (`cmd_script.c:349-357`、`cmd_if` の `v1[256]` / `v2[256]`)。

## 1. 確認した経路

### 1-1. 分割時に挙げた 14 件 (レビュー往復 1 で実コードと照合し、6 件を訂正)

| # | 場所 | 今の挙動 | 起きること |
|---|---|---|---|
| T1 | `cmd_script.c:349-357` `strip_quotes` + `cmd_if` の `v1[256]` / `v2[256]` | 255 文字で切って比較 | **条件が逆になる**。`==` が偽であるべきところで真、`!=` は逆。長さは**クォート除去後**で数える (`sh_args.inc:169,185` が既に落としている。`cmd_script.c:345` のコメントは古い) |
| T2 | `cmd_script.c:150` (`li < 255`) と `:135-136` (`SCRIPT_MAX_LINES` 128) | 256 文字目以降を切り詰め。129 行目以降は**赤字を出してから** `break` し、`return 0` で先頭 128 行を実行する | 切れた行が実行される。捨てた行は無かったことになる (「黙って」ではないが成功扱い) |
| T3 | `main.c:140-170` `try_exec` (`limit_args` = 510)、`cmd_mnt.c:41` `exec` (255)、`cmd_base.c:127` `time` (510) | 溢れた引数を落として**起動する** | 意図と違う引数でプログラムが走る。クォートの再付与で `"` `\` は 2 倍、空白入りは +2 |
| T4 | `main.c:299-305` `run_cmd_internal` | コマンド名を `PATH_MAX_LEN - 5` で切って `.bin` を付ける | **別のファイルが起動する** |
| T5 | `main.c:606` (`MAX_PIPE_STAGES` 8)、関数は `:608-631` | 9 段目以降と空の段を捨てる | 実行されない段の失敗が見えない |
| T6 | `sh_args.inc:84` `glob_cb` | `mem_alloc` の失敗で印を立てずに戻る | 一致の**一部だけ**を渡す |
| T7 | `sh_args.inc:222,230` (`patlen < 255`)、ディレクトリ部は `PATH_MAX_LEN-1` | パターンを切る | **別のファイルに一致する** |
| T8 | `cmd_env.c:205-210` (`set NAME VALUE` / `export`)、`cmd_script.c:332` (`ask`)、`:227` (値 256) | **訂正**: `set NAME=VALUE` で名前が 32 文字以上なら「値の表示」分岐に落ちて登録しない。切れた名前で登録するのは `set NAME VALUE` / `export NAME VALUE` / `ask` / `env_set` 直呼び。値の 256 文字切りは `=` 形でも起きる | 壊れた変数が登録される |
| T9 | `cmd_env.c:126-135` | `${NAME}` も**非ブレースの `$NAME`** も、31 文字で打ち切って残りがリテラルとして残る | 展開されない文字列がコマンド行に混ざる |
| T10 | `rshell.c:120-124` | 126 文字で読み取りを止め、**その接頭辞を実行する** | 途中まで実行される。残りが次の入力になる |
| T11 | `sh_launch.inc` + `exec/launch.c:142-146` | `len >= LAUNCH_CMDLINE_MAX` (256) で `INVAL`。`sh.bin` は「GUI 外」と読む | 理由が取り違えられる |
| T12 | `cmd_script.c:427-430` `cmd_if` の `join_args` | 切れたコマンド行を実行する。**到達するのは glob 展開で argv が伸びたとき** (`if a == a rm /tmp/*`) | 意図と違うコマンドが走る |
| T13 | `main.c:643` `execute_command` と `main.c:536` `execute_single` (どちらも `>= CMD_BUF_SIZE`) | 空行と長大行を**同じ扱い**で黙って return | 長すぎる行が無言で消える |
| T14 | `ui.c:65` `hist_add` (`j < 511`)、`ui.c:467` `hist_load` (8191 バイト読み) | 512 バイト目以降を切って履歴に残す。外部で書かれた `.history` の末尾行も途中で切れて入る | ↑キーで**切れた行が実行される** |

### 1-2. レビュー往復 1 が見つけた追加 (同型)

| # | 場所 | 起きること |
|---|---|---|
| T15 | `ui.c:645,666,705` 行編集 (`cmd_len < CMD_BUF_SIZE - 4`) | 4092 バイトで打鍵を黙って捨て、ENTER でその接頭辞を実行する。**機械注入 (`/api/key text=`) では気付けない** = 本票の動機であるランナーが偽の結果を得る |
| T16 | `cmd_filer.c:215-217` (`FL_MAX_NAME_LEN` 64)、`:144-153` `fl_path_join`、`:331-339`、`:73-76` `ft_load` (8191) | 63 バイトで切った名前で**別のファイルを起動する / 別のディレクトリへ入る**。`/etc/filetypes` の末尾行が切れて別コマンドに関連付く |
| T17 | `sh_exec.inc` の `try_exec_from_path` の PATH 分割、`ui.c:264` タブ補完 | **255 文字以上**の PATH 項目で区切りを見失い、残りが別のディレクトリとして扱われる (票は当初 254 と書いたが実測は 255。段 3 で訂正)。連結が入り切らない場合も同型 |
| T18 | `cmd_script.c:289-296` `ask` の prompt (254) / `:323` input (254) | 255 文字目以降の打鍵を捨てて変数に入れる |
| T19 | `rshell.c:242-257` `resolve_host_path` (`host_path[256]`) | `push` / `recv` で切れた別パスを `O_TRUNC` で作る |
| T20 | `ui.c:413` `hist_build_path`、`ui.c:522` profile パス | `HOME` が 244 文字以上だと**別ディレクトリの `.profile` を読む** |
| T21 | `ui.c:143,210` タブ補完の `name_store` (63 / 126) | 補完結果が切れて別の名前になる (画面には出るが ENTER で別ファイルに作用) |
| T22 | `cmd_sys.c:161-199` `cfg_set_key` | `/etc/system.cfg` を 1023 バイトで読み、**切れたまま書き戻す** (1KB 超の設定が消える) |
| — | `cmd_fs_shared.c:215,261` | `fs_append_basename` / `fs_parse_two_args` は**呼び手が無い**。表に「未使用」と記録し、削除するか決める |

**変更不要と確認されたもの** (レビューが現物で確かめた): `cmd_file.c` (`fs_join_path` の `-1` を全呼び手が見る)、
`cmd_dir.c`、`cmd_base.c` の `time` 以外、`sh_ls.inc`、`split_pipeline` の段バッファ、`sh_stage_is_builtin`、
`sh_path_normalize`、カーネルのトークナイザ (`exec/exec.c:1636-1660`)。

## 2. 方針

**「切り詰めたら実行しない」を 1 つの規則にする。**

1. 入力が上限を超えたと分かったら、**その場で赤字のエラーを 1 行出して、行全体を実行しない**。
   切り詰めた値で先へ進まない (比較・起動・登録・展開のいずれも)。
2. エラーの文言は「何が上限を超えたか」と「上限」を出す (例: `sh: argument too long (max 255)`)。
3. 上限そのものは**変えない** (バッファを増やす改修ではない)。上限は既に定数なので [C4] に従って参照する。
4. 履歴 (T14) は実行経路ではないので、**切れた行は履歴に入れない**。`hist_load` も切れた行を読み込まない。
5. `glob` の確保失敗 (T6) は行全体を断り、確保済みの文字列を解放する。
6. `filer` (T16) は収まらない名前を**表に載せず件数だけ報せる** (`sh_ls.inc` の作法に揃える)。
   `fl_path_join` は溢れたら断り、`ft_load` は読み切れなければ関連付け表を使わない。

### 2-1. スクリプトの中で断ったら、その先を実行しない (レビュー往復 1 blocker 1)

**断った行を捨てて次の行へ進むと、切り詰めを直したせいで本来走らないコマンドが走る。**

```
set A=<249 文字>
if $A$A == $A$A goto done      ← 展開後 498 文字。今は切り詰めて一致し goto done へ飛ぶ
rm -rf /data                    ← if を「実行しない」だけにすると、ここへ落ちる
:done
```

規則: **スクリプト実行中に行を断ったら `script_abort_flag` を立てて打ち切る** (`goto` のラベル無しと同じ扱い)。
組み込み handler は `void` のまま (`int` 化は [`TASK_EXIT_STATUS.md`](TASK_EXIT_STATUS.md) の範囲) なので、
**断ったことを表す印をグローバルに 1 本**持ち、`execute_command` の入口で消し、`script_exec` が
`execute_command` の後に見る。`if` / `time` 経由の入れ子 `execute_command` からも伝わること、
入れ子 `source` は `script_source_file` の戻り値で親に伝えることを実装の条件に入れる。

起動時の `/etc/profile` で断ったときは、メッセージを出して**既定値で続行する** (起動を止めない)。

### 2-2. `rshell` で断るときは EOT を返す (同 blocker 2)

`/api/cmd` は rshell にシリアルで流して **EOT (0x04) を待つ**。断りを空行と同じ `continue` で書くと
EOT が返らず、ホスト側は timeout まで待たされ**以後のコマンドが全滅する** (`POLICY_DEBUG.md` §4-7 と同じ壊れ方)。
規則: 赤字を 1 行出した後も `buz_off` → 1 tick 待ち → `serial_putchar(0x04)` を必ず通す。
行末までの読み捨ては `rshell.c:127` と同じ待ちで行う。

### 2-3. 網羅の要求

上の 22 件はレビュー 5 往復が積み上げたもの。実装の前に `userland/shell/` の**入力が通る固定長バッファを
1 度全部洗い**、「場所 / 上限 / 今の挙動 / 変更の有無」の表を `tools/tests/sh_truncation_tdd.md` に残す。
洗う対象: `char [N]` の宣言、`strncpy` / `kstrncpy` / `str_copy` / `strncat`、
`PATH_MAX_LEN` / `CMD_BUF_SIZE` / `SCRIPT_MAX_*` / `TRY_EXEC_BUF_SIZE` / `ENV_*_MAX` / `HIST_LINE_MAX` /
`LAUNCH_CMDLINE_MAX` / `NAME_CAP` / `FL_MAX_NAME_LEN` / `MAX_PIPE_STAGES` / `MAX_ARGS`。

## 3. 範囲

`userland/shell/` の `main.c` / `shell.h` / `sh_args.inc` / `sh_launch.inc` / `cmd_script.c` / `cmd_env.c` /
`cmd_mnt.c` / `cmd_base.c` / `cmd_filer.c` / `cmd_sys.c` / `cmd_fs_shared.c` / `rshell.c` / `ui.c`、
`userland/lib/filer/` (名前の幅)。**カーネルは変えない** (`exec/launch.c` の上限は読むだけ)。
文書は `docs/manpages/` のシェルの頁と `docs/POLICY_DEBUG.md` §4 (T1 / T4 / T16 の記録)。

## 4. 受入

ホスト試験 `tools/tests/sh_truncation_host.c` + `test_sh_truncation.py` + `sh_truncation_tdd.md`
(RED → GREEN を記録)。**実物の登録表と `execute_command` を通す**。`try_exec` / `run_cmd_internal` は
`main.c` の static なので `sh_exec.inc` に切り出す (この切り出しは `TASK_EXIT_STATUS` §3 も前提にする)。

境界は「超」ではなく**「以上」**で書く (レビューが実コードと突き合わせた値):

| ID | 反例 | 期待 |
|---|---|---|
| U1 | `if` の両辺がクォート除去後 256 文字**以上**で先頭 255 文字が同じ (`==` と `!=` の両方) | コマンドを実行しない。**スクリプト中なら後続行も実行しない** (§2-1) |
| U2 | 255 文字ちょうど / 256 文字 (クォート除去後で数える) | 255 は通る、256 は断る |
| U3 | スクリプトに 256 文字以上の行 / 129 行目 / 読み込み上限超のファイル | スクリプトを実行しない (`script_load` が `-1`) |
| U4 | `try_exec` (510) / `exec` (255) / `time` (510) の再構築が溢れる。クォート再付与で伸びる場合も | **子を起こさない** |
| U5 | 251 バイトの接頭辞が実在する状態で長い名前 (T4) | `P.bin` を起動しない |
| U6 | 9 段のパイプ、`echo ok \|`、`\| echo`、`a \|\| b` | 行全体を実行しない。**`echo "a\|\|b"` も断る** (PM 決裁 2026-09-16: クォートを見ない分割では `a \|\| b` と区別できない。従来の挙動は「壊れたパイプとして妙な動きをする」だったので、黙って動くより断るほうが規則として一貫する。クォートを見る分割は引き続き範囲外) |
| U7 | `glob` の `mem_alloc` を N 回目で失敗 | 行全体を断り、handler を呼ばず、確保済みを解放する |
| U8 | 256 文字以上の glob パターン / `PATH_MAX_LEN` 以上のディレクトリ部 | 一致を試みない |
| U9 | `set NAME VALUE` / `export NAME VALUE` / `ask` で名前 32 文字以上、値 256 文字以上 (`=` 形も) | 登録しない |
| U10 | `${` + 32 文字以上 + `}` と、**非ブレースの `$` + 32 文字以上** | 展開エラー。素通りさせない |
| U11 | `rshell` に 127 文字以上の行 | 実行しない。**EOT が返る**。次の行は正常に動く |
| U12 | `sh.bin` で 256 バイト以上のコマンド行 | `launch_req` を呼ばない |
| U13 | `CMD_BUF_SIZE` 以上の行を `execute_command` と `execute_single` の両方へ | 空行と区別して断る |
| U14 | 512 バイト以上の行を実行した後に ↑ キー。外部で書いた長い `.history` | 履歴に切れた行が入っていない / 読み込まない |
| U15 | 行編集に 4092 バイト以上を注入 (`/api/key text=`) して ENTER | 実行しない、履歴にも入れない、`sh: line too long` を出す |
| U16 | `filer` で 64 バイト以上の名前の実行 / ディレクトリ移動、切れた `/etc/filetypes` | 別のファイルを起動しない。載らない名前は件数で報せる |
| U17 | 254 文字以上の PATH 項目 | 区切りを見失って別のディレクトリを試さない |
| U18 | `rshell` の `push` / `recv` で 250 文字以上のホスト側パス | 切れた別パスを作らない |
| U19 | `HOME` が 244 文字以上のときの履歴 / profile のパス | 別ディレクトリを読み書きしない |
| U20 | `cfg_set_key` に 1023 バイト以上の `/etc/system.cfg` | 切れたまま書き戻さない |
| R1 | `test_sh_shell.py` / `test_sh_launch.py` / `test_fs_kind_callers.py` / `test_hsync_*.py` | 退行なし |
| R2 | 起動時の `/etc/profile` が断られる場合 | 起動は続く (既定値)。メッセージは出す |

**ゲスト受入の経路** (レビュー往復 1 所見 8): `/api/cmd` は rshell 経由で 126 文字上限、スクリプト行は 255 上限
なので、256 文字の反例は **(a) `/api/key text=` で行編集へ注入**するか **(b) スクリプトで `set A=<249 文字>` →
`if $A$Ax == $A$Ay …` と展開で伸ばす**しか届かない。代表として U1 (条件の逆転) と U5 (別のファイルの起動) を
この経路で再現 → 修正後に断ることを確認する。

## 5. 実装の分け方 (レビュー往復 1 所見 13)

1 コミットにしない。**4 つに割り、各コミットに RED → GREEN を付ける**:

1. **足場** (挙動変更なし): `sh_exec.inc` の切り出し + `sh_truncation_host.c` の骨 + §2-3 の洗い出し表。
2. **T1 単独** (最重・最小・ゲスト受入の代表): `strip_quotes` / `cmd_if` + §2-1 の中断規則と印の配線。
3. **ルーター**: `main.c` / `sh_args.inc` / `sh_launch.inc` — T3 / T4 / T5 / T6 / T7 / T11 / T13 / T17。
4. **内蔵と入口**: `cmd_script.c` / `cmd_env.c` / `cmd_base.c` / `cmd_mnt.c` / `rshell.c` / `ui.c` / `cmd_filer.c` /
   `cmd_sys.c` — T2 / T8 / T9 / T10 / T12 / T14 / T15 / T16 / T18 / T19 / T20 / T21 / T22。

2 だけ先に着地させてよい。

## 6. この票でしないこと

終了コードの配線と `$?` ([`TASK_EXIT_STATUS.md`](TASK_EXIT_STATUS.md))、上限そのものの拡大、
`if` / `&&` / `||` の構文の追加、クォートを見るパイプ分割、カーネル側 (`exec/launch.c`) の上限の変更。

## 7. 往復記録

### 往復 1 — 設計レビュー (Fable 5.1 サブエージェント、2026-09-16) — Request changes

Codex と Antigravity (`agy`) がクォータ切れのため次点で実施。blocker 4 件・非blocker 9 件。
**blocker はすべて反映済み**:

| # | 所見 | 反映先 |
|---|---|---|
| 1 | スクリプト中で断った行を捨てて続行すると、**本来走らないコマンドが走る** (`if … goto` の後続に落ちる) | §2-1 (中断規則)、U1 / U3 |
| 2 | `rshell` の断りで **EOT を返さないと `/api/cmd` が全滅する** | §2-2、U11 |
| 3 | 洗い残し: 行編集が 4092 バイトで打鍵を捨てる (機械注入では気付けない) | T15、U15 |
| 4 | 洗い残し: `filer` が 63 バイトで切った名前で**別のファイルを起動する** | T16、U16、§3 に `cmd_filer.c` |

非blocker の反映: T8 / T9 / T2 / T11 / T12 の記述の訂正、境界を「以上」に統一、
追加の同型 6 件 (T17〜T22) と未使用関数の記録、ゲスト受入の経路 (§4 末尾)、実装の分け方 (§5)。
「方針で壊れる正当な用途は見つからなかった」ことも確認された (`assets/profile` は 4 行・最長 28 文字など)。


## 8. ゲスト受入 (PM、2026-09-16、`9728039`)

NP21/W 停止 → `nhd-pull` → `os32-cycle deploy` → 起動 (`API: v53`、配備サイズ一致)。

| # | 反例 | 結果 |
|---|---|---|
| 1 | 展開後 498 文字の `if` を含むスクリプト (`set A=<240>` → `if $A$A == $A$B …`) | **`if: left value too long (max 255)` + `script: aborted (line refused)`**。後続行は実行されない |
| 2 | ホストから 255 文字の行を送る (`/api/cmd` 経由) | `rshell: command line too long (max 126)`。接頭辞を実行しない |
| 3 | スクリプトの行が 255 文字超 | `source: script line too long (max 255)`。1 行も実行しない |
| 4 | `set <32 文字以上の名前>=x` | `set: variable name too long (max 31)` + 打ち切り |
| 5 | 普通のスクリプト (短い行だけ) | 最後まで走る (誤発火なし) |
| 6 | 断りの**直後**に普通のスクリプト | 最後まで走る (段 4b の退行の確認) |

**実機で見つけた退行 (段 4b で修正)**: 一度断りが起きると以降すべてのスクリプトが 1 行目で
打ち切られていた。`rshell` 自身が `execute_command("rshell")` の中で走るため、入口の印の消去
(`g_exec_depth == 0` のときだけ) が効かない。**ホスト試験は `-DSHELL_AS_APP` の構成だけを組んでおり、
`rshell` は常駐シェル側にしかないので出なかった**。深さを作る足場を追加して塞いだ。

未実施: T16 / T23 (ファイラ) の実機確認、`push` の 4KB 超の実機往復、`ui.c` の行編集の
機械注入 (`/api/key text=`)。いずれもホスト試験では押さえている。
