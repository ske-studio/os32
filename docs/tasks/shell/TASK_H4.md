# TASK_H4 — 配備マニフェストと世代の確認

> 発行: PM (Claude Code `claude-opus-5`、2026-09-16) / 状態: **実装中 (2026-09-16、設計レビュー 3 往復で Approve、設計凍結)**

基点: `feat/gui` = `9742a6b`。
計画の正典: [`HSYNC_IMPROVEMENT_PLAN.md`](HSYNC_IMPROVEMENT_PLAN.md) §7-1 (本文)、§8 (H4 の行)、§9 の A18。
前提の票: [`TASK_H1.md`](TASK_H1.md)、[`TASK_H2.md`](TASK_H2.md) (置換の安全化)、[`TASK_H3.md`](TASK_H3.md) (mtime)。
引き継ぎ: [`../agents/HANDOVER_2026-09-16.md`](../agents/HANDOVER_2026-09-16.md) §4。

## 0. 目的

**「古い配備元から新しい成果物へ戻してしまう」事故を検出する。**

内容の違いは H1 の内容比較で分かる。しかし**どちらが意図した版か**は分からない。
`make deploy` を忘れたまま `hsync` を打てば、ゲストの新しいファイルがホストの古いもので
上書きされる。これは「同期が成功した」ように見える。

H4 は配備元に**世代の名札**を置き、`hsync` がそれを読んで食い違いを報せる。

## 1. 確認した事実 (2026-09-16、`9742a6b`)

1. ホスト側の配備は `tools/hostdrv_deploy.py sync`。層ごとの `deploy.yaml` を
   `tools/deploy_manifests.py` がまとめ、`C:\os32` (WSL から `/mnt/c/os32`) へ写す。
2. 失敗の扱いは H2 の往復で直っており、**コピーの失敗は握り潰さない** (`state` が
   `'ok'` / `'protected'` / `'error'`、致命的な失敗は `sys.exit(1)`)。
   **マニフェストを書く場所はこの成功判定の後**にする。
3. `hsync` 側には既に理由コードと集計がある (H1 / H2)。**世代の照合はここに足す**。
4. CRC-32 のストリーム版 `lib/crc32_core.inc` はホストとゲストで同じ値を出すことを
   H1 で確認済み。マニフェストの CRC はこれで出す。
5. ゲストに JSON のパーサは無い。**行指向の平文**にする。
6. 版の識別に使えるもの: `git rev-parse --short HEAD`、作業ツリーの汚れ (`git status --porcelain`)、
   `build/out/kernel.map` の有無。**日時や SHA の大小で新旧を決めない** (計画 §7-1)。

## 2. 設計

### 2-1. マニフェストの形式 — 決裁 D1

**推奨 (a)**: `C:\os32\.deploy\manifest.txt` (行指向の平文、UTF-8、LF)。

```
format=1
build=9742a6b+dirty
generated=2026-09-16T21:45:19Z
count=198
---
bin/cat.bin 16428 3b7f2a10 1789520013
boot/vmkernel.lz4 481400 a41c0e55 1789520100
```

- 先頭は `key=value` の行、`---` の後が 1 行 1 ファイル。
- ファイルの行は **パス / サイズ / CRC-32 (8 桁 16 進、小文字) / mtime (Unix 秒)**。
  区切りは空白 1 つ。**パスに空白を含むファイルは配備対象に無い**ことを検査で固定する
  (在ったら配備側が拒否する)。
- `build` は `<短い SHA>` に、作業ツリーが汚れていれば `+dirty` を付ける。
  **これは「同じか違うか」を見るための名札で、順序を表さない**。
- パスは `C:\os32` からの相対。**絶対パスと `..` は書かない・読まない**。

(b) 案: JSON。ゲストにパーサが無く、書くだけで数百行の実装が要る。推奨しない。

### 2-2. 書く側 (`tools/hostdrv_deploy.py`)

- **全ファイルの配備が成功した後にだけ書く**。1 件でも `error` があれば書かない
  (既にあるマニフェストは**消す** — 古い名札が残ると「配備済み」と誤読される)。
- 書き方は **一時ファイル + 置き換え**。中途半端なマニフェストを読ませない。
- `--no-manifest` で抑止できるようにする (調査時に使う)。

### 2-3. 読む側 (`hsync`)

- 起動時に `/host/.deploy/manifest.txt` を読む。**無ければ今までどおり動く** (後方互換)。
- 読めたら **1 行目に世代を表示**する: `DEPLOY build=9742a6b+dirty count=198 generated=…`。
- **`--expect-build <ID>`**: 名札が違えば**1 件も書かずに**非ゼロ終了 (`reason=build_mismatch`)。
  一致の判定は文字列の完全一致。**大小を比べない**。
- 既定 (`--expect-build` 無し) は**表示だけ**して同期を続ける。
  **勝手に拒否しない** — 意図した巻き戻しもあるため (計画 §7-1)。
- マニフェストは**全件を先に検査**してから使う。1 件でも壊れていたら**名札ごと捨てる**:
  - **`--expect-build` を指定していないとき**は表示だけして同期を続ける
    (`DEPLOY manifest invalid: <理由>`)。名札の不備で作業が止まるのは本末転倒。
  - **`--expect-build` を指定して、範囲を絞っていない (全体同期) ときは断る**
    (`reason=manifest_invalid`、1 件も書かずに非ゼロ終了)。利用者は世代を確かめるつもりで
    指定している。**名札が読めないことを「一致」と扱わない**
    (往復 1 所見 2: 壊れた名札で保護が素通りすると、H4 が防ぐはずの事故がそのまま起きる)。
  - **範囲を絞った同期 (`hsync usr` のように引数がある) では表示だけにして続ける**
    (往復 2 所見 1)。名札はルートの世代を表すもので、絞った範囲の正しさを保証しない。
    ここで止めると「名札の不備で作業が止まる」形になり、本末転倒。
  捨てる条件:
  - 形式版が `1` でない / `---` が無い / `count` と行数が合わない
  - 重複するパス / 絶対パス / `..` を含むパス / 長すぎるパス (`NAME_CAP` 超)
  - サイズや CRC の桁が不正
- **マニフェストを信じて内容比較を省かない** (初期実装)。CRC が一致していても、
  宛先が同じである証明にはならない (計画 §7-1)。**永続 CRC キャッシュも作らない**。
- **集計の数え方** (往復 2 所見 2 で設計を訂正):
  `hsync` は**配備元を正として列挙する** (`sync_directory`)。名札にあるのに配備元に無いファイルは
  **列挙に現れないので、そのままでは数えられない**。数えるには名札を正とする走査が別に要る。
  - `manifest_extra=N` (名札に無いものを写した) は**今の列挙のまま数えられる**。各ファイルを
    写すときに名札の表を引けばよい。**初期実装はこれだけにする**。
  - `manifest_missing` (名札にあるのに配備元に無い) は**初期実装では出さない**。
    名札の全件を配備元に対して `stat` し直す走査が要り、128 件の列挙上限や `/sys` の既定除外と
    絡むので、**別票**にする。票にそう書く。
  - 名札を引く表は**メモリ上限がある**。`hsync` の既存の上限 (`MAX_FILES` 128 / `NAME_CAP` 64) と
    同じ考え方で、名札が大きすぎたら**名札ごと捨てる** (`manifest invalid: too many entries`)。
    実際の配備は約 200 件なので、上限は実測に合わせて決める (票では値を固定しない)。

### 2-3-1. 保証しないこと — 配備と名札の間の非原子性 (レビュー往復 1 所見 1)

**ファイル群のコピーと名札の更新は原子的ではない。** 全件のコピーが成功した後、名札を書く前に
ホストが落ちると、**配備元は新しいのに名札は古い (または無い)** 状態になる。
「一時ファイル + 置き換え」が守るのは**名札そのものの完全性**だけで、この隙間は埋まらない。

この状態で `--expect-build <新しい ID>` を打つと **`build_mismatch` で断られる**。
ファイルは新しいのに同期できない。**安全側の壊れ方**ではあるが、起こることとして書いておく。

復旧は `make deploy` をもう一度打つだけ (名札が書き直される)。
埋めるには配備の完了を示す別の印か世代別ディレクトリが要るので、**§2-4 の範囲外**とする。

### 2-4. しないこと (H4 の範囲外)

- **世代別ディレクトリ** (同期中に配備元が動かない仕組み)。本命だが別票。
  運用契約「同期中に `make deploy` しない」で代替する (計画 §7-1)。
- `kernel` / `shlib` / `apps` を**原子的に切り替える**保証。
- SHA-256 の併記。CRC-32 で足りる用途に限る。
- **`manifest_missing` の集計** (名札にあるのに配備元に無い)。名札を正とする走査が要る。別票。

## 3. 範囲

| 層 | ファイル |
|---|---|
| ホスト | `tools/hostdrv_deploy.py` (書く)、`tools/deploy_manifests.py` (必要なら) |
| ゲスト | `userland/system/hsync.c` (読む・照合・表示・`--expect-build`) |
| 試験 | `tools/tests/h4_manifest_host.c` + `test_h4_manifest.py` + `h4_manifest_tdd.md`、ホスト側は `tools/tests/test_hostdrv_manifest.py` |
| 文書 | `docs/manpages/hsync.1`、`docs/08_build.md` §8-4 (配備 3 経路の節) |

## 4. 受入

### 4-1. ホスト試験

| ID | 反例 | 期待 |
|---|---|---|
| M1 | 全件成功の配備 | マニフェストが書かれ、`count` と行数が一致する |
| M2 | 1 件でも失敗した配備 | **書かない**。既にあるマニフェストは消す |
| M3 | 書き込みの途中で失敗 | 中途半端なマニフェストが残らない (一時ファイル + 置き換え) |
| M4 | `--no-manifest` | 書かない。配備自体は今までどおり |
| M5 | 名札が無い配備元 | `hsync` は今までどおり動く (後方互換) |
| M6 | 名札が壊れている (形式版・`count` 不一致・重複パス・`..`・絶対パス・長すぎる名前)、`--expect-build` **無し** | **名札ごと捨てて** `manifest invalid` を表示し、同期は続ける |
| M6b | 同、`--expect-build` **有り** | **1 件も書かずに断る** (`reason=manifest_invalid`)。名札が読めないことを「一致」と扱わない |
| M6c | 名札が**無い**配備元に `--expect-build` を指定 | 同上 (断る)。指定した以上、確かめられないなら進まない |
| M11 | 全件コピー成功 → 名札を書く前に中断 → `--expect-build <新 ID>` | `build_mismatch` で断る (§2-3-1 の想定どおり。安全側) |
| M7 | `--expect-build` が一致 | 同期する |
| M8 | `--expect-build` が不一致 | **1 件も書かずに** 非ゼロ終了。`reason=build_mismatch` |
| M9 | 名札に無いものを写した | `manifest_extra` に数える |
| M9b | 名札が大きすぎる (表に収まらない) | 名札ごと捨てる (`too many entries`)。`--expect-build` の扱いは M6 / M6b と同じ |
| M12 | `--expect-build` + **範囲を絞った同期** で名札が壊れている | 表示だけして同期を続ける (往復 2 所見 1) |
| M10 | 名札の CRC が宛先と一致していても | **内容比較を省かない** (H1 の判定順を変えない) |
| R1 | `test_hsync_h1/h2/h3/protect.py` | 退行なし |

### 4-2. ゲスト受入

1. `make deploy` → ゲストで `hsync -n` → **1 行目に `DEPLOY build=…` が出る**。
2. わざと古い配備元 (別の `build` の名札) を置いて `hsync --expect-build <新しい ID>` → 1 件も書かずに断る。
3. 名札を壊して `hsync` → `manifest invalid` を表示して同期は続く。
4. 名札の無い配備元 (`--no-manifest` で作る) → 今までどおり動く。

## 5. 往復記録

### 往復 1 — 設計レビュー (ローカル `gemma4:31b`、`e3d6159` 対象、2026-09-16) — Request changes

| # | 所見 | PM の確認 | 対応 |
|---|---|---|---|
| 1 | 全件コピーの後・名札を書く前に落ちると、**ファイルは新しいのに名札は古い**。「一時ファイル + 置き換え」では埋まらない | そのとおり。埋めるには別の印か世代別ディレクトリが要る | §2-3-1 に**保証しないこと**として明記。受入 M11 を追加 |
| 2 | 名札が壊れていると「捨てて同期を続ける」が働き、**`--expect-build` の保護が素通りする** | **設計の穴**。利用者は世代を確かめるつもりで指定している | `--expect-build` 指定時は**断る** (§2-3)。受入 M6b / M6c を追加 |

確認された範囲: 名札を信じて壊れる経路 (内容比較を省かないので無し)、書く側の順序、読む側の検査項目、
`--expect-build` の完全一致と意図した巻き戻しの許容、受入表の網羅性。

### 往復 2 — 設計レビュー (ローカル `gemma4:31b`、`f1a733e` 対象、2026-09-16) — Request changes

往復 1 の 2 件は閉じたと確認された。新しい所見 2 件、**どちらも妥当**:

| # | 所見 | PM の確認 | 対応 |
|---|---|---|---|
| 1 | 範囲を絞った同期 (`hsync usr`) でも、壊れた名札を理由に断ってしまう。名札はルートの世代を表すもので、絞った範囲の正しさは保証しない | そのとおり。行き過ぎ | 断るのは**全体同期のときだけ**に限定 (§2-3)。受入 M12 |
| 2 | **`manifest_missing` は今の実装では数えられない**。`hsync` は配備元を正として列挙するので、名札にあるのに配備元に無いファイルは列挙に現れない | **設計の誤り**。実装可能性を確かめずに書いていた | `manifest_extra` だけを初期実装にし、`manifest_missing` は**別票**へ (§2-3、§2-4)。名札の表のメモリ上限も追加 |

確認された範囲: 往復 1 の 2 件、H1〜H3 との干渉 (名札は `.hs~` の掃除に当たらない、同期対象としての扱い)、
名札の読み取り失敗時の分岐、受入表の境界条件。

### 往復 3 — 設計レビュー (ローカル `gemma4:31b`、`29b028f` 対象、2026-09-16) — **Approve**

往復 1・2 の所見がすべて閉じたことを確認。**新しい blocker は無し**。

実コードで確かめられた点:
- `hsync /` は `subdir` が無く全体同期として扱われ、名札の不備で断られる。
  `hsync usr` は絞り込みなので表示だけで続く。引数が 2 つ以上は既存の解析がエラーにする。**抜け道なし**。
- 名札は `.deploy/` 配下なので、H2 の予約名 (`.hs~`) の掃除に当たらない。
- 名札が大きすぎるとき捨てる判断は妥当。名札は事故防止の補助で、本体の同期は名札なしでも正しく動く。
  名札のためにメモリを食って本来の列挙 (`FileList`) を妨げるほうが害が大きい。

**この票は設計凍結。実装へ進む。**

## 6. 決裁

| ID | 問い | 推奨 | 決裁 |
|---|---|---|---|
| D1 | マニフェストの形式 | (a) 行指向の平文 (`.deploy/manifest.txt`) | **(a) ユーザー決裁 2026-09-16** |
