# TASK_H2 — hsync の置換安全化 (一時ファイル + 検証 + 置換)

> 発行: PM (Claude Code `claude-opus-5`、2026-09-16) / 状態: **受入完了 (2026-09-16、設計 3 往復 + 実装レビュー Approve、実機受入済み)**

基点: `feat/gui` = `d191fd5`。
計画の正典: [`HSYNC_IMPROVEMENT_PLAN.md`](HSYNC_IMPROVEMENT_PLAN.md) §6 (手順と保証の限界)、§8 (H2 の行)、§9 (A14 / A15 / A17)。
引き継ぎ: [`../agents/HANDOVER_2026-09-16.md`](../agents/HANDOVER_2026-09-16.md) §3。
前提の票: [`TASK_H1.md`](TASK_H1.md) (内容比較・CRC 読戻し)、[`TASK_H3.md`](TASK_H3.md) (mtime)、
[`TASK_FS_TYPE.md`](TASK_FS_TYPE.md) (B8: 読めなかったを無いと読まない、ext2 の書き込み順序)。

## 0. 目的

**新内容の書き込みと検証が済むまで旧宛先を残す。**
今の `hsync` は宛先を `O_WRONLY | O_CREAT | O_TRUNC` で開いて直接上書きする
(`userland/system/hsync.c` の `copy_verify`)。書き込み途中・検証・同期のどこで失敗しても
旧内容は既に切り詰められていて戻らない (失敗行に「直接上書きなので旧内容は残らない」と出している)。

保証する範囲と、しない範囲:

| 事象 | H2 の保証 |
|---|---|
| **公開の前** (一時ファイルへの書き込み・検証・mtime・置換の途中まで) の I/O 失敗、空き不足 | 旧宛先の名前と内容が残る。一時ファイルは**片づけられれば**片づける — メタデータの失敗でマウントが書き込み禁止 (`ROFS`) になった後は unlink も通らないので `STALE` と表示し、次の実行が予約名として消す (往復 3 所見 4)。非ゼロ終了 |
| **公開の後** (宛先エントリの inode が新しい方を指した後) の失敗 | **検証済みの新しい内容が宛先に現れる**。後始末 (旧 inode の解放、一時名の削除) が落ちたら漏れが残り、`e2fsck` が回収する。hsync は宛先を読み直して「公開された」ことを確かめ、`replace_partial` として報告する (成功に数えない) |
| コピー中の **コピー元 / 宛先の変化** (stat で見えるもの) | 置換しない (`source_changed` / `dest_changed`)。同サイズ・同時刻へ戻る変化は捕まえない (計画 §6) |
| **電源断・エミュレータ強制終了** | 宛先の名前は旧 inode か新 inode のどちらかを指す (§2-2 の順序による)。**一時ファイルが残り得る**。後始末は §2-4 の決裁。ジャーナルは無いので「原子的」とは書かない |
| 異なる FS をまたぐ置換、複数ファイルの一括切り替え | 対象外 |

## 1. 確認した事実 (2026-09-16、`d191fd5`)

1. **排他的作成が無い**。open フラグは `KAPI_O_RDONLY / WRONLY / RDWR / CREAT / TRUNC` の 5 つ
   (`sdk/include/os32/os32_kapi_shared.h:477-488`)。`wrap_sys_open` はフラグを素通しで `vfs_open` へ渡す。
   `vfs_open_internal` (`fs/vfs_fd.c`) は「`get_file_size` が NOTFOUND なら `write_file(path, "", 0)` で作る」。
   知らないビットは黙って無視される。
2. **ext2 のファイル置き換えは宛先を先に消す**。`ext2_rename` (`fs/ext2_dir.c:828`) は宛先が
   通常ファイルなら `ext2_unlink(new)` → `links_count++` → `ext2_add_entry(new)` → `ext2_delete_entry(old)`
   → `links_count--` の順。unlink の後で落ちると**宛先の名前が無い状態**が残る。
   ディレクトリ側 (`ext2_rename_dir`) は B8 で「旧名を先に消す」に決裁済みで、本票の対象外。
3. `sys_rename` の利用者は `userland/shell/cmd_file.c:263` (`mv`) と `userland/system/install.c:89`。
   SQLite の VFS (`lib/sqlite3/os32_sqlite_vfs.c`) は rename を使っていない。
   §2-2 の変更はこの 2 者の挙動も (より安全な方へ) 変える。
4. VFS の FD は**パスで** FS を指す (`open_files[fd].path`)。置換で inode が替わると、開いている FD の
   続きの read は新しい内容を読む。今の unlink + add でも同じなので、H2 で新しく生まれる問題ではない。
5. `OS32_Stat` に `st_nlink` がある (`os32_kapi_shared.h:463`)。
6. KAPI は v52。アプリは `api->version` で版を見られる (`userland/tests/host_test.c:48` に前例)。
7. `hsync` は `/boot` も同期できる (`g_touched_boot`)。**古いカーネルで新しい hsync を動かす**場面
   (カーネルを入れ替える途中) は現に起こる。

## 2. 設計

### 2-1. 排他的作成 `O_EXCL` — 決裁 D1

**推奨 (a)**: `KAPI_O_EXCL` (`0x0400`) を足し、KAPI を **v53** にする。

- `vfs_open_internal`: `O_EXCL` は `O_CREAT` と組でだけ有効 (単独は `VFS_ERR_INVAL`)。
  名前が**何であれ存在すれば** `VFS_ERR_EXIST` (ディレクトリでも ISDIR ではなく EXIST)。
  判定できない (NOTFOUND 以外の負値) なら**その値を返す** (B8)。
- 排他性の根拠: VFS は非再入で、ゲストは協調型 (GetMessage 方式) なので、1 回の `sys_open` の中の
  「無いことの確認 → 作成」に他のゲスト処理は割り込まない。**ホスト側が同時に書ける FS では成り立たない**。
- そのため `VfsOps` の末尾に任意フック `create_excl(ctx, path)` を足し、**持つ FS だけが `O_EXCL` を受ける**。
  ext2 は実装する (内部で `find_entry` → NOTFOUND のときだけ作成)。HostDrv / FAT / ISO9660 は持たず、
  `O_EXCL` は `OS32_ERR_NOSYS`。`VfsOps` はカーネル内部の構造体で ABI ではない (H3 の `set_mtime` と同じ扱い)。
- `KAPI_O_EXCL` は `os32_kapi_shared.h` の手書き定数 (生成物ではない)。`kapi.json` の構造体は変わらないが、
  **`sys_open` の意味が広がる**ので版数を上げる ([ABI3]、手順はスキル `os32-kapi-add`)。

(b) 案: hsync 専用の `sys_create_temp(dir, prefix, out_name)`。汎用性が無く、名前の規則をカーネルに持ち込むので推奨しない。

### 2-2. ext2 のファイル置き換えを「宛先の名前を消さない」順にする — 決裁 D2

**推奨 (a)**: 宛先のディレクトリエントリの **inode 番号をその場で書き換える** (Linux の `ext2_rename` と同じ考え方、コードは参照しない)。

新しい順序。**不変条件は 2 つ**: (i) どの inode も名前の数 ≤ `links_count`、
(ii) 宛先の名前は常に旧 inode `D` か新 inode `S` のどちらかを指す。

| 段 | 書くもの | 進む条件 | ここで落ちたときの媒体 |
|---|---|---|---|
| 0 | 名前解決メモの世代を進める (`ext2_ns_touch`) | — | 何も書いていない |
| 1 | `S` の `links_count++` | — | 名前 1、links +1 (多い側)。`D` は無傷 |
| 2 | **公開**: 宛先エントリの `inode` フィールドを含む**セクタだけ**を書く (`D` → `S`) | — | §2-2-1 の 3 値 |
| 3 | 一時名 (移動元) を消す (`ext2_delete_entry`) | 段 2 が**公開済みと確定**したときだけ | 名前 2・links 2 = 整合。次の実行が一時名を消して収束 |
| 4 | `S` の `links_count--` | **段 3 の削除が確定したときだけ** | 名前 1・links 2 (多い側)。次の更新は §2-3 手順 6 で `hardlink` と報告 |
| 5 | `D` の `links_count--`、0 なら `ext2_unlink` の後半と同じ解放 (参照を消してから解放、B8 往復 3) | 段 2 が公開済みと確定したときだけ (段 3 / 4 の成否には**依存しない**) | `D` の漏れ |
| 6 | `ext2_sync` | — | — |

- **宛先の名前はどの段でも消えない** (旧 inode か新 inode を指す)。これが H2 の I/O 失敗時保証の土台。
- **段 4 を段 3 の成否に縛るのが要点** (Codex 往復 2 所見 1)。段 3 が失敗したのに段 4 を実行すると
  「名前 2・links 1」になり、次の実行が一時名を消した瞬間に `links_count` が 0 になって
  **宛先が指している新しい内容が解放される** (`fs/ext2_file.c:480` の `ext2_unlink` はこの減算と解放を実際に行う)。
  減らせないときは**多い側に残す** — 漏れは e2fsck が回収できるが、解放は戻せない。
- 段 2 の対象は宛先エントリの `inode` (4 バイト、エントリ先頭)。`file_type` は `S` と `D` が
  同じ型のときだけこの経路に進む (通常ファイル同士だけ。それ以外は従来の unlink + add 経路)。
- `D` が他にも名前を持つ (`links_count > 1`) 場合は段 5 で減らすだけ。hsync は §2-3 手順 6 で先に断る。

### 2-2-1. 「公開したか」を 3 値で確かめる

段 2 は「成功 / 失敗」ではなく **公開済み / 未公開 / 不明** の 3 値で扱う (往復 2 所見 2)。
1KB のブロックを 512B ずつ 2 回書く経路なので、「変更したフィールドが 1 セクタ内」は
「失敗したら未変更」を意味しない。フィールドがブロックの**後半**にある配置では、前半だけ書けて
後半が失敗する = 未公開なのに「書けた」ように見えることがある。

- 段 2 は**フィールドを含むセクタ 1 本だけ**を書く (ブロック全体を書き直さない)。対象セクタの決め方は
  `fs/ext2_dir.c:107` の `ext2_same_sector` と同じ考え方。
- 書き込み成功 → **公開済み**。
- 失敗 → そのセクタを**読み直す**。`S` を指していれば公開済み、`D` のままなら未公開。
- 読み直しも失敗 → **不明**。ここで止め、`ext2_fs_error` でマウントを書き込み禁止にし、
  段 3〜5 を**一切実行しない** (`D` を解放しない)。媒体は「宛先 `S`・名前 2・links 2」か
  「宛先 `D`・名前 2・links 2」のどちらかで、どちらも整合する。

戻り値: 公開済みで段 3〜6 (最後の `ext2_sync` を含む) まで通れば `EXT2_OK` — **段 6 の失敗も伝播する**
(解放に伴う superblock / グループ記述子の同期が落ちる経路がある。今の `ext2_rename` と同じ契約、往復 3 所見 5)。
公開済みだが後始末が落ちたら `EXT2_ERR_IO`
(呼び手は §2-3 手順 8 で公開の有無を確かめる)。未公開の失敗も `EXT2_ERR_IO`。不明は書き込み禁止になる。

### 2-2-2. 名前解決メモを先に捨てる

`ext2_resolve_path` (`fs/ext2_vfs.c:45`) はパス → inode の記憶を持ち、`ext2_ns_touch` で世代が
進むまで使い回す。段 2 は `delete_entry` / `add_entry` を通らないので**既存の無効化に相乗りできない**
(往復 2 所見 4)。段 2 を**試みる前に** `ext2_ns_touch` を呼ぶ。さもないと手順 5 の stat で
`dst → D` が記憶され、段 2 の後に開き直しても古い `D` を読む (解放済み inode を読む経路もできる)。

### 2-2-3. `ext2_unlink` の失敗を握り潰さない

`fs/ext2_file.c:507` の複数リンク分岐は `ext2_write_inode` の戻り値を捨て、末尾の `ext2_sync` の
戻り値も捨てて `EXT2_OK` を返す (往復 2 所見 5)。このままでは復旧の掃除で「名前は消えたが
`links_count` は 2 のまま」でも `cleaned` 成功・`errors=0` になる。**両方の戻り値を伝播する**。
`fs/ext2_file.c` を §3 の範囲に足す。

### 2-2-4. 途中で止まった媒体からの復旧

(往復 1 所見 3、往復 2 所見 6)

| 止まった位置 | 媒体 | 次の `hsync` |
|---|---|---|
| 段 1 の直後 | 一時名だけが `S`、links 2 | 一時名を予約名として消す (`st_nlink` は見ない) → links 1・名前 0 = 漏れ。再コピーで**内容は収束** |
| 段 2 の直後 | 宛先と一時名が `S`、links 2 | 一時名を消す → 名前 1・links 1。**新しい内容として整合、収束** |
| 段 3 の直後 (段 4 の前) | 宛先 `S`・links 2、一時名なし | 内容は新しい。次の更新は `hardlink links_count=2` で断る。媒体の修復はホストの `e2fsck` |
| 段 3 が失敗・段 4 は未実行・段 5 は成功 | 宛先と一時名が `S` (2 名・links 2)、`D` は解放済み | 一時名を消して 1 名・links 1。**収束** (往復 3 所見 3) |
| 段 4 の直後 | 宛先 `S`・links 1、旧 `D` は名前 0・links 1・データ保持 | 内容は正しい。`D` は漏れ |
| 段 5 の途中 | `S` は正常、`D` は links 0・ポインタ 0。ブロックか inode が返りきっていない | 内容は正しい。漏れ |

**「内容の収束」と「媒体の修復」は別**。hsync は内容を収束させるが、`links_count` の過大計上や
漏れは直さない (推測で links を書き換えない)。`e2fsck -fn` は**診断**で、直すのはホスト側で
修復モードの `e2fsck` を回したとき。B8 §2-6 の「漏れは意図した交換」と同じ扱い。

- 失敗注入: `tools/tests/b8_open_host.c` の once / sticky 掃引と二重故障掃引に「ファイルの置き換え
  rename」を載せ、`media_check` と `e2fsck -fn` で**漏れ以外の不整合 0** を示す。媒体の配置は
  「移動元と宛先が同じブロックの前 / 後」「同じセクタ / 別セクタ」「別ブロック」「対象フィールドが
  ブロックの前半 / 後半」に分ける (往復 1 所見 5、往復 2 所見 2)。

(b) 案: ext2 はそのまま、hsync が「宛先を `.old` に rename → 一時を本名に rename → `.old` を消す」。
2 回目の rename の前後で**宛先の名前が無い瞬間**が残り、`.old` の後始末も増える。推奨しない。

(c) 案: ext2 はそのまま、hsync の復旧契約に「宛先が消えていたら一時ファイルから戻す」を入れる。
`mv` と `install` は守られないまま。推奨しない。

### 2-3. hsync の新しい手順 (dry-run 以外、コピーが要ると決まった後)

計画 §6 の 6 段を具体化する。既存の判定順 (H1 / H3: サイズ → mtime 前置 → 内容比較) は**変えない**。

1. **一時名**: 宛先と同じディレクトリに `.hs~<名前>`。長さの上限は `EXT2_NAME_LEN` ではなく
   **hsync 自身の列挙幅** `NAME_CAP` (64、`hsync.c:203`) — 一時名が `NAME_CAP` に収まらないと
   掃除の列挙で拾えなくなる (往復 2 所見 7)。`strlen(name) + 4 >= NAME_CAP` なら
   `reason=name_too_long` で失敗 (errors)。**直接上書きへは落とさない**。
   コピー元の列挙で `.hs~` で始まる名前は**同期対象にしない** (`ls_cb` の中で、128 件の枠に入れる前に弾く。
   予約名が枠を食って通常ファイルが落ちるのを防ぐ)。
2. **作成**: `sys_open(tmp, O_WRONLY | O_CREAT | O_EXCL)`。`EXIST` なら §2-4 (決裁 D3)。
   `NOSYS` (宛先 FS が排他的作成を持たない) なら `reason=replace_unsupported` で失敗し、**直接上書きへ黙って落ちない**。
3. **書き込み + CRC**: 今の `copy_verify` の本体を一時ファイルへ。short write は進め、0 進捗・負値は失敗。
   `OS32_ERR_FULL` は `reason=no_space`。
4. **同期 → 読戻し検証**: `vfs_sync` → 一時ファイルを開き直して長さ・CRC を照合 (今と同じ)。
5. **再確認**: コピー元を stat し直し、サイズと mtime が手順開始時と同じか (`source_changed`)。
   宛先を stat し直し、「判定時に無かった → まだ無い」「判定時に通常ファイル → まだ通常ファイルで、
   サイズ・mtime が判定時と同じ」か (`dest_changed`)。**保護対象の判定 (`dst_protected`) もここで取り直す**。
   **このとき一時ファイルの `st_ino` と、宛先が在るなら宛先の `st_ino` を控える** (手順 8 で使う)。
6. **hardlink**: 宛先の `st_nlink > 1` なら `reason=hardlink` で失敗 (計画 §6、別名まで更新する仕様を持ち込まない)。
   判定は手順 5 と同時に、**一時ファイルを作る前にも**行う (無駄な書き込みを避ける)。
7. **mtime**: 一時ファイルに `sys_set_mtime` (H3)。rename は inode の mtime を変えないので、本名に現れた時点で
   日時が揃っている。**ここでの失敗は「公開の前の失敗」**なので `reason=metadata_failed` で中断し、
   一時ファイルを消す。**`copied` に数えない** (Codex 往復 1 所見 4: ext2 はメタデータの I/O 失敗で
   マウントを書き込み禁止にするので、続く rename は必ず `ROFS` になり、宛先は旧内容のまま。
   本名へ書いた後に mtime を付ける今の `apply_mtime` の集計とは前提が違う)。
   コピー元の mtime が不明 (0) のときに設定を省くのは今どおり (`mtime_unknown`)。
8. **置換**: `sys_rename(tmp, dst)`。
   - 成功: `copied`。
   - 失敗: **宛先の `st_ino` で公開の有無を判定する** (往復 1 所見 1、往復 2 所見 3)。
     手順 5 で控えた一時ファイルの ino と一致 → `reason=replace_partial` (公開済み・後始末が落ちた)。
     旧宛先の ino と一致 → `reason=replace_failed` (旧内容のまま)。
     どちらとも違う / stat が失敗 → `reason=replace_unknown`。いずれも errors、非ゼロ終了。
     **サイズと CRC は公開の証拠にしない** — `-f` で新旧が同じ内容のときに未公開を公開と誤るし、
     CRC 不一致は「旧内容のまま」の証明にもならない (往復 2 所見 3)。CRC は内容の検証にだけ使う。
     一時名が残っていれば消す (消せなければ `STALE`)。
9. **同期**: `vfs_sync`。失敗は `reason=sync_failed` (置換は済んでいる可能性がある、と表示)。

各失敗の後始末は**この実行が作った一時ファイルだけ** (`O_EXCL` で作れた = 自分の物) を `sys_unlink`。
unlink も失敗したら `STALE <tmp>` を表示して errors に数える。

新規ファイル (宛先が無い) も同じ手順を通す。途中で落ちても**本名で半端なファイルが見えない**。

**古いカーネル** (`api->version < 53`): 一時ファイル方式は使えない (`O_EXCL` が無視され、ext2 の置き換えも
旧順序)。**既定は変更前に断る** (`reason=kernel_too_old`、1 件も書かずに非ゼロ終了)。
直接上書きが要るなら `--unsafe-overwrite` を明示する — そのときだけ今の直接上書きで進み、
`WARN kernel KAPI v52 < 53: direct overwrite (no H2)` と集計 `direct_overwrite=N` を出す
(Codex 往復 1 所見 5: 「更新の道が無くなる」は成り立たない。カーネルは停止中の NHD 配備経路で入れ替えられる)。
`-f` では解除しない。

### 2-4. `.hs~` は hsync の予約名 — 決裁 D3 (a′)

利用者の決裁は (a) 「訪れた所で自動削除」。Codex 往復 1 所見 2 が出した反例
(利用者が `/usr/bin/.hs~notes` という**自分のファイル**を置いていると消える) は到達可能なので、
**所有の根拠を「作った印」ではなく「予約された名前空間」に置く**形に直す:

- `.hs~` で始まる名前は **hsync の予約**。man ページ (`hsync.1`) と `docs/06_filesystem.md` に
  「この接頭辞のファイルは hsync が作り、消す。利用者は使わない」と明記する。
- 掃除の対象は `.hs~` で始まる**通常ファイル**。`st_nlink` は**見ない**
  (途中で止まった媒体では 2 になり得る。§2-2 の復旧表)。ディレクトリ・特殊ファイルは消さない。
- 掃除するのは `hsync` が**実際に訪れた** (同期対象の) ディレクトリだけ。既定除外の `/sys` や
  保護対象は今までどおり触らない。表示は `CLEAN <path>` (dry-run は `PLAN-CLEAN`)、集計 `cleaned=N`。
- 手順 2 で一時名が既に在ったとき: 上の条件を満たせば消してから 1 回だけ作り直す。
  ディレクトリ等で消せないなら `reason=temp_exists` で失敗。
- コピー元の列挙で `.hs~` で始まる名前は同期対象にしない (予約名を宛先へ運ばない)。
- **掃除の列挙は同期の列挙と枠を分ける** (往復 2 所見 7)。掃除の候補は `.hs~` で始まる名前だけなので、
  同期用の `FileList` (128 件 × `NAME_CAP` 64) とは別の小さな配列に集める。`.hs~` は同期の 128 件の枠を
  消費しない。それでも 1 ディレクトリの一時ファイルが掃除用の枠を越えたら、`truncated` と同じ扱いで
  **「全部は見ていない」と表示する** — 掃除の完了を主張しない。
- 保護対象 (`settings.db` 系の実体) への hardlink が `.hs~` という名前で存在する場合は、
  **保護を予約より優先して消さない** (`reason=protected`)。

(b) 案 (消さずに `STALE` と数える) と (c) 案 (`--clean-temp` のときだけ消す) は退けた。理由は
電源断のたびに手作業が要ること。**予約を文書化することが (a) の前提**なので、文書の変更を §3 の範囲に含める。

## 3. 範囲

| 層 | ファイル | 変更 |
|---|---|---|
| KAPI | `sdk/include/os32/os32_kapi_shared.h`、`sdk/kapi.json` (版数)、`docs/KAPI_SPEC.md` | `KAPI_O_EXCL`、v53 |
| VFS | `fs/vfs.h`、`fs/vfs_fd.c` | `VfsOps.create_excl`、`O_EXCL` の分岐 |
| ext2 | `fs/ext2_dir.c`、`fs/ext2_vfs.c`、`fs/ext2_priv.h`、`fs/ext2_file.c` | 置き換え順序 (§2-2)、公開の 3 値判定、`ext2_ns_touch`、`ext2_unlink` の戻り値の伝播、`create_excl` |
| hsync | `userland/system/hsync.c`、`docs/manpages/hsync.1` | §2-3、§2-4、理由コード追加 |
| 試験 | `tools/tests/hsync_h2_host.c` + `hsync_h2_tdd.md`、`tools/tests/b8_open_host.c`、`tools/tests/vfs_excl_host.c` (名前は任意) | §4 |
| 文書 | `docs/POLICY_DEBUG.md` §4-36、`CLAUDE.md:157`、`docs/06_filesystem.md` (rename と O_EXCL の契約) | 制限の書き換え |

外部リポジトリ (`apps/` `game/`) は `make external` で組み直すだけ ([ABI3])。

## 4. 受入

### 4-1. ホスト試験 (コーダー、RED → GREEN を `*_tdd.md` に)

| ID | 反例 | 期待 |
|---|---|---|
| X1 | `O_CREAT | O_EXCL` で既存ファイル / 既存ディレクトリ / 判定不能 (I/O 失敗) | EXIST / EXIST / その負値。**作らない・切り詰めない** |
| X2 | `O_EXCL` 単独、`create_excl` を持たない FS | INVAL / NOSYS |
| X3 | ext2 のファイル置き換え rename に once / sticky / 二重故障を全位置で。媒体の配置は「同じブロックの前 / 後」「同じセクタ / 別セクタ」「別ブロック」 | 宛先の名前が**全試行で**存在し、旧 inode か新 inode を指す。`media_check` と `e2fsck -fn` は漏れ以外 0 |
| X3b | 対象フィールドが**前半** / **後半**セクタの配置 × 3 つの落ち方: (i) 媒体に届く前に失敗、(ii) **媒体に届いた後**に失敗 (`drivers/ide.c` はデータ転送後にも失敗を返す)、(iii) 書き込み失敗 + 読み直しも失敗 | (i) 未公開 → `D` を解放しない、(ii) 読み直しで `S` を見て**公開済み**として段 3〜6 へ、(iii) 不明 → 書き込み禁止・段 3〜5 を実行しない。**`b8_open_host.c` の注入は今「媒体へ写す前」に失敗を返すので、(ii) の模様を足す必要がある** (往復 3 所見 2) |
| X3c | 段 2 の後に `ext2_resolve_path` を通す経路 (stat / open / unlink) | 古い `D` を返さない (`ext2_ns_touch` 済み) |
| X3d | 掃除の `ext2_unlink` で `links_count` の書き込みだけ失敗 | `EXT2_OK` を返さない。hsync は `cleaned` を成功に数えない (往復 2 所見 5) |
| X4 | 置き換え後の旧 inode (links 1 → 0) の解放、links 2 → 1 | 解放される / 残る。交差リンク無し |
| A14a | 一時ファイルへの write 失敗、空き不足 (FULL)、読戻し CRC 不一致、**公開の前**の rename 失敗 | 旧宛先の内容・サイズ・mtime が不変、errors、非ゼロ。一時ファイルは消せたときだけ消える — **メタデータ失敗で `ROFS` に落ちた後は `STALE`** (往復 3 所見 4) |
| A14a2 | **公開の後**に段 3〜5 が失敗 (rename は失敗を返す) | 宛先が新しい内容になっていることを hsync が読み直して確かめ、`replace_partial` で errors、非ゼロ。旧内容に戻そうとしない |
| A14a3 | 一時ファイルへの `sys_set_mtime` が I/O 失敗 → マウントが書き込み禁止 (`ROFS`) → rename も一時ファイルの unlink も拒否される | `metadata_failed`、**`copied` は 0**、宛先は旧内容のまま、一時ファイルは消せないので `STALE` 表示。次の実行が予約名として片づける (往復 2 所見 6) |
| A14a4 | 段 2 の書き込みが失敗し、**カーネル内の読み直しも失敗** (カーネルは不明と判断) | マウントが書き込み禁止になり、段 3〜5 を実行せず `D` を解放しない。**hsync の報告は手順 8 の ino 照合で決まる** — その後の `sys_stat(dst)` が通れば `replace_partial` か `replace_failed` になり得る (`ROFS` は読み取りを止めない)。`replace_unknown` は stat 自体が失敗したか、ino がどちらとも違うときだけ (往復 3 所見 1) |
| A14a5 | 段 3 が失敗 (段 4 に進まない) → 再マウント → 掃除で一時名を消す | 宛先の内容が**解放されない**。`links_count` は多い側に残る (往復 2 所見 1) |
| A14a6 | `-f` で新旧が同じ内容、rename が公開前に失敗 | `replace_failed` (ino で判定)。`replace_partial` と誤らない (往復 2 所見 3) |
| A14b | 一時名が既に存在 (通常ファイル / ディレクトリ / `st_nlink=2`) | 通常ファイルは消して作り直す、ディレクトリは `temp_exists` で失敗、`st_nlink` では判断しない |
| A14b2 | 利用者が `.hs~notes` という自分のファイルを置いている | **消える** (予約名。man ページに明記されていることを試験の前提として記録する) |
| A14c | 一時ファイルの unlink も失敗 | `STALE` 表示、errors |
| A15 | hsync の各 I/O の直後で「停止」(以降の I/O を全部失敗) → 再マウント相当 → 再実行 | 宛先は旧か新のどちらか完全な内容。§2-2 の復旧表の 3 状態を**個別に**通す: 段 1 直後 (一時名 + links 2) と段 2 直後 (2 名 + links 2) は再実行で収束、段 3 直後 (links だけ 2) は `hardlink` で断り `e2fsck` を案内する |
| A15b | A15 の各状態のあとでコピー元が更に更新された | 同じ判定。宛先を壊さない |
| A15c | `create_excl` が名前を作った直後に `vfs_sync` / FD 発行が失敗 | 一時名だけが残る。次の実行が予約名として片づける |
| A17a | 書き込み中にコピー元のサイズ / mtime が変わる | `source_changed`、置換しない |
| A17b | 判定後に宛先が消える / 型が変わる / サイズか mtime が変わる / 保護対象になる | `dest_changed` (保護は `settings_db`)、置換しない |
| A17c | 宛先の `st_nlink > 1` | `hardlink`、一時ファイルを作らない |
| R1 | 古いカーネル (version 52)、`--unsafe-overwrite` 無し | 1 件も書かずに `kernel_too_old` で非ゼロ終了 |
| R1b | 同、`--unsafe-overwrite` あり + コピー元の read が途中で失敗 | `WARN` 1 行、直接上書き、`direct_overwrite=N`、**旧内容は失われる**ことを表示と文書で明示 |
| R2 | H1 / H3 のホスト試験全部、`test_hsync_protect.py` | 変更なしで通る |
| R3 | 新規ファイル | **公開の前**に失敗したら本名は不存在のまま。**公開の後**に後始末が落ちたら本名は完全な新内容 (半端な内容は現れない) |
| R4 | 1 ディレクトリの `.hs~` が掃除の枠を越える、`.hs~` が 128 件の枠を食わないこと、名前が長くて一時名が `NAME_CAP` に収まらない | 掃除の完了を主張しない、通常ファイルが落ちない、`name_too_long` (往復 2 所見 7) |
| R5 | 保護対象への `.hs~` hardlink | 消さない (`protected`) |

### 4-2. ゲスト受入 (PM、[D1]) — **実施 2026-09-16 (`528c5cb`)**

| # | 項目 | 結果 |
|---|---|---|
| 1 | `make clean` → `make all` → `make external` → `make check` | **通過** (47 目標、84 秒) |
| 2 | NP21/W 停止 → `nhd-pull` → `os32-cycle deploy` → 起動 → `ver` / kselftest | **通過**。`ver` の `API: v53`、`Build: Sep 16 2026 09:32:32`、配備サイズ 480880 B 一致。kselftest は新しい `kernel.map` の番地で **pass=87 / fail=0** |
| 3 | 変更の無い全体同期の所要時間 | **3.82 秒** (H3 の基準 2.9 秒 + 予約名の掃除の走査分)。初回は日時の更新 193 件で 79 秒 |
| 4 | 同サイズ・別内容の差し替え | **通過**。`UPDATE reason=content_changed size=64`、**inode が 35 → 36 に替わった** (= 一時ファイル + 公開の経路を通った証拠)、内容も新しいもの、`nlink=1`、`errors=0` |
| 5 | 予約名の残骸の掃除 | **通過**。ゲストで作った `/usr/bin/.hs~h2probe.txt` を `CLEAN` で消し `cleaned=1`。実行後に `.hs~` は 0 件 |
| 6 | `cp` / `cp -r` / `mv` / `rm` の退行 (`5455843` の修正込み) | **通過**。`rm` はディレクトリを `Is a directory (use rmdir)` で断る |
| — | 空き不足 (`no_space`)、旧カーネルでの `kernel_too_old`、強制終了からの復旧 | **未実施**。空き不足と旧カーネルはホスト試験で通している。強制終了は NHD の退避 ([D2]) が要るので別途 |

### 4-2-0. 当初の手順 (参考)

1. `make clean` → `make all` → `make external` → `make check`。
2. NP21/W 停止 → `os32-cycle deploy` → 起動 → `ver` で API v53、kselftest を新しい `kernel.map` の番地で読む。
3. `make deploy` → `hsync -n` → `hsync`: 変更の無い全体同期の所要時間が H3 (全体 2.9 s、`hsync sys` 0.26 s) から
   目立って悪化していないこと (一時ファイルはコピーが要るものにしか作らない)。ゲストの `time` で測る。
4. `libos32gui.shlib` 級 (約 110KB) を 1 本差し替えて `hsync sys` → `ls -l` のサイズ、`/host` 側との CRC 一致、
   `.hs~` が残っていないこと。
5. 空き不足: 宛先ボリュームの空きより大きいファイルを配備元に置いて `hsync` → `no_space`、**旧宛先のサイズ不変**。
6. (承認が要る、任意) 大きいファイルのコピー中に NP21/W を強制終了 → 起動 → 宛先が旧か新の完全な内容、
   ホストで `e2fsck -fn` (NHD を停止中に取り出す)。**NHD の作業イメージを先にバックアップする ([D2])**。

## 5. 決裁の記録

| ID | 問い | 推奨 | 決裁 |
|---|---|---|---|
| D1 | 排他的作成の口 | (a) `O_EXCL` を KAPI フラグで、v53 | **(a) ユーザー決裁 2026-09-16** |
| D2 | 置換の手段 | (a) ext2 の置き換えを宛先エントリの inode 書き換えに | **(a) ユーザー決裁 2026-09-16** |
| D3 | 残った一時ファイル | (a) 訪れたディレクトリで自分の形のものだけ消す | **(a) ユーザー決裁 2026-09-16**。Codex 往復 1 所見 2 を受け、所有の根拠を「予約名 `.hs~`」に置き換えて (a′) に精密化 (§2-4) |

## 5-1. 実装レビュー (Fable 5.1 サブエージェント、`528c5cb` 対象、2026-09-16) — **Approve**

Codex と Antigravity (`agy`) が同日にクォータ切れになったため、ROLES §5 の次点で実施。
**blocker 0 件** (到達可能な破壊・誤報告の反例は見つからなかった)。非 blocker 8 件の扱い:

| # | 所見 | 扱い |
|---|---|---|
| 1 | 置換の後に `vfs_sync` が落ちると `note_target` が呼ばれず、「再起動が必要」の案内が消える (置換は媒体に載っているのに) | **直す** |
| 2 | コピー元の `.hs~*` を黙って落としている (`excluded` にも `-v` にも出ない) | **直す** |
| 3 | 名前が長くて掃除の一覧に入らない予約名を「枠を越えた」と表示する | **直す** (語だけ) |
| 4 | mtime の失敗 1 件が `errors` 2 と数えられる | **直す** |
| 5 | `HS_TEMP_PREFIX_LEN` と `".hs~"` の二重定義 ([C4]) | **直す** |
| 6 | FILETYPE 機能の無い ext2 では新経路に入らず旧経路に落ちる (OS32 の `ext2_fmt.c` は FILETYPE を立てるので手元では起きない) | **文書に 1 行** (`06_filesystem.md`) |
| 7 | 新経路は `add_entry` / `delete_entry` を通らないので**宛先ディレクトリの mtime が更新されない** (`ls -l` の日時だけ) | **直さない**。後続票で扱う (下の「残る制限」) |
| 8 | v53 以上では `--unsafe-overwrite` を無視するので、`create_excl` を持たない FS (FAT ルート等) では `replace_unsupported` で書けない | **直さない** (票 §2-3 手順 2 で承認済みの挙動) |

PM 判断の 4 点 ((a) `app.conf` の要求版 52、(b) 保護化を errors に、(c) dry-run でも版の門、
(d) 未公開でも `ext2_fs_error`) はすべて**妥当**と確認された。

確認された範囲: 段の依存 (段 4 は段 3 の確定の内側、段 5 は公開の確定だけが条件、「名前 2・links 1」に
至る経路は無い)、公開の 3 値判定 (対象 4 バイトは 4 バイト境界で 1 セクタに収まる。OS 側にブロック
キャッシュは無く「読み直しが古い値を返す」経路は無い)、`ext2_fs_error` の位置、共有バッファの寿命
(保持したまま別操作を呼ぶ箇所は無い)、`ext2_ns_touch` の位置、`ext2_unlink` の戻り値伝播と 5 つの呼び手、
`vfs_open_probe` の 1 対 1 対応、`O_EXCL` の分岐位置、hsync の手順 5 / 8 / 掃除 / 版の門、[C1]〜[C4] と [ABI2]。
未確認: ビルドと試験の実行、試験本文の妥当性、装置内部のキャッシュ、`drivers/ide.c` の転送後失敗の実装。

### 残る制限 (H2 の後に扱う)

- **新しい置き換え経路では宛先ディレクトリの mtime が更新されない**。エントリの inode を書き換えるだけで
  `ext2_add_entry` を通らないため。`ls -l` のディレクトリ日時にしか出ないので H2 では直さない。

## 6. 往復記録

### 往復 1 — 設計レビュー (Codex、`34cfc3f` 対象、2026-09-16) — Request changes

| # | 重大度 | 所見 | PM の確認 | 対応 |
|---|---|---|---|---|
| 1 | blocker | 段 2 の後に失敗すると宛先は**新内容**になる。「失敗したら旧内容」は成り立たない。1KB を 512B ずつ書くので「1 セクタ内のフィールド」も未変更を保証しない | 到達可能。`ext2_write_block` の実装と一致 | §0 の保証表を**公開の前 / 後**に分割。段 2 を公開境界と定義し、公開後は段 3〜5 を最後まで試みる。hsync は rename 失敗時に宛先を読み直して `replace_partial` / `replace_failed` / `replace_unknown` を分ける (§2-2、§2-3 手順 8、A14a2、X3b) |
| 2 | blocker | `.hs~` + `nlink==1` では所有を証明できない。利用者の `.hs~notes` が消える | 到達可能 | 所有の根拠を**予約名**に変更 (§2-4 (a′))。man ページと `06_filesystem.md` に予約を明記し、`st_nlink` は見ない。受入 A14b2 を追加 |
| 3 | blocker | 段 1〜3 の途中で止まると `links_count` が名前の数より多く残り、再実行で収束しない (掃除条件・hardlink 検査に当たる) | 到達可能。3 状態とも再現する | §2-2 に復旧表を追加。一時名は `st_nlink` を見ずに消す (段 1・2 の状態は収束)。段 3 の状態は `hardlink` で断り `e2fsck` を案内する (B8 §2-6 の「漏れ」と同じ扱い)。A15 を 3 状態に分割 |
| 4 | blocker | 一時ファイルへの mtime 失敗は **ROFS** を招き rename が通らない。`copied` に数えられない | 到達可能 (`fs/ext2_super.c` のエラー状態) | §2-3 手順 7 を「公開前の失敗として中断、`copied` に数えない」に変更。A14a3 を追加 |
| 5 | 非blocker | 旧カーネルでの直接上書きは旧内容を失う。「更新の道が無くなる」は成り立たない (停止中の NHD 配備がある) | 妥当 | 既定を**拒否** (`kernel_too_old`) に変更し、`--unsafe-overwrite` を明示したときだけ直接上書き。R1 / R1b |

観点 1〜5 すべて「見た」。媒体の故障注入と実機試験は未確認 (実装段階の受入で行う)。

### 往復 2 — 設計レビュー (Codex、`7f7d889` 対象、2026-09-16) — Request changes

| # | 重大度 | 所見 | PM の確認 | 対応 |
|---|---|---|---|---|
| 1 | blocker | 段 3 が失敗したのに段 4 を実行すると「名前 2・links 1」になり、次の掃除が一時名を消した瞬間に**生きている新内容が解放される** | 到達可能。`fs/ext2_file.c:480` の `ext2_unlink` が実際に減算と解放を行うことを確認 | 段 4 を**段 3 の確定に縛る** (§2-2 の表)。減らせないときは多い側に残す。A14a5 |
| 2 | blocker | フィールドがブロック後半にあると、前半だけ書けた状態を「公開済み」と誤り、生きている `D` を解放し得る | 到達可能。1KB を 512B ずつ書く経路 | 段 2 を**フィールドを含む 1 セクタだけ**の書き込みにし、公開を**公開済み / 未公開 / 不明**の 3 値で判定 (§2-2-1)。不明は書き込み禁止にして段 3〜5 を実行しない。X3b |
| 3 | blocker | サイズと CRC は公開の証拠にならない (`-f` で新旧同内容だと未公開を公開と誤る) | 到達可能 | 判定を **`st_ino` の照合**に変更 (§2-3 手順 8)。CRC は内容検証だけに使う。A14a6 |
| 4 | blocker | 段 2 は `delete_entry` / `add_entry` を通らないので名前解決メモが無効化されず、古い `D` を読む | 到達可能。`ext2_resolve_path` と `ext2_ns_touch` を確認 | 段 2 を**試みる前に** `ext2_ns_touch` (§2-2-2)。X3c |
| 5 | blocker | `ext2_unlink` が `ext2_write_inode` と `ext2_sync` の戻り値を捨てるので、掃除の links 修復失敗が成功に見える | 到達可能。`fs/ext2_file.c:507` で確認 | 両方の戻り値を伝播 (§2-2-3)。`fs/ext2_file.c` を範囲に追加。X3d |
| 6 | 非blocker | 復旧表に段 4 / 5 の状態が無い。A14a3 は ROFS 下で一時ファイルを消せない。R3 の書き方が公開境界と矛盾 | 妥当 | 復旧表を 5 状態に (§2-2-4)。A14a3 を `STALE` に、R3 を公開の前 / 後で書き分け |
| 7 | 非blocker | 掃除と既存の列挙制限 (128 件、`NAME_CAP` 64) が干渉する。長い名前の一時名は保持できない | 妥当 | 掃除の枠を同期の枠と分け、`.hs~` は 128 件を消費しない。`strlen(name)+4 >= NAME_CAP` は `name_too_long`。保護対象への `.hs~` hardlink は消さない (§2-3 手順 1、§2-4)。R4 / R5 |

観点はすべて「見た」。ext2 の共有バッファを保持したまま別操作を呼ぶ実装、新しい掃除ループ、
公開処理の実コードは**まだ存在しない**ので未確認 (実装レビューで見る)。

### 往復 3 — 設計レビュー (Codex、`60d7f13` 対象、2026-09-16) — **Approve**

往復 2 の blocker 5 件は「設計上閉じた」と確認された。新しいデータ破壊の反例は**無し**。
残る 5 件はすべて非 blocker (契約と受入の不整合) で、そのまま反映した:

| # | 所見 | 対応 |
|---|---|---|
| 1 | `A14a4` の `replace_unknown` 固定が手順 8 と矛盾する (`ROFS` は読み取りを止めないので stat は通る) | A14a4 を「報告は ino 照合で決まる。`replace_unknown` は stat 失敗かどちらとも違うときだけ」に |
| 2 | X3b が旧設計 (2 セクタ書く) の条件のまま。`b8_open_host.c` の注入は媒体へ写す**前**に失敗するが、`drivers/ide.c` は転送後にも失敗を返す | X3b を「前半 / 後半 × (届く前 / 届いた後 / 読み直しも失敗)」に。注入側に「届いた後」の模様を足す |
| 3 | 復旧表に「段 3 失敗・段 4 未実行・段 5 成功」が無い | 行を追加 (2 名・links 2、`D` 解放済み → 掃除で収束) |
| 4 | 公開前の失敗で「一時ファイル無し」は保証できない (段 1 の失敗で `ROFS` に落ちると unlink も通らない) | §0 と A14a を `STALE` 込みに |
| 5 | 段 6 (`ext2_sync`) の失敗も rename の戻り値に含めるべき | §2-2-1 の戻り値の定義に明記 |

確認範囲: 段の依存、3 値判定とキャッシュ、ino の再利用、名前解決メモ、`ext2_unlink` の呼び手
(VFS / rm / install / SQLite)、復旧表、名前長と掃除枠、新規作成、`O_EXCL` 非対応 FS、旧カーネル —
すべて「見た」。装置とエミュレータ内部のキャッシュ、実コード、故障注入の実行は未確認。

**この票は設計凍結。実装へ進む。**
