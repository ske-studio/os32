# Phase 5: 検証とドキュメント更新

## 概要

Phase 1-4 の変更を統合テストし、プロジェクトドキュメントを総合的に更新する。

## 検証チェックリスト

### ビルド検証

- [ ] `make clean && make all` がエラーなく完了
- [ ] `i386-elf-nm kernel.elf | grep __bss_end` が 0x40000 未満
- [ ] `i386-elf-objdump -h programs/shell.elf` の _end が 0x500000 未満
- [ ] `i386-elf-objdump -h programs/demo1.elf` の _end が 0x500000 未満

### 起動テスト (NP21/W)

- [ ] カーネルが正常起動する (起動メッセージ表示完了)
- [ ] Shell が正常起動する
- [ ] `mem` コマンドで新しいヒープサイズ (316KB) が表示される
- [ ] `heap` コマンドで exec_heap の統計が表示される
- [ ] 日本語文字の表示が正常 (フォントキャッシュ再配置検証)

### プログラム実行テスト

- [ ] `gfx_demo` が正常に動作する (スプライト移動)
- [ ] `demo1` が Page Fault なしで動作する (★最重要)
- [ ] `spr_test` が正常に動作する
- [ ] `vz` エディタが正常に起動する
- [ ] `bench` ベンチマークが正常に動作する

### ガードページ検証

- [ ] GUARD A テスト: sbrkを意図的に上限まで使い、超過時に Page Fault が発生することを確認
- [ ] GUARD B テスト: 深い再帰呼び出しでスタックオーバーフロー時に Page Fault が発生することを確認
- [ ] 上記のフォルト発生後、exec_fault_recover() による正常復帰が機能すること

### 回帰テスト

- [ ] shell からの全コマンド実行 (ls, cat, cd, mkdir, rm, cp, mem, heap 等)
- [ ] serialfs 経由のファイル転送 (kmalloc が正常動作すること)
- [ ] HDD/FDD 両方のブートモードで起動確認

## ドキュメント更新対象

### 1. `docs/OS32_SPEC.md` — メモリマップセクション

メモリマップの章 (第2部) を新レイアウトに合わせて全面更新:
- コンベンショナルメモリのヒープ拡張
- 拡張メモリ配置の変更
- プログラム空間の動的レイアウト説明

### 2. `docs/KAPI_SPEC.md` — KAPI_ADDR 変更

KernelAPIテーブルの配置アドレスを 0x3F0000 → 0x189000 に更新。

### 3. `docs/DEVELOPMENT.md` — メモリマップ再構築の経緯

変更の理由と経緯をデベロップメントログに追記:
- 旧レイアウトの問題点サマリ
- 新レイアウトの設計根拠
- sbrk_heap_limit バグの詳細

### 4. `include/memmap.h` — ファイルヘッダコメント

memmap.h のファイルヘッダに新メモリマップの図を追加し、
定数の一覧と各レイヤーの関係を明確にする。

### 5. `pagefault_investigation.md` — クローズ

調査メモを更新し、原因と対策を記録:
- 根本原因: sbrk_heap_limit のクリッピング不整合
- 対策: Phase 4 で修正済み
- ステータス: Closed

### 6. `.agents/GEMINI.md` — メモリマップ情報更新

GEMINI.md 内のメモリマップ関連記述を新レイアウトに更新。

## コミット方針

```
docs: Phase 5 — メモリマップ再構築のドキュメント総合更新
```

## 依存関係

- Phase 1-4 が全て完了し、エミュレータでの動作確認が済んでいること
