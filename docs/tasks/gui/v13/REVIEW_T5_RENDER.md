# T5R — 初回レビューと差し戻し

> 発行: PM (2026-09-09) / 状態: **完了記録 (2026-09-10)**

再実装後のT5Rホストアダプタを受入済み。初回の手順未達は下記のとおり保持する。
親: [TASK_T5_RENDER.md](TASK_T5_RENDER.md)。
初回独立レビュー: `/tmp/os32-v13-t5-render-review.txt`。

PM再実行でhost21試験/check/fmt成功。独立レビューは機能上の反例未検出。
ただし13試験のRED/GREENに対し追加8試験は初回GREENであり、部分clip/halo、保存行部分不足、
i32受理側境界、glyph引数受渡し等には事前REDがないと指摘された。
単なる回帰追加として全手順適合と扱わない。

ユーザーは明示的に「手順逸脱を認めず、対象部分をテスト先行で再実装する」を選択。
Codexへ対象実装の除去・最小stubからのテスト先行再構築を発注。
既存の適合テストは維持可能。今回のREDを過去の履行証跡としない。
単発mutationと復元だけでは再実装として受け入れない。
対象範囲、維持部分、実コマンド・失敗・成功をクレート内REWORK_LOG.mdへ記録させる。

## 機能契約のPM判断

幅不一致の事前検査は、可視行と読出haloのセルに限定する方針を採用。
画面外の幅不一致検査は描画の責任に含めず、空交差は無出力成功とする。
これはモデル全体の幅検証APIではない。巨大モデル全体の走査を要求しない。
座標検査は本文全体がi32へ収まる保守契約を維持し、小clipでの救済は保証しない。

## 再実装の受入

独立再レビュー `/tmp/os32-v13-t5-render-rework-review.txt` は今回の手順・機能を受入可と判定。
部分clip/halo、保存行部分不足、i32受理側境界、glyph引数の各経路について、
除去・stub・RED・段階的再実装のログと最終コードの対応を確認した。
これは証跡の内部整合性確認であり、過去の実行履歴の真正性を証明したという意味ではない。

PM再実行: 39試験（unit7、既存21、rework11）、host check、fmt、差分チェック成功。
build/sdk.mkにcheck-term-renderを追加し、登録後make check全体もexit0。
ログ: `/tmp/os32-t5r-make-check.log`。
REWORK_LOGのFINALにある本体・unit試験・既存試験・rework試験の4ファイルSHA256は現物と一致。

非阻害の残事項:
- retained_rows.start==0と全角ペア整合はT4の現行不変条件に依存。
- 到達不能/巨大容量を必要とする防御分岐の実行試験は未実施。
- run併合のassertion単独REDは未観測。今回のdraw再実装は画素assertionでREDを確認。
- T4は未追跡のため、git diffだけでは委託前後の無変更を証明できない。専用の事前ハッシュは未採取。
- 人工glyphのoracleであり、実ROM、ゲスト最終リンク・表示・性能・配備は未検証。

ユーザーが要求した対象のテスト先行再実装として受け入れ、初回TDD逸脱の免除とは扱わない。
