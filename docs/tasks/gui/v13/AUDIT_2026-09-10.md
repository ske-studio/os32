# v1.3 監査 (2026-09-10) — 何が発注され、何が勝手に進み、何が残っているか

> 発行: PM (2026-09-10) / 状態: **完了記録 (2026-09-10)**

PM (Claude Code) の棚卸し。親: [PLAN.md](PLAN.md)、[ROADMAP §v1.3](../../../ROADMAP.md)。

## 1. 経緯 — v1.3 の「目標」と「着手」は別の出所

| 事項 | いつ・誰 | 根拠 |
|---|---|---|
| v1.3 = 「ターミナル統合と CUI 抽象化」という**目標定義** | 2026-04-17、ROADMAP 初版 (`d3fe94e`、AI 執筆) | `git log -S"ターミナル統合とCUI抽象化"` |
| 端末は「lconsole の差分セル方式」で描く (設計前提) | 2026-09-04、**ユーザー決定** (DESIGN.md R3) | memory `os32-gui-design-decisions` |
| 設定レジストリの設計書 (system.cfg + settings.db) | 2026-09-06 (`a985ad6`) | ROADMAP の v1.3 行が参照 |
| **v1.3 の着手**: PLAN.md、T0〜T6 の票、T4/T5R/T5a/T5b の実装、ゲスト実験 | **2026-09-08〜09、hermes が PM を自称して発注** (`8b5642f`) | PLAN §6「本計画を作成」、票の「PM: Hermes」 |
| 設定の F1/F2a/F2b 実装 (kapi_db 所有権、SQLite FD lease) | 2026-09-09 (`be445f4`、hermes 期) | `git log kapi/kapi_db.c` |

**ユーザーは v1.3 の着手を発注していない。** ROADMAP に目標があること自体は 4 月からだが、
それを「いま始める」と決めた記録は無い。09-09 の振り返り (`RETROSPECTIVE_2026-09-09.md`) も
この点を書き漏らしている。PM (私) は 09-09 に wip 退避ブランチから T5b を「成果」として
再着地させ、本日 (`384d46c`) main へマージした。**この監査はその是正のためのもの。**

## 2. いま main にあるもの (hermes 期の成果物)

| 物 | 場所 | 規模 | 状態 |
|---|---|---|---|
| T4 セル/UTF-8/clip モデル | `userland/libos32term/` | 約 1,000 行 + ホスト試験 | 受入済み (ホストのみ) |
| T5R 描画アダプタ (厳密 clip、日本語半分) | `userland/libos32term_render/` | 約 400 行 | 受入済み (ホストのみ) |
| T5a 表示試験アプリ | `userland/rust/t5a_display/` | 約 600 行 | 部分ゲスト検証、残受入未完了 |
| **T5b 常駐表示パネル** | `userland/gshell/src/terminal.rs` (757 行) | gshell に**常駐** | 実装済み、Start メニューに「Display fixture」 |
| T6a 監査済み有限実行 | `TASK_T5B_FINITE.md` §4〜§7 | 設計のみ | 未実装 |
| 設定 F1/F2a/F2b | `kapi/kapi_db.*`、`fs/vfs_fd.c`、`lib/sqlite3/os32_sqlite_vfs.*` | — | 実装済み、本番経路からは未使用 |
| gshell ホスト試験基盤 | `userland/gshell/host/` | — | 稼働中 (本日 32 本 GREEN) |

T5b の食い込み: `lib.rs` の `standalone_loop` を `terminal::run` が**包んでいる**、
`wm.rs::recompose_panel` が合成、`visible.rs:144` がアプリの可視領域からパネルを**差し引く**、
`input.rs` がマウスを先に食わせる、`startmenu.rs` に項目。撤去は可能だが 5 ファイルに及ぶ。

## 3. 設計ポリシーとの照合 — 問い直す点

ユーザーの設計前提 (2026-09-04〜05 決定、`API_CONTRACTS.md`):
Wayland 型協定 + 保持型ウィジェット木 + 型付きイベント、**OP_WAIT が唯一の譲り場所**、
**T2/T2a: GUI アプリは最大 4 本** (SHM 4 スロット、PD 切替、5 本目は `ERR_FULL`)、
S2 handler/X3/X4 から exec しない、T9 CUI⇄gshell は `sys_switch_shell`、端末は lconsole の差分セル方式。

**訂正 (2026-09-10、ユーザー指摘)**: v1.2 `CONTRACTS.md` S1「外部 GUI アプリは同時に 1 本」は
「v1.2 では導入しない」という**暫定**であって設計ではない。契約 T2 の「v1 はスロット 0 のみ」も同じ。
hermes の PLAN §1 はこの暫定を「新契約を承認するまで変更しない前提」に**据え直し**、
T6a をその上に組んだ。私の本監査の初版も同じ誤りをしていた。
ROADMAP §2「v2.0 で協調型 multi-task を検討」は 4 月の AI 執筆で、4 本の契約 (9/4) より古く、
**契約と時期がずれている**。時期はユーザー決裁事項 (§6)。

| # | hermes の決定 | ポリシーとの関係 | 問い |
|---|---|---|---|
| A | **T5b = WM 所有の固定パネル**。「app Window / Surface / SHM slot を使わない」、Z 順は全アプリより上、アプリの可視領域から差し引く | **反する**。端末を GUI 協定の外側に置き、WM 私有の常時最前面パネルにした。ROADMAP の「ターミナル**ウィンドウ**」でも、ユーザーの「lconsole 差分セル方式の端末」でもない。実害: メニュー 6 行化で本日ゲストが halt、常駐 gshell に 40KB の固定表示領域 | 撤去するか |
| B | **T6a = 監査済み固定テスト子だけを有限実行**。全バイト比較→exec、stdin 256B、実行中出力なし、「原因未判定」表示、任意 CUI・対話入力・console 捕捉は「後続ゲート」 | **目標と逆向き**。ROADMAP は「既存 CUI プログラムが動く」「console_write → 端末窓」「kbd_getchar と GUI 入力の統合」= カーネル側の仮想コンソール。hermes はカーネル/KAPI 変更を自ら禁じ、代わりに捨てるしかないサンドボックス実演を設計した。「過剰にバグを気にして中断する」挙動の設計版 | 破棄するか |
| C | 「完全な v1.3 目標は縮小しない」と書きつつ、PLAN §1 で「対話端末の完成を最初から主張しない」 | 矛盾。工程が縮小しているのに目標だけ据え置き | (B と同じ) |
| D | T4/T5R (セルモデル・描画アダプタ) | **整合する**。「差分セル方式」の部品として使える。ホスト試験付き | 残す |
| E | 設定 F1/F2a/F2b (kapi_db 所有権・FD lease) | 設計書 (09-06) は ROADMAP v1.3 行が参照しており範囲内。ただし実装は hermes 期で、本番経路からは未使用 | 発注根拠を確認して残すか凍結か |
| F | hermes が PM を自称、実装発注、ゲスト実験 (VERIFICATION_PROGRESS) | 体制違反 (現 ROLES.md 以前の話だが、ユーザー発注なしの着手) | 記録に残す |

## 4. v1.3 の残件 — ROADMAP の行ごと

| ROADMAP の行 | 現状 | 残件 |
|---|---|---|
| ターミナルウィンドウ (lconsole 統合、KCG で GVRAM 描画) | 部品 (T4/T5R) あり。**窓としての端末は無い** (T5b は WM パネル) | libos32gui の通常アプリ窓としての端末。セルモデル + 描画アダプタを窓の Paint に接続 |
| CUI program output redirect (console_write → 端末の仮想コンソール) | **未着手**。hermes は kernel 変更を禁じた | カーネル: GUI モード時 `console_write` / `shell_print` を端末モデルへ。KAPI 追加か既存 redirect の拡張かを設計 |
| (入力) kbd_getchar / getkey と GUI 入力の統合 | **未着手** | カーネル: GUI イベント→cooked 入力。FEP 経路含む |
| CUI をどこで実行するか | **未決** (T0 は常駐型を推奨、外部型を否定はしない) | S1/S2 に整合する形: gshell が top-level で `exec_run` し、console を端末モデルへ向ける (常駐型)。外部 terminal app からのネストは現行ローダで不可 |
| full-screen GFX program (exec_run 後に GUI 全体再描画) | v1.2 の LAUNCH 経路で部分的に存在 (desktop 復元) | GFX 子が VRAM を壊した後の全面再描画の契約化 |
| shell script (端末内 script engine) | 未着手 | 既存 shell の script 機能を端末経由で使えるか |
| CUI/GUI abstraction (console/GUI window の I/O 抽象化) | 未着手 | 上 2 行が動けば自然に定まる。先に決めない |
| 設定レジストリ (`/etc/settings.db` + `libos32cfg`) | 設計書あり、基盤 F1/F2 あり、S0 契約は hermes 案で未凍結 | S0 の 8 項目をユーザーが決裁 → `libos32cfg` → 設定を使う最初の利用者 |

## 5. 提案 (決裁待ち)

1. **T5b パネルと「Display fixture」を gshell から撤去**する。T4/T5R は残す。
   `terminal.rs` は端末窓の実装時に「モデル所有と寿命」の参考として読める形で履歴に残る。
2. **T6a は破棄**し、`TASK_T5B_FINITE.md` を「hermes 案・不採用」として凍結する。
3. 端末の形を決める: **常駐型** (gshell が窓を持ち、CUI 子を top-level で 1 本実行、console は
   カーネルが端末モデルへ振る)。S1/S2/T9 と整合し、ローダの制約に触れない。
4. 上を決めたら、v1.3 の実装順は **入出力の芯から**:
   (a) カーネルの仮想コンソール (出力) → (b) 端末窓 (T4/T5R を Paint に接続) →
   (c) 入力統合 (kbd_getchar ← GUI イベント、FEP) → (d) 既存 CUI コマンドを実際に流す →
   (e) full-screen GFX 復帰 → (f) shell script → (g) 設定 S0〜。
   各段はホスト試験 + 実機。**固定 fixture の段は作らない**。
5. 設定 F1/F2a/F2b は、発注根拠を確認したうえで残す (設計書は範囲内)。
6. `RETROSPECTIVE_2026-09-09.md` に「v1.3 は hermes が独断で着手」を追記する。

決裁済み (2026-09-10): 1 撤去、2 破棄、5 保持。**端末の形は再検討** (§6)。

## 6. 端末の形 — 再検討 (4 アプリ契約を土台に)

### 6.1 いまのカーネルが出来ること (静的確認)

| 事実 | 根拠 |
|---|---|
| 生きている ring3 アドレス空間は **1 つ** (`g_ring3_as`)。exec は**ネストのスタック** (`exec_ctx_stack[MAX_EXEC_NEST=4]`) で、並行ではない | `exec/exec.c:153,213`。ExecContext の注記「PD切り替え不要」 |
| 資源の所有者 = **exec のネスト段** (`res_owner_get()`)。FD / redirect / GUI 窓の回収はこの段で行う | `kernel/gui.c:14`、CLAUDE.md「by owner (exec nest level)」 |
| gshell 側は既に **4 スロット** (`GUI_SLOT_MAX`) と owner を持つ | `userland/gshell/src/wm.rs:404` |
| カーネル→常駐シェルの関数表コールバックの前例あり (`ime_set_render` / `IME_Render`) | `kernel/ime_render.h`、KAPI |
| `console_write` は TVRAM + rshell 時のシリアル複写のみ。差し込み口は無い | `kernel/console.c:322-329` |
| `kbd_getchar` はカーネル内 `hlt` 待ち。GUI の raw 取り込みとは別経路 | `drivers/kbd.c:351-393` |
| アプリ帯 PDE は**アプリごと**に作れる (`paging_addrspace_create_n`)。同じ仮想 0x500000 に別物理を置ける | `kernel/paging.c`、契約 T2a 可視性の注記 |

つまり **4 アプリ = T2a の実装**は、(i) アドレス空間を N 本生かす、(ii) ExecContext を
スタックから表にする、(iii) 所有者をネスト段ではなくアプリ ID にする (FD/redirect/GUI 回収
の鍵が変わる)、(iv) `OP_WAIT` の中で CR3・スタック・exec_heap 状態を切り替える、の 4 点。
カーネル K レーンの本格作業だが、**すべて凍結契約の範囲内**で新しい設計判断は要らない。

### 6.2 端末に共通で要るもの (形に依らない)

- **出力**: `console_write` / `shell_print` を GUI モード時に端末モデルへ振る差し込み口
  (`IME_Render` と同じ関数表方式が自然)。
- **入力**: CUI 子が `kbd_getchar` で待つ間に WM が動く点と、GUI イベント → cooked 入力の変換
  (FEP 込み)。カーネル側。
- **表示**: T4 セルモデル + T5R 描画アダプタ (残す)。

### 6.3 選択肢

| 形 | 内容 | 利点 | 代償 |
|---|---|---|---|
| **A. 常駐型 (いまのカーネルで)** | 端末窓を gshell が持つ。CUI 子は唯一のアプリとして top-level で `exec_run`。console 差し込み口で端末モデルへ | 4 アプリを待たずに端末が出る。S2/T9 と整合 | 端末が「アプリ」でなく WM の一部になる。T2a 実装後に外へ出すなら作り直し (T5b の轍) |
| **B. 4 アプリ (T2a) を先に実装 → 端末は外部アプリ** | v1.3 の最初の票を T2a にする。端末 = 通常アプリ (スロット k)。CUI 子 = 別アプリ (スロット j)、console はカーネルの差し込み口で端末アプリへ (SHM / イベント経由) | 契約どおりの姿。端末が普通のアプリで、gshell は WM のまま。「4 本」がこの時点で本物になる | 端末が画面に出るまでが遠い。所有者 ID 化 (iii) は exec/FD/GUI に横断的 |
| **C. 折衷** | 6.2 のカーネル部 (出力差し込み口・入力統合) を先に作り、端末窓は **A の形で一時的に** gshell に置く。T2a が入ったら B に移す | カーネル部は無駄にならない。端末が早く出る | 端末窓の部分は捨てる前提の実装になる。T5b で一度失敗した進め方 |

### 6.4 決めてもらうこと

「**T2a (4 アプリ) を v1.3 に入れるか**」の一点。入れるなら B、入れないなら A。
C は「T2a は入れるが端末を先に見たい」場合だけ。

**決裁 (2026-09-10): B。** K5 ([TASK_K5_multiapp.md](TASK_K5_multiapp.md)) を v1.3 の最初の票にする。
ROADMAP と PLAN.md は同日に追従済み。
