# TASK_EDIT_GUI — テキストエディタの GUI 版 (v1.4 の最後の受入試験)

> 発行: PM (Claude Code `claude-opus-5`、2026-09-17) / 状態: **一部受入 (2026-09-18)** — E1 / E2 / E3 / E6 / E8 / E9 / E10 合格 (穴 H13 §9・H16 §10 を修正)。E4 / E5 は**環境**が整わず不可 (§9-5)、E7 のみ未実施

基点: `feat/gui` = `c342c36`。
正典: [`../../ROADMAP.md`](../../ROADMAP.md) §1 の v1.4 表、
[`DESIGN.md`](DESIGN.md)、[`API_CONTRACTS.md`](API_CONTRACTS.md)。

## 0. これは何のための票か

**アプリを 1 本増やすことが目的ではない。**
ROADMAP は「**API の退行検出を兼ねる**」と書いている。
`libos32gui` / 設定レジストリ / Host Services を**通しで使う受入試験**として、
v1.4 に 1 本だけ残されたもの。

**これが通れば v1.4 の API に穴が無いことの証明になる。** 逆に言えば、
**穴を見つけるのが仕事**であって、機能の豊かさを競う票ではない。

今日 (2026-09-17) の 3 段のランナーが「機械に読ませたら露見する穴」を 3 件出した。
**この票は「人が使う経路を通したら露見する穴」を拾う。** 角度が違う。

## 1. 確認済みの事実 (2026-09-17)

| 事実 | 影響 |
|---|---|
| GUI の正典は **`userland/rust/libos32gui`** (Rust、retained widget tree)。`userland/lib/ui/` の microui 系は別物で `apps/ui_demo` しか使っていない | **GUI 版は Rust で書く。** 参照実装は `userland/rust/filer` (1,789 行) |
| **既存の `textbox` は 1 行しか扱えない** (`widget.rs:190`)。改行の扱いが無い | **複数行の部品が要る。これが票の中心** (§2) |
| `listbox` / `label` / `button` / `input_open` (モーダル入力) はある | 開く・保存する・検索の UI はこれで組める |
| CUI 版 `apps/edit/` は **C で 3,248 行** (`core.c` / `command.c` / `text_search.c` / `text_undo.c` ほか) | §3 |
| Host Services の口は `libos32gui/src/hostsvc.rs` (15.8KB) にある | 印刷とクリップボードに使える |
| 設定は `libos32gui/src/cfgro.rs` (18.9KB) | 設定の読み出しに使える |

## 2. 中心 — 複数行の編集部品

**既存の `textbox` は 1 行用。** エディタには複数行が要る。ここが**この票で一番
API の穴が出る場所**なので、正面から扱う。

### 決裁 A1 — **新しい種類を末尾に追記する** (ユーザー、2026-09-17)

[API_CONTRACTS.md](API_CONTRACTS.md) は **「op 番号・イベント種別・構造体は末尾追記のみ。
以後の変更にはユーザ承認を要する」**と凍結している。

- **既存の `WK_TEXTBOX` は 1 行のまま。触らない。** 既存の利用者 (filer ほか) に影響が出ない。
- 複数行は **新しい種類を末尾に追記**する (`WK_TEXTAREA` 等)。**契約の「末尾追記のみ」に収まる。**
- 欠点は似た部品が 2 つ並び、描画と入力の処理が一部重複すること。**受け入れる** —
  凍結契約の変更を避けるほうが重い。

**重複は共通の下請けに寄せてよい**が、**`WK_TEXTBOX` の見え方と振る舞いを変えないこと。**
変わっていないことを試験で押さえる (受入 E10)。

残りの決めること:
- **どこまでを部品が持つか。** 折り返し / 縦横のスクロール / 選択範囲 / 桁と行の表示。
  **全部を部品に持たせない** — エディタ側が持つべきものを切り分ける。
- **1 画面に入らない大きさをどう扱うか。** 部品が本文を丸ごと持つのか、
  エディタが持って見える範囲だけ渡すのか。**後者でないと大きなファイルで破綻する。**

## 3. 本文の扱い — CUI 版を再利用しない (PM 判断)

CUI 版の中身 (行の管理・取り消し・検索) は **C で 3,248 行**ある。
Rust から呼ぶことは技術的には可能だが、**この票ではやらない。**

理由: **目的が API の穴を見つけることだから。** 言語をまたぐ呼び出しを挟むと、
不具合が出たときに「GUI の API が悪いのか、橋渡しが悪いのか」が分からなくなる。
**変数を増やさない。**

代わりに **GUI 版は意図して小さく作る**。行の配列と、挿入・削除・改行だけ。
取り消しも検索も**この票では要らない** (§6)。

**CUI 版はそのまま残る。** 2 つのエディタが併存するが、**役割が違う**ので問題ない。

## 4. 通しで使うもの (これが受入の本体)

| 使うもの | 何をするか | 何を確かめるか |
|---|---|---|
| **libos32gui** | 窓・複数行部品・メニュー・モーダル | §2。**穴が出るならここ** |
| **設定レジストリ** | 直前に開いた場所などを読む | `cfgro` の読み出しが実アプリで通るか |
| **Host Services** | **印刷** (`lpr` 相当) と**クリップボード** | `hostsvc` が実アプリで通るか |
| ファイル | 開く・保存する | VFS が GUI から通るか |

**3 つとも通すこと。** 1 つでも欠けると「通しの受入試験」にならない。

## 5. 受入

| ID | 反例・操作 | 期待 |
|---|---|---|
| E1 | gshell から起動して、開く → 打つ → 保存する | 保存した内容が CUI の `cat` で一致する |
| E2 | 1 画面に入らないファイルを開く | 縦にスクロールできる。**表示だけで本文が壊れない** |
| E3 | 日本語を打って保存する | 3 バイト 2 桁の扱いが崩れない ([`POLICY_DEBUG.md`](../../POLICY_DEBUG.md) §4-27)。**桁の数え方と折り返しの位置** |
| E4 | 選択してクリップボードへ、別の場所へ貼る | Host Services 経由で往復する |
| E5 | 印刷する | Host Services 経由でホストに出る |
| E6 | 保存せずに閉じようとする | **確かめてくる**。黙って捨てない |
| E7 | 書き込めない場所へ保存する | **理由が出る**。黙って成功にしない ([V4]) |
| E8 | ホスト試験 | 本文の操作 (挿入・削除・改行・折り返しの桁) を**ゲスト抜きで**確かめる。`--mutate` で否定側 |
| E9 | `make check-guest` と `make check` | 通る (回帰) |
| E10 | 既存の `WK_TEXTBOX` を使う画面 (filer ほか) | **見え方と振る舞いが変わっていない**。共通の下請けに寄せた結果 textbox が変わる変異で RED |

**E8 が肝。** 今日のランナーと同じ考え方で、**ゲストが要る部分と要らない部分を分ける。**
本文の操作はホストだけで試験でき、そこが壊れていたら GUI で触っても意味がない。

**E3 は特に落ちやすい。** 日本語は 3 バイトで 2 桁。**桁で折り返す部品**に入れると
UTF-8 の途中で切れる。過去に何度も踏んでいる。

## 6. この票でしないこと

> **機能は最低限でよい** (ユーザー念押し、2026-09-17)。迷ったら**足さない側に倒す**。
> 価値があるのは動くエディタではなく、**通しで使ったときに API の穴が出るかどうか**。
> 機能を足すほど実装に時間が取られ、肝心の「詰まった箇所の報告」が薄くなる。
>
> 必要な線はこれだけ: 開く / 打つ / 保存する、1 画面に入らないファイルを縦に
> スクロールできる、日本語が壊れない、クリップボードと印刷を 1 回ずつ通す、
> 保存せずに閉じようとしたら確かめてくる / 書けない場所なら理由が出る。
>
> 行番号の表示も色分けも複数の窓も**要らない**。
> 「これが無いと通しの受入にならない」と思うものがあれば、**実装する前に PM へ報告する。**


- **取り消し (undo) / 検索 / 置換。** CUI 版にはあるが、**API の穴を見つける目的には要らない。**
  欲しくなったら別票。
- CUI 版 `apps/edit/` の変更。**そのまま残す。**
- 複数の窓で同時に開くこと。
- 構文の色分け、行番号以外の飾り。
- **8bpp バックエンド (R2) と About** — v1.4 の残り 2 件だが**別の票**。

---

## 7. ゲスト受入 (PM、2026-09-17)

`make all` → **`make external`** (SDK の Rust stub が変わったため) → `make deploy`
→ ゲストの `hsync` → **`hsync sys`** → **リセット**。

| ID | 操作 | 結果 |
|---|---|---|
| E1 | 開く → 打つ → 保存する | **合格**。GUI で打った `hello os32` が CUI の `cat /tmp/e2.txt` と一致 (11 バイト)。状態行に `saved, bytes 11`、変更済みの印も消える |
| E2 / E3 / E4 / E5 / E6 / E7 | スクロール / 日本語 / クリップボード / 印刷 / 閉じる確認 / 書けない場所 | **未実施** ([V4])。§7-2 の理由 |
| E8 | ホスト試験 | **合格**。BASE 34 件 GREEN、変異 9/9 RED |
| E9 | `make check` | **合格** (EXIT=0)。`make all` / `make external` も 0 |
| E10 | 既存の `WK_TEXTBOX` が変わっていない | **合格**。変異 `textbox_overflows` / `textbox_boundary_is_one_byte` が RED |

起動画面: 窓・ボタン列 (`Open Save Copy Paste Print Quit`)・編集面・状態行が揃う。
打鍵で本文が入り、未保存の印 `*` が付き、保存で消える。

### 7-1. 踏んだ罠 — **`hsync` は既定で `/sys` を外す**

**最初の起動で窓が出なかった。** 例外も中断も 0 件で、`launch_orphan_count` だけが増えた。

原因は**共有ライブラリが古いままだったこと**。ジャンプ表を 111 → 118 本に増やしたのに、
ゲストの `/sys/lib/libos32gui.shlib` は **113,752 バイト** (手元は 125,384 バイト) のまま。
`bind()` が本数の不一致で `sys_exit` していた。

`hsync` は既定で `/sys` を外す (稼働中のシェルと共有ライブラリのため)。
**`hsync sys` と明示し、さらに再起動が要る** (共有ライブラリは起動時に読まれる)。
`hsync sys` 自身がそう案内していた。

**GUI アプリを入れ替えたら `hsync sys` + リセット。** 静かに窓が出ないだけなので気づきにくい。

### 7-2. 未実施の理由 — **検証道具が大文字を送れない**

`tools/gui_gate.py` の `key(text=...)` で `ABC` を送ると `abc` になる。
**CUI のシェルでも同じ**なので **エディタの問題ではなく注入道具の制約**。
`_` は通るのに英大文字だけ落ちる。

そのため**日本語 (E3) とクリップボード (E4) と印刷 (E5) は通せなかった** —
FEP の起動にも大文字や特殊キーが要る。**道具側を直さないと E2〜E7 は自動では確かめられない。**

これは [`../realhw/PLAN.md`](../realhw/PLAN.md) の「ホスト側のシリアル実装」と同じ性格の穴で、
**アプリではなく検証の側**にある。別票に値する。

---

## 8. 受入の続き (PM、2026-09-18) — キー注入を直した後

[`../tools/TASK_KEY_INJECT.md`](../tools/TASK_KEY_INJECT.md) で大文字が届くようになったので再開した。

### 通ったもの

| 見たもの | 結果 |
|---|---|
| **設定レジストリ** | **合格**。起動すると**前回保存した `/tmp/e2.txt` を覚えて開き直す**。§4 の「設定を通す」の実証 |
| 開く (ファイル選択) | **合格**。`/` → `tmp` と辿れ、**一覧の縦スクロールも動く** (`DOWN` で `long.txt` まで送れた) |
| 30 行のファイルを開く | **合格**。16 行ぶんが表示され、本文は壊れない |
| 大文字の入力 | **合格**。`ZZZ` がそのまま入り、変更済みの印 `*` も付く |

### **穴 H13 — 編集面でカーソルキーが効かない**

**30 行のうち 16 行しか見えず、下へ送れない。**

切り分け済み:

- **文字は届く** (`ZZZ` が 1 行目に入った)。つまり**フォーカスは編集面にある**。
- **注入側には矢印キーがある** (`aidebug_keys.cpp` の `UP` 0x3a / `DOWN` 0x3d /
  `LEFT` 0x3b / `RIGHT` 0x3c / `ROLLUP` / `ROLLDOWN`)。
- **ファイル選択の一覧では `DOWN` が効く**。同じ注入で受け手が違うと効かない。

→ **受け手 (`WK_TEXTAREA` のキー処理) がカーソルキーを扱っていない。**

**受入 E2 (縦にスクロールできる) は、これがあるので通せない。**
本文の保持と表示の切り出しはホスト試験で固定されているが、**動かす口が無い。**

### 影響 — 残りの受入が止まる

| ID | 状態 |
|---|---|
| E2 (縦スクロール) | **H13 で止まる** |
| E3 (日本語) | 未実施。FEP は `SHIFT+SPACE` で起動できるが、**確定後にキャレットを動かせないと確かめきれない** |
| E4 (クリップボード) | 未実施。**選択にカーソルキーが要る** |
| E5 (印刷) | 未実施 (H13 とは独立。次に確かめられる) |
| E6 (閉じる確認) | 未実施 (同上) |
| E7 (書けない場所) | 未実施 (同上) |

**H13 を直すのが先。** 直せば E2 / E3 / E4 が一度に動く。
E5 / E6 / E7 は H13 と独立なので**先に確かめてもよい**。

---

## 9. 穴 H13 の原因と修正 (PM、2026-09-18)

### 9-1. §8 の見立ては外れていた

§8 は「受け手 (`WK_TEXTAREA` のキー処理) がカーソルキーを扱っていない」と書いたが、**違った。**
`app.rs` の `GUI_EV_KEY` は、ウィジェット側の処理の結果に関わらず**アプリの `on_key` を必ず呼ぶ**。
キーはアプリに届いていた。

### 9-2. 本当の原因 — `widget::set_focus()` が通知を捨てていた

フォーカス移動の通知 (`WEV_FOCUS`) には**入口が 2 つ**あった。

| 入口 | 通知 |
|---|---|
| 入力から合成 (TAB / クリック) — `on_key` / `on_button_down` | `WidgetOut` でループへ返り、`on_widget_focus` として配られる |
| **アプリが自分で呼ぶ — `widget::set_focus()`** | **`WidgetOut` を作って捨てていた。どこにも返らない** |

`edit_gui` はフォーカスを `on_widget_focus` だけで追っていた
(`widget::focused()` は**窓スロットの添字**を取り、アプリが持つ WindowId から引けない — §8 の「報告 2」)。
そのため:

1. ボタンを押す → `WEV_FOCUS`(ボタン) が届く → `focus_area = false`
2. `on_click` の末尾が `set_focus(編集面)` で戻す → **通知が来ない**
3. `focus_area` は **false のまま固定**

文字は `GUI_EV_TEXT` → フォーカス中のウィジェット経由で入り `focus_area` を見ない。
だから**アプリ側の門を通る編集キーだけが落ちた**。

**「起動直後は効くのに、一度ボタンを押すと死ぬ」**性質だった (`focus_area` の初期値は `true`)。
§8 の調査が `Open` でファイルを選んでから試したので、最初から死んで見えた。

### 9-3. 修正 — 通知を溜めてループが配る

契約もジャンプ表 (118 本) も変えていない。

- `UiState.pending_focus` — アプリ発の通知の待ち行列 (1 件、最新が勝つ)
- `widget::set_focus()` — `WEV_FOCUS` をそこへ溜める / `take_pending_focus()` で取り出す
- `app.rs` の `drain_pending_focus()` — **窓を組む間のぶん**と、**ハンドラの中で移したぶん**
  (1 件配送するごと) の 2 か所で配る。往復には上限 `FOCUS_DRAIN_MAX`

**アプリは 1 行も変えていない。** ライブラリが約束を守るようになっただけで直った。

### 9-4. 受入の結果 (2026-09-18、ゲスト実測)

`make all` → `make external` → `make deploy` → `hsync` → **`hsync sys`** → リセット。
ゲストの `/sys/lib/libos32gui.shlib` が **125,404 バイト**で手元と一致することを確認 ([V4]、§4-42)。

| ID | 操作 | 結果 |
|---|---|---|
| **E2** | 30 行のファイルを縦にスクロール | **合格**。`DOWN`×20 で 01〜16 行 → 06〜21 行、さらに 16〜30 行 (末尾) まで。**本文は壊れない** |
| **H13** | **ボタンを押した後**に `DOWN` | **合格**。Copy を押してから `DOWN`×10 で 06〜21 → 16〜30。直前まで 1 行も動かなかった経路 |
| E6 | 変更したまま Quit | **合格**。`The text has changed. Save it before closing?` (Yes / No)。黙って捨てない |
| E9 | `make check` | **合格** (EXIT=0)。`docs/TESTS.md` の再生成が要った (試験が `app.rs` も読むようになったため) |
| E8 | ホスト試験 | **合格**。BASE GREEN。静的検査に §9-3 の配線規則を追加し、**通知を捨てる版で STATIC FAIL になることを実測** |
| 設定レジストリ | 起動時に前回のパスを復元 | **合格**。`/tmp/long.txt` を覚えて開き直す |
| **E4** | クリップボード | **不可 (環境)**。`copy failed, error -100` = `HOST_ELINK`。§9-5 |
| **E5** | 印刷 | **不可 (環境)**。同上。未実施 |
| E3 | 日本語 | 未実施。H13 は外れたので**次に確かめられる** |
| E7 | 書けない場所へ保存 | 未実施。Save As は `path_len == 0` のときだけ出るので、手順を組む必要がある |

### 9-5. E4 / E5 が通らない理由は**アプリではなく環境**

`-100` は `HOST_ELINK`「STALE / リンク未確立」(`userland/lib/host/libos32host.h`)。
Host Services はホスト側の常駐 `tools/host_agent.py` と話すが、**常駐が動いておらず、
`np21x64w.ini` に LAN の設定も無い**。通すには常駐の起動と ini の LAN 設定が要り、
**後者は [D2] の承認対象**。

**アプリの振る舞いは正しい** — 失敗を黙って成功にせず、状態行に理由 (-100) を出す ([V4])。
詳細と「撮る間合いの罠」は [`POLICY_DEBUG.md`](../../POLICY_DEBUG.md) §4-45。

### 9-6. 残っている API の穴 (未修正、報告のみ)

**`widget::focused()` がアプリから使えない。** 引数が**窓スロットの添字**で、アプリが持つのは
WindowId。添字を引く口が公開されていない (`os32gui_w_resolve` はウィジェット用)。
そのためアプリはフォーカスを**自分で追うしかなく**、それが H13 を踏む前提になっていた。

直すには既存の輸出 `os32gui_w_focused` の引数の意味を変えるか、末尾に 1 本追記することになる。
**どちらも契約の変更 / ジャンプ表の追記なので、ユーザーの承認を待つ** (決裁 A1 の凍結範囲)。
H13 自体は §9-3 で塞がっているので、急がない。

---

## 10. 穴 H16 — **開いて保存するだけでファイルが伸びる** (PM、2026-09-18)

E3 (日本語) を通す途中で出た。**票が狙っていた「人が使う経路を通したら露見する穴」そのもの。**

### 10-1. 何が起きたか

275 バイトの日本語ファイルを開き、全角 2 文字 (6 バイト) を消して保存したら
**`saved, bytes 270`**。269 のはずだった。編集せずに開き直して保存すると **271**。
**編集しなくても、開いて保存するたびに 1 バイトずつ増える。**

### 10-2. 原因

`Doc::load` が**末尾の改行を空行 1 本として数えていた** (`nlines = li + 1`)。
`save_file` は行ごとに改行を書くので、その空行にも改行が付く。

```text
"L1\nL2\n"  → 3 行 (L1, L2, "")  → 保存 → "L1\nL2\n\n"  (+1)
                                   → 次に開くと 4 行 → 保存 → +1 ...
```

**単調に増える。** 内容は壊れないが、**ユーザーが触っていない部分をアプリが書き換えている。**

### 10-3. なぜ試験が捕まえなかったか

- `load_file_round_trips` は**行の中身しか見ていなかった** (`d.line(0)` / `d.line(1)`)。
  `nlines` も、保存し直したバイト列も見ていない。
- `save_writes_every_line_with_a_newline` に至っては
  **`"ab\nあ\n"` → `"ab\nあ\n\n"` を期待値として書いており、不具合を固定していた。**

**往復を「行の中身」で見ると通り、「バイト列」で見ると落ちる。** 保存の試験は
**入れたものと出てきたものをバイトで突き合わせる**こと。

### 10-4. 直し方

`load` の末尾で、最後の区切りが空なら行として数えない。

```rust
self.nlines = if len == 0 && li > 0 { li } else { li + 1 };
```

**本物の末尾の空行は残る** (`"a\n\n"` は 2 行のまま。その行の終端の改行は 1 つ前で数え終えている)。
末尾に改行の無いファイルには 1 本足す (正規化。これは仕様)。

### 10-5. 試験

- `save_writes_every_line_with_a_newline` の期待値を正した
- `load_then_save_is_byte_identical` — 4 通りの入力で**バイト列が一致**すること
- `saving_over_and_over_does_not_grow_the_file` — 4 往復して長さが変わらないこと
- `a_real_blank_last_line_survives` — 修正が本物の空行を食っていないこと
- 変異 `trailing_newline_grows_the_file` (元に戻す) が **RED** になることを実測

### 10-6. E3 の受入 (ゲスト実測)

`/tmp/jp.txt` (275 バイト、5 行、全角 43 文字の行を含む)。

| 見たもの | 結果 |
|---|---|
| 表示 | **合格**。□ が 1 つも出ない |
| 折り返し | **合格**。86 桁の行が **68 桁で文字境界ちょうど**に折り返る (`まみむめ` / `もやゆよ`)。UTF-8 は割れない |
| 全角と半角の混在 | **合格**。`日本語abc英数字def混在ghi…` が 1 行に収まる (66 桁) |
| カーソル移動 | **合格**。`RIGHT`×5 で全角 5 文字ぶん進む |
| 削除 | **合格**。`BS`×2 で `え` `お` が**まるごと**消える (バイト単位で割れない)。折り返しも組み直る |
| 保存の往復 | **合格 (H16 修正後)**。2 往復しても **275 バイトのまま**。ゲストから吸い出して
  ホストの元ファイルと **md5 一致 / `cmp` で IDENTICAL** |

**E3 合格。**
