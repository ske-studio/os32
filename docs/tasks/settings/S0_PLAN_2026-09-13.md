# S0 — 設定レジストリの着手計画 (PM 提案、2026-09-13)

状態: **決裁済み (2026-09-13)** — 4 点とも提案どおり承認。追加決裁: **初期値 DB はホスト側でビルド毎に生成し、インストーラ媒体に添付する** (通常配備には載せない)。実装票は [TASK_S0.md](TASK_S0.md)。正典は [DESIGN.md](DESIGN.md) と [S0_FOUNDATION.md](S0_FOUNDATION.md)。
本書は「いま着手するもの」と「後回しにするもの」を決めるための縮約案で、契約の正典を書き換えない。

## 1. 現状 (コードで確認、2026-09-13)

| 項目 | 状態 |
|---|---|
| F1 DB slot の owner 別回収 | **着地済み** (`kapi/kapi_db.c` `db_cleanup_owned`、`exec_reclaim_owned` から呼ぶ)。ただし順序は `vfs_close_owned` (FD) → `shm_free_owned` → `db_cleanup_owned` で、S0 §3 F2 が求める「DB を FD 回収より先」は**未達** |
| F2a FD lease / F2b 接続専用 VFS group | 内部基盤として着地 (`fs/vfs_fd.c`、`lib/sqlite3/os32_sqlite_vfs.c`、host 試験 3 本)。呼び出し側 (KAPI / FEP / exec) は**未移行** |
| KAPI `db_*` | v42 の 10 本のまま。`sqlite3_open` (RW+CREATE) のみ、**bind 無し**、exec は先頭 statement だけ |
| ツール・資産 | `tools/mk_settings_db.py`、`assets/settings/*.tsv`、`cfg` コマンド、`libos32cfg` はいずれも**未着手** |
| gshell | 設定として読む値は**まだ無い** (壁紙・色・taskbar は固定) |
| KAPI 版 | v49 まで実装。v43 はネットワーク予約。設定は **v50** が次 |

## 2. 縮約案 (着手順)

S0_FOUNDATION の F0〜F5 / D0 / S1〜S6 のうち、**v1.3 で settings.db を実際に使う**ために要る最小を先に通す。

| 段 | 内容 | レーン | 依存 |
|---|---|---|---|
| **S0-K** | KAPI **v50** = S0 §4 の 7 本を末尾追記: `db_open_existing(path, writable)` (RO / RW、CREATE 無し、`:memory:` / URI 拒否)、`db_prepare_only`、`db_bind_int/text/blob/null`、`db_error_code`。既存 10 本は不変。`shm_write_row` の境界検査 (F5 の一部)。**F2d の順序修正** (`db_cleanup_owned` を `vfs_close_owned` より先に) をここに含める (呼び出し順の 1 行 + host 試験。FD lease の完全統合 F2c/F2d 全体は後回し) | K | — |
| **S0-D** | **D0 配備保護**: `tools/nhd_deploy.py` / `hostdrv_deploy.py` / `prune_stale.py` の実コピー直前に共通判定を置き、`/etc/settings.db` `settings.db-journal` (+ `-wal` `-shm` `.bak`) を通常配備で**作らない・上書きしない・消さない** (manifest / glob / sync-from-hostdrv の全経路)。host 試験 | ツール | — |
| **S0-T** | `assets/settings/defaults.tsv` (初期値の正典) と `tools/mk_settings_db.py` (tsv → settings.db、ホストの sqlite3 モジュール)。生成物は**通常配備に載せない** (S1 の隔離出力) | ツール | — |
| **S2** | `libos32cfg` (C89、静的リンク、S0 §2 の契約: RO+no-create で schema 検査、欠損は fallback、書きは `cfg_begin` の明示、int/text/blob の上限、enum は private copy) と `cfg get\|set\|list\|export\|import` コマンド、libos32gui の末尾追記 (101〜)。host TDD は実 SQLite + 実 VFS で | C | S0-K |
| **S4** | gshell が起動時に `gshell` scope を読む最初の消費者 (デスクトップ色・壁紙の有無・時計の表示形式など**数キー**)、書き戻し点は「設定が変わったとき」だけ (X4 では触らない) | W | S2 |
| **S5** | 実測 (DESIGN §6: プール、ジャーナル、同期、速度、大きさ、メモリ) と受入 | PM / テスター | S2, S4 |
| 後回し | S3 リカバリ (`install --recover-settings`)、S6 `tar` (microtar)、F3a/b/c (VFS の失敗と排他の正直化)、F2c (FEP の group 移行)、FEP_BOUNDARY、MEMORY_RAM_INTEGRATION、DEVICE_RESERVATION | — | v1.3 の後 |

各段は設計 → Codex 独立レビュー (網羅指示つき、3 往復) → 実装 (worktree コーダー、ホスト TDD) → Codex 実装レビュー → 配備 → 受入、の T9 と同じ流儀。

## 3. ユーザー判断が要る点

1. **KAPI v50 (7 本) を取るか**。DESIGN 初版の「KAPI 追加なし」は S0 の静的監査で不成立と判定済み (bind 無しでは SQL 引用 / hex 化になり、RO+no-create も保証できない)。ABI break は全体 clean rebuild で吸収 (F0 の方針、承認済み)。
2. **欠損時の生成をどうするか**。DESIGN は「初期値はインストール媒体だけが持ち、通常運用では自動生成しない」。開発中の NHD には settings.db が無く、リカバリ (S3、FDD ブート) は後回しにする案なので、**明示コマンド `cfg init`** (無いときだけ、配備した `/etc/settings.tsv` の初期値から生成。既存があれば拒否) を認めるか。自動生成ではないので DESIGN の趣旨 (誤操作で設定を失わない) は保つ。
3. **F2 の扱い**: 順序修正 (DB → FD) だけを S0-K に含め、FD lease の完全統合 (F2c/F2d の全体) は後回しでよいか。cfg は「開いて読んで閉じる」「書きは 1 トランザクション」なので、子の異常終了で残るのは最悪 hot journal 1 つで、次の RO open が検出して通知する (自動回復しない) 契約で受ける。
4. **S4 の最初の消費者**: gshell の何を設定にするか (案: デスクトップ色、taskbar の時計 12/24h、壁紙のパス (無ければ単色))。少なくて良い。`assets/filetypes` の DB 移行 (ファイラの関連付け) は次段。

決裁後に S0-K / S0-D / S0-T の 3 票を切り、Codex 設計レビューへ回す。
