/* ======================================================================== */
/*  BOOT_SPLASH.H - ブートスプラッシュ画面                                   */
/* ======================================================================== */

#ifndef BOOT_SPLASH_H
#define BOOT_SPLASH_H

/* ブートスプラッシュを表示 (3秒間)
 * PC98 標準表示のみ使用。任意デバイスの probe は行わず、GUI の希望設定は保存。
 * GFXモードの初期化・終了・VRAM/パレットクリアを全て内部で行う。
 * 呼び出し後はテキストモードに復帰済み。 */
void boot_splash(void);

#endif /* BOOT_SPLASH_H */
