# 移植準備 順序 4-a — 外部形式 (LE) の直アクセス検査 (試験の記録)

- 票: 移植準備 順序 4-a (媒体・書庫の上のバイト列を `include/endian_le.h` のアクセサへ統一する)。
  数字の正典は [`docs/tasks/portability/ARM_GAUGE.md`](../../docs/tasks/portability/ARM_GAUGE.md) §9
  「順序 4-a でも数字は動かない — この計測器が測っていないものだから」
- 実行: `python3 tools/check_le_access.py` (`make check-le-access`)
- 対象: `tools/check_le_access.py` の `GUARDED` に並ぶ 9 ファイル
- 関連: [`SURVEY_N1.md`](../../docs/tasks/portability/SURVEY_N1.md) の 2026-09-15 追記 (順序 4-a で解消)

## 0. 正直に書く ([V4])

**検査は置き換えのあとに書いた。** `fs/ext2_*.c` / `fs/iso9660.c` /
`drivers/kcg.c` / `lib/utf8.c` の `*(u32 *)&buf[off]` を先にアクセサへ
置き換え、そのあとで `tools/check_le_access.py` を書いて
「置き換え漏れが 0 である」ことと「今後増えない」ことを固定した。
だから「先に RED を見た」とは書けない。

ただし **RED の数字は残っている** — 置き換え前の `-Wcast-align=strict` の
警告数 163 件 (§2 の表) がそれで、これは
[`ARM_GAUGE.md`](../../docs/tasks/portability/ARM_GAUGE.md) §9 に実測として
記録されている。この文書はその根拠を `docs/TESTS.md` から名前で引けるように
するために 2026-09-15 に起こしたもの。

これは TDD の記録ではなく**静的検査の記録**なので、ハーネスも `--mutate` も無い。

## 1. なぜ要るのか

媒体の上に並ぶバイト列 (ext2 の superblock / group descriptor / inode /
ディレクトリエントリ / 間接表、ISO9660 のレコード、KCG フォント書庫のヘッダ) を
`*(u32 *)&buf[off]` で読み書きすると、**2 つの前提が同時に隠れる**:

1. CPU がリトルエンディアンであること
2. 非アラインアクセスが許されること

x86 ではどちらも成り立つので動いてしまう。ARM (`SCTLR.A=1`) では 2 で落ち、
ビッグエンディアンの CPU では 1 で値が化ける。外部形式は
`include/endian_le.h` の `le16_rd` / `le16_wr` / `le32_rd` / `le32_wr`
(バイト単位で組み立てる純 C) を通すこと。

**この穴は ARM コンパイル計測 (`make check-arm-compile`) には現れない。**
`*(u32 *)&buf[off]` は ARM でも普通にコンパイルできて、壊れるのは実行時だから。
実際、置き換えた 8 本のうち 7 本は順序 1 の時点から ARM を通っていた。
だから物差しを別に置いたのがこの検査。

## 2. 何を検査するか — 2 段

**(1) 文字列検査** — 対象ファイルに `*(uNN *)&` の形が無いこと。
コンパイラが要らないので、どの環境でも必ず走る。

**(2) コンパイル検査** — 対象ファイルを `-Wcast-align=strict` で単体コンパイル
(`-fsyntax-only`) し、警告が 0 であること。文字列検査をすり抜ける書き方
(`u32 *p = (u32 *)buf;` と 2 行に分ける等) はこちらが捕まえる。

`-Wcast-align` の既定は「非アラインアクセスを許す CPU では黙る」ので、
x86 のホストで見張るには **`=strict` が要る**。フラグとインクルードは
`build/config.mk` を読んで実ビルドに揃える (写すと二重管理になる)。
読み方は `tools/check_arm_compile.py` と共有する。

コンパイラが見つからない環境では (2) を SKIP して (1) だけで通す。
[V4] のとおり、飛ばしたことは出力に書く。

### 対象 (`GUARDED`) と、置き換え前後の警告数

| ファイル | 順序 4-a の前 | 後 |
|---|---|---|
| `fs/ext2_dir.c` | 29 | 0 |
| `fs/ext2_fmt.c` | 54 | 0 |
| `fs/ext2_inode.c` | 46 | 0 |
| `fs/ext2_super.c` | 28 | 0 |
| `drivers/kcg.c` | 3 | 0 |
| `lib/utf8.c` | 3 | 0 |
| `fs/ext2_file.c` / `fs/ext2_vfs.c` / `fs/iso9660.c` | 0 | 0 |
| **計** | **163** | **0** |

## 3. GREEN (現状、2026-09-15)

```
=== 外部形式 (LE) の直アクセス検査: OK (9 ファイル) ===
    i386-elf-gcc -Wcast-align=strict — 警告 0
```

## 4. 壊すとどうなるか

| 壊し方 | どちらが捕まえるか |
|---|---|
| `le32_rd(buf, off)` を `*(u32 *)&buf[off]` に戻す | (1) 文字列検査 — 行番号つきで報告 |
| `u32 *p = (u32 *)buf; v = p[0];` と 2 行に分ける | (2) `-Wcast-align=strict` — (1) はすり抜ける |
| `-Wcast-align` を `=strict` 無しにする | **どちらも黙る** (x86 では警告が出ない)。この検査自体の前提なので、フラグを緩めないこと |
| 新しく外部形式を触るファイルを書いて `GUARDED` に足し忘れる | **どちらも黙る**。§5 参照 |

## 5. この検査が言わないこと

- **`GUARDED` に無いファイルは検査されない。** 新しく外部形式を触るファイルを
  書いたら手で足すこと。逆に、ext2 のビットマップのように 1 バイトずつ触る
  コードは元から移植性があるので足す必要はない。「足し忘れを検出する仕組みは
  無い」というのが今のところの穴。
- **等価性 (x86 で振る舞いが変わらないこと) は見ていない。** アクセサが
  インライン展開されると命令列は変わりうるので `.o` の突き合わせは成立しない。
  等価性は **B8 の試験と実物の `e2fsck`** で見た — `test_b8_open.py` が
  1461 件 0 失敗、`e2fsck` 654 像で自前の媒体検査との食い違い 0
  ([`b8_tdd.md`](b8_tdd.md))。
- **ビッグエンディアンで実際に動くことの証明ではない。** BE の実機も
  エミュレータも回していない。この検査が言えるのは「LE 前提とアライン前提を
  ソースから消した」ことまで。
