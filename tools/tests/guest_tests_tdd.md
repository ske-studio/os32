# ゲスト一括実行ランナー — ホスト試験の記録 (RED→GREEN)

- 票: [`docs/tasks/test/TASK_TEST_RUNNER.md`](../../docs/tasks/test/TASK_TEST_RUNNER.md)
  — 受入 **R7** (「生成と集計はホストだけで試験でき、そこが壊れていたらゲストで
  回しても意味がない」)。R1〜R6 / R8 の**ホストで見える側**もここで踏む。
- 前段: [`docs/tasks/test/TASK_TEST_RESULT.md`](../../docs/tasks/test/TASK_TEST_RESULT.md)
  §2 の約束事 (終了コード 0/1/2 と集計行 `<名前>: PASS n/m`)。
  その記録は [`result_conv_tdd.md`](result_conv_tdd.md)。
- 実行: `python3 -B tools/tests/test_guest_tests.py [--mutate]`
  (`make check-guest-host` が同じものを `--mutate` 付きで回す)
- 対象: `tools/guest_tests.py` (実物を `import`) / `tools/tests/guest_tests.txt` (実物を読む)

## 0. この試験が**触らないもの**

**NP21/W・エミュレータ・HTTP・実時間に一切触らない。** 待ち時間は 1 秒も無い。
ゲストは贋物 (`FakeGuest`)、時計は贋物 (`Clock`)、出力ファイルだけが本物の
一時ディレクトリのファイル。だから `make check` の列に入れてある。

**ゲストで 1 度も走らせていない** ([V4])。`make check-guest` (本番) の結果は
この記録には無い。

## 1. なぜ「純粋な部分」と「触る部分」に割ったか

票 §5 の R7 がそう要求している。壊れると痛いのは

- 一覧 → **スクリプトの生成**  (順序・3 行の形・切り詰め)
- 出力 → **集計**              (終了コードと集計行の突き合わせ)
- **食い違いの検出**           (`0` なのに `FAIL`、`1` なのに `PASS`)
- **見張りの判定**             (伸びなくなった / 上限に当たった)

の 4 つで、どれも**エミュレータにも時計にも関係が無い**。`tools/guest_tests.py`
はこれを上半分の純関数に置き、下半分 (HTTP・ファイルの見張り・rshell の
開き直し) を薄く保って `run_suite(guest, ..., now=, sleep=, log=)` で
**注入できる**形にしてある。

| 純粋 (贋物の入力だけで全部踏める) | 触る (注入して同じ道を通す) |
|---|---|
| `parse_list` / `build_script` | `Np21Guest` (`/api/cmd` `/api/key`) |
| `parse_output` / `parse_summary` | 出力ファイルの読み取り・大きさ |
| `classify` / `aggregate` / `format_report` | `reopen_shell()` (ESC → `rshell` → `ver`) |
| `Watch` / `running_test` / `hostdrv_verdict` | `resolve_hostdrv_dir()` |

HTTP は `tools/np21w_mcp/np21w_client.py` を借りている (WSL 直結と Windows の
`curl.exe` の切り替えを既に持っているので作り直さない)。

## 2. RED (最初に踏んだところ)

試験を先に書いて回したときの実際の出力 (7 件):

```
== 5. 集計 ==
  FAIL 報告にも後続の cc が出る
== 12. 通し (全体の上限) ==
  FAIL 上限に当たったら非ゼロ (得たもの: 2 / 期待: 3)
  FAIL 上限に当たったと記録する
== 13. 通し (/host が無い) ==
  FAIL /host が無ければ前提不足で非ゼロ (得たもの: 2 / 期待: 4)
  FAIL 報告を作らない (合格にしない)
  FAIL 前提が無い時点で止まる (走らせない) (得たもの: 1 / 期待: 0)
  FAIL 断る理由を言う [V4]
```

中身は 3 種類だった。

1. **実装の欠陥 (直した)** — 「固まった」ことを `stalled_at` (名前) 1 つで
   表していたので、**固まったが名前が分からない**ときに
   `report_exit_code()` が「固まった」を見落として `2` を返していた。
   票 §3 は「どれで止まったかを言う」ことを求めているが、**名前が分からない
   ことは「止まっていない」ことではない**。`Report.stalled` (起きたか) と
   `Report.stalled_at` (どれか) を分け、終了コードは前者で決めるようにした。
2. **試験の台本の誤り (試験を直した)** — 上限の場面を「100 秒おきに出力」と
   書いたが、1 本あたりの見張り (60 秒) のほうが先に発火する。**伸び続けた
   まま全体の上限に当たる**台本 (30 秒おき) に直した。固まったのと上限に
   当たったのは別の事情なので、`not rep.stalled` も見るようにした。
3. **試験の足場の誤り (試験を直した)** — `/host` が無い場面を作るつもりで、
   贋物の下拵えが先に `<置き場>/test/` を作ってしまっていた。置き場が無い
   ときは作らないようにした。

## 3. GREEN (現状)

```
HOST 0 checks failed
```

| 節 | 見ているもの |
|---|---|
| 1 一覧 | コメント・空行、名前は最初の語、引数は行ごと保つ、**名前の重複は誤り**、空の一覧は誤り、**実物の `guest_tests.txt`** が読めて第 1 陣が入っていること、画面デモ・対話・ベンチ・落ちるのが正解のものが**入っていない**こと |
| 2 生成 | 先頭は `>` で切り詰め (R8)、試験ごとに**ちょうど 3 行**、引数つきもそのまま、最後に完了印、`;`・ループ・パイプを使わない (シェルに無い)、**全件が無条件に並ぶ** (1 本の事故で後続が消えない)、並びは一覧のとおり |
| 3 出力 | BEGIN/DONE の合い言葉、出た順、`RC` を数として、集計行の数と理由、**CRLF でも読める**、**他人の名前で始まる行を自分の集計行にしない**、最後の集計行を採る、`$?` が展開されなかったら `None` (勝手に 0 にしない) |
| 4 突き合わせ | **票 §2-4 の表がそのまま**。0+PASS / 1+FAIL / 2+SKIP、予約値 139/126/127/130 は集計行が**出ないのが正常**、そして**食い違い 9 通り** (0 なのに FAIL、1 なのに PASS、1 なのに SKIP、集計行が無い、数が釣り合わない、落ちた値なのに集計行がある…)。合格に数えるのは `pass` と `skip` だけ |
| 5 集計 | 全件合格 → 0、**1 本壊しても残りは走り切る** (R2)、**139 の後続も表に出る** (R3)、食い違いは非ゼロ (R5)、SKIP は不合格と区別、走っていないものは `MISSING`、完了印が無ければ非ゼロ、**合い言葉が違えば中身が全部 PASS でも非ゼロ** (R8)、`RUN` が 2 回、一覧に無い結果 |
| 6 名指し | 始まって終わっていない試験の名前、全部閉じていれば `None`、1 本目で固まっても名指しできる |
| 7 見張り | 伸びていれば待つ、60 秒伸びなければ `stall`、伸び続けても上限で `timeout`、**60 秒未満の指定を断る** ([V3])、上限が 1 本より短い指定を断る |
| 8 `/host` | 置き場が無い門・ゲストの印が無い門を**別々に**踏む、内容が違えば断る |
| 9〜17 通し | 贋のゲスト + 贋の時計で `run_suite` を通す。全件合格 / 139 で後続 / **固まって名指し** (R4) / **上限でもそこまでの結果を出す** / `/host` が無ければ断る (R6) / ゲストに届かなければ断る / rshell が開き直せなければ断る / **2 回続けて回して同じ結果** (R8) / 報告が表になっている |

`run_suite` の通しは `rshell` の開き直しが**先に 1 回だけ**起きること、
`source` で流すこと、前提が無いときは**走らせずに**止まることも見ている。

## 4. 否定側 (`--mutate`) — 10 本すべて RED

実物の `tools/guest_tests.py` を 1 か所ずつ壊して、この試験が**実行時に**
落ちることを見る。どれも Python として正しく `import` は通る。
**「例外で死んだだけ」「構文が壊れただけ」は検出に数えない** — 検査が 1 つも
落ちていない変異は `**検査が 1 つも落ちていない (落ちただけ)**` として不合格に
する。目が働いた証拠は「検査が落ちたこと」だけ。

```
MUTATE 1 食い違い (0 なのに FAIL) を見逃す           RED (6 件)
MUTATE 2 見張りが発火しない                        RED (1 件)
MUTATE 3 固まった試験を名指ししない                    RED (3 件)
MUTATE 4 /host が無いのに合格にする (置き場の門)         RED (2 件)
MUTATE 5 ゲストに /host が無いのを見逃す (印の門)        RED (4 件)
MUTATE 6 落ちた試験 (139) で後続を打ち切る             RED (2 件)
MUTATE 7 前回の出力が混ざるのを見逃す (R8)              RED (1 件)
MUTATE 8 1 なのに PASS を見逃す                   RED (2 件)
MUTATE 9 予約値 (139) を合格として読む                RED (15 件)
MUTATE 10 全体の上限で結果を全部捨てる                   RED (1 件)
```

### 変異が最初に 2 本すり抜けた — 試験のほうを直した

最初に回したとき **4 (置き場の門) と 8 (1 なのに PASS)** が GREEN のままだった。
どちらも**別の門に隠れていた**もので、規則を見ていないのは試験のほうだった。

- **4**: `hostdrv_verdict(False, None, …)` しか踏んでいなかったので、置き場の
  門を消しても**印の門**が拾ってしまう。`hostdrv_verdict(False, "tk", …)` を
  足して、門を 1 つずつ踏むようにした。
- **8**: `classify(1, PASS 3/3)` しか踏んでいなかったので、種別の門を消しても
  **数の門** (`FAIL n/n` は有り得ない) が拾ってしまう。`classify(1, SKIP …)` と
  `classify(1, PASS 1/2)` を足した。`$?=1` なのに `SKIP` は「不合格」と
  「実行しなかった」を混ぜる実害のある形なので、踏む値としても正しい。

**片方だけ見ると、もう片方が壊れても気づけない** — これは票 §2-4 が
終了コードと集計行について言っていることと同じ形の穴だった。

## 5. この試験が言わないこと ([V4])

- **ゲストで 1 度も走らせていない。** 実際にシェルが `source` を受けるか、
  `$?` が生成したスクリプトの中で正しく拾えるか、`>>` が 50 行ぶん効くか、
  `restest all` のように**引数とリダイレクトが同居した行**が字面どおり
  通るかは、**すべて未確認**。票 §1 の実測 (PM が 2026-09-17 に確かめた)
  に寄りかかっている。
- **HostDrv 越しの見え方**は見ていない。ゲストが `/host` へ追記した内容が
  ホスト側で**どれだけ遅れて**読めるか (`os.path.getsize` が伸びを拾えるか)
  は実機でしか分からない。見張りの秒数 (既定 180 秒) はそこで決め直すこと。
- `/api/key` での **rshell の開き直しが本当に効くか**は未確認。形は
  `tools/gui_gate.py` の実績 (ESC → `rshell` → `ver` で確かめる) を写した。
- 一覧の 16 本が**今も**その集計行を出すかは見ていない。載っている値は
  `TASK_TEST_RESULT.md` §9 (2026-09-17) の実測の写し。
